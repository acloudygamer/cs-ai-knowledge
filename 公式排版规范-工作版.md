# 公式排版规范-工作版

> 给 AI 的写作执行规范。判据只有一个：公式活着 = 被服务端包进 `<math-renderer>`；生死全在服务端，MathJax 只排版不判定。按管线阶段排布，逐条执行即可。完整排版规范见 `公式排版/公式排版规范.md`（实测档案 `全量验证.md`、管线推导 `Markdown渲染管线研讨.md` 同目录）。

## Phase 1 · 块结构（候选形成之前就会死）

核心概念：`$$` 块不是独立语法，就是一个"整段恰为 `$$…$$`"的普通段落——段落被拆散、并入、吞噬，块就死。

- `$$` 块必须独占段落：前后各留空行，块内禁止空行。
- `$$` 块内禁止行首 `<div>` 等块级标签、禁止 `---` 行（段落被切断，死）。
- 行首禁止写块级标签包裹公式：HTML 块内公式必死，无例外。
- 表格单元格内的公式禁止裸 `|`，一律写 `\|`。
- 列表项内禁止 `$$` 块：文档前文有任何公式，列表内块即静默失效。列表内用行内 `$…$`，块级移到正文。
- 引用内可用：每行带 `>`，`$$` 前要有 `> ` 空行，`$$` 后禁止无 `>` 的裸文字行。

## Phase 2 · 解析（转义被吃一层、配对照常发生）

核心概念：`\` 先被 Markdown 当作自己的转义声明消耗，损失在此定型，后续阶段救不回来。

- TeX 需要的 `\`+ASCII 标点一律双写：`\%`→`\\%`、`\{`→`\\{`、**`\,`→`\\,`**（间距命令 `\;` `\!` `\:` `\.` 同样被吃，真实公式高频中招）；或把字符移出公式（`$M_1 = 10$%`）。
- 行尾 `\\`（aligned 换行）必须紧贴行尾换行，禁止单行内写 `$$…\\$$`。
- 行尾孤 `\` 会被吃掉（硬换行），禁止依赖它。
- 下标 `_` 两侧是字母/数字时（`x_i`、`M_1`）永不配对，无需转义。
- 行内公式内禁止成对的 `*` `_` `**` `~~`（配成元素即死，见 Phase 3.5 反向）。
- 用 `\\_` 修复 `_` 时，同段禁止出现其他边界位的 `_`（`_foo`、`foo_`、`$_{j}$`）。

## Phase 3.5 · 提取与序列化（生死判定、送达定型）

### 行内边界（只给手段）

- 两端各留一个半角空格：`值为 $x+y$ 个`。
- 公式内部紧贴定界符，不贴空白。
- 闭 `$` 后禁止紧跟字母/数字/`_`；开 `$` 前只允许空格、行首、半角 `(`。

### 容器与标签

核心概念：正向——公式整体落进 `em`/`b`/`a`/`code` 子树，提取时被跳过（死）；反向——任何真实生成的元素边界落进 `$…$` 内部即死（提取要求 `$…$` 是连续文本节点，元素边界一切就断）。

- 公式禁止放进斜体（`*`/`_`）、加粗、链接文字、代码 span；斜体改用 `<i>`。
- 公式内部禁止任何成对标记与手写标签。
- 手写标签必须闭合：未闭合 `<em>`/`<b>`/`<i>` 会跨段污染，标签之后公式全死。

### 内容与字符

- 行内公式内容禁止含子串 `select`、`begin`（词内也算、大小写不敏感）：`\begin{aligned}` 等一律用块级 `$$…$$`。
- 行内公式内 `&` `<` `>` 一律不可得：需要时用块级 `$$…$$`（块级送达正常）。
- 乘号一律 `\cdot`，禁止 `a*b*c`（块级会同化成 `a_b_c`）。
- 块级公式内禁止 `__` `**` `~~`：块照活、不报错、标记静默消失（`__` 会把中间内容一并卷走）。
- 块级公式内禁止链接、代码 span（整块死）、图片（内容被丢弃）。
- 块级内的下标 `_` 无需 `\_`：单行块配上对也字节级往返；多行块配上对会在送达首尾多出空行——多行块内避免边界位的 `_`（如行首 `_{a}` 又遇闭合 `_`）。
- `$$` 与文字同行会被提取成行内（退化）：块级公式一律独占段落，不要用"公式还在"判断成功。

## Phase 4 · 渲染

MathJax 只读 `<math-renderer>` 的文本排版，不做任何判定。服务端没包进去的，永远不会是公式。

## 兜底

`` $`x+y`$ `` 代码包裹免疫转义层与配对（GitHub 专属语法，可迁移性差）。
