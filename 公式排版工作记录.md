# 公式排版工作记录

## 已完成（2026-08-31）

- 文档体系成形：
  - `公式排版规范-工作版.md`（根目录）——AI 写作执行版，按管线阶段组织（Phase 1 块结构 → Phase 2 解析 → Phase 3.5 提取与序列化 → Phase 4 渲染），命令式，只留手段与核心概念。
  - `公式排版/公式排版规范.md`——完整版，全部规则 + 考据。
  - `公式排版/全量验证.md`——实测档案，按规范章节组织，含复跑确认与勘误。
  - `公式排版/Markdown渲染管线研讨.md`——管线推导（Phase 0–4，Phase 3.5 为服务端数学提取）。
- 全部结论经 GitHub `POST /markdown` API 实测（约 700 例，含同日两轮复跑零漂移）。
- 本仓库其他文档写作时遵守工作版（`2-Python/README.md` 写作约定已改指向）。

## 下一轮任务（新对话执行）：工作版独立压力测试

**目的**：验证"只给 AI 工作版"是否足够——不查阅完整版与实测档案，检验工作版能否独立指导写出全部存活的公式。

**任务**：只用 `公式排版规范-工作版.md`，写 100 个人工智能与计算机领域的公式（机器学习、深度学习、概率图、信息论、算法复杂度等；行内与块级混合、简单与复杂混合），逐条送 GitHub `POST /markdown` API 实测。

**方法与纪律**：

- API：`POST https://api.github.com/markdown`，JSON body `{"text": md, "mode": "gfm"}`，header 带 `Authorization: token $(gh auth token)`。
- 判据：返回 HTML 中 `<math-renderer>` 的有无 / class（行内 `js-inline-math`、块级 `js-display-math`）/ inner 与源文逐字比对（注意：块级 `&` `<` `>` 在 HTML 源中以 `&amp;` `&lt;` `&gt;` 单实体形态出现属正常）。
- **教训（必须遵守）**：含反斜杠的测试输入绝不用 bash heredoc 内联（会吞反斜杠产假数据）——必须 Write 写 Python 脚本文件再 `python` 跑；脚本内用原始字符串并逐字校验。
- **测试期间禁止阅读 `公式排版/` 目录下的任何文件**（防止完整版知识泄露污染测试）。
- 每个失败/内容异常案例先按工作版自查：工作版能解释的记为"写法违规"；解释不了的才是规范缺口。测试结束后才允许对照完整版分析归因。

**产出**：失败清单 + 每条归因（写法问题 / 工作版缺口）。若确有缺口，先改 `公式排版/公式排版规范.md` 与 `公式排版/全量验证.md`，再同步工作版。
