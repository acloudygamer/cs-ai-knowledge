# 公式排版未验证项 · 网页渲染测试

> 三个未验证项各一组用例。API 送达形态均已实测，此处只验证网页端 MathJax 渲染。
> 每组标注预期效果，对照实际显示即可。

## 1. 行内公式内的 `<`

- 用例：若 $a < b$ 且 $x < 0$ 成立。
- 预期：两个公式正常渲染，`<` 显示为小于号（`>` 已实测正常，`<` 同机制推定、待验）。

## 2. 行内 `&` 的修复写法 `\\&`

- 用例（修复写法）： $A \\& B$
- 预期：渲染出 `A & B`，字面 & 正常显示，公式不炸。
- 对照（单写 `\&`，预期必炸）： $A \& B$
- 对照预期：渲染失败或显示异常——若对照也正常，说明前端行为有变，需回报。
- 备注：本组首版因 `：` 紧贴开 `$`（贴中文死）从未被提取，已修正为空格分隔后重测。

## 3. strong 配对致块级送达首端空行

### 3a. strong 配对（标记整对丢弃）

$$
a **b** c + d
$$

- 预期送达形态：`$$` + `a b c + d`（`**` 整对丢失，无空行——2026-09-01 API 复测：同行/跨行 strong 配对均不产生空行，档案"首端一空行"未能复现）。
- 待验：内容被改写后的块能否正常排版（显示为 a b c + d）。

### 3b. em 配对（送达首尾各多一空行，顺带复核）

$$
_{a} x_{b}
$$

- 预期送达形态：`$$` 首尾各多一空行 + `_{a} x_{b}`。
- 已网页实测过一次（无影响），此处顺带复核空行对排版的影响。
