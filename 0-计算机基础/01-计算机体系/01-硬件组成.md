# 硬件组成

## 定义

计算机是存储程序式电子计算机，其本质是通过指令序列对二进制数据进行变换的机器。冯·诺依曼架构将指令与数据统一存储在同一个读写存储器中，CPU 通过显式的取指-执行周期驱动所有计算。这一设计的深远影响在于：程序本身成为可以被读写、修改、甚至自生成的数据——图灵完备性的物理载体。

**归约视角**：从数学上讲，任何图灵机可归约为"一个无限状态寄存器 + 一个有限状态控制器"。冯·诺依曼机的物理实现将这个抽象具体化：寄存器文件是状态寄存器，ALU + 控制单元是有限状态控制器，内存是外部存储带。

## 数学模型

**冯·诺依曼瓶颈的定量描述：**

$$
B_{瓶颈} = \min\left(\frac{W_{总线}}{T_{周期}}, \quad N_{CPU} \times \frac{W_{内存带宽}}{T_{周期}}\right)
$$

带宽争用时整机性能受限于最窄总线或内存通道，而非 CPU 算力。当 $N_{CPU}$ 增加（多核化）而总线带宽和内存带宽未同比扩展时，瓶颈效应加剧。

**局部性原理的缓存命中率模型：**

$$
H = \frac{\sum_{i=0}^{L} h_i \cdot C_i}{\sum_{i=0}^{L} C_i}
$$

其中 $h_i$ 为第 $i$ 层命中率，$C_i$ 为第 $i$ 层访问代价（周期数）。真实系统性能由加权命中时间决定：
$$
T_{有效} = (1 - M) \times T_{命中} + M \times T_{未命中}
$$
$M$ 为未命中率。

**存储层次容量-延迟积（Area-Latency Product）：**

$$
AP_i = S_i \times L_i
$$

第 $i$ 层的面积-延迟积反映其"数据缓存能力"。L1 的 AP 最小（32KB × 4周期），L3 的 AP 最大（64MB × 40周期）。

**缓存行（Cache Line）的平均访问代价：**

$$
T_{avg} = (1 - \alpha) \times L_i + \alpha \times (L_i + L_{i+1})
$$

其中 $\alpha$ 为本层未命中且需访问下一层的概率，$L_i$ 为第 $i$ 层延迟。当考虑多层级时，未命中传递形成连锁代价。

**NUMA 架构的内存访问延迟模型：**

$$
T_{内存访问} = \begin{cases}
L_{本地} & \text{访问本地节点} \\
L_{本地} + L_{互连} + L_{远程} & \text{访问远程节点}
\end{cases}
$$

其中 $L_{互连}$（QPI/UPI 延迟）通常比 $L_{本地}$ 高 1.5-2 倍。

## 数据流

<pre>
用户程序（源代码文本）
    ↓ 编译（clang/gcc/llvm）
可执行文件（ELF/PE/Mach-O）
    ↓ 链接器（符号解析 + 重定位）
    ↓ 操作系统加载器
内存空间 [代码段(.text) | 只读数据(.rodata) | 数据段(.data) | BSS | 堆 | 栈]
    ↓ 取指（PC 寄存器 → 地址总线）
CPU 控制单元
    ├──→ IR（指令寄存器，32/64bit 指令字）
    ├──→ ID（译码阶段）
    │    ├──→ 寄存器文件读取（源操作数）
    │    └──→ 立即数符号/零扩展
    └──→ EX（执行阶段）
         ├──→ ALU 运算
         ├──→ 有效地址计算
         └──→ 分支条件判断
              ↓
         MEM（访存阶段，仅 load/store）
         ├──→ L1d cache 查询（4 周期，32KB/SM）
         ├──→ 未命中 → L2 cache（12 周期，512KB/SM）
         ├──→ 未命中 → L3 cache（40 周期，共享）
         └──→ 未命中 → 主存 DRAM（200+ 周期）
              ↓
         WB（写回阶段）→ 寄存器文件或 cache

I/O 设备通过 DMA 直接访问主存（绕过 CPU）
    ↓
总线仲裁（总线争用时 CPU 让出总线）
</pre>

**关键路径延迟对比（典型 x86-64，Zen 3 架构）：**

| 层级 | 容量 | 命中延迟 | 备注 |
|------|------|----------|------|
| L1d | 32KB/SM | 4 周期 | 最短路径 |
| L2 | 512KB/SM | 12 周期 | 核私有 |
| L3 | 32MB（共享） | 40 周期 | 核间互连 |
| 主存 | 32GB+（DDR5） | 200+ 周期 | 内存控制器 |
| NVMe SSD | TB 级 | ~10,000 周期 | PCIe 4.0 x4 |
| HDD | TB 级 | ~10,000,000 周期 | 机械寻道 |

**缓存行（Cache Line）传递数据流：**

<pre>
CPU 核心 0                    CPU 核心 1
    │                            │
    ├──→ L1d (64B line) ←───────┤ MESI 总线广播
    │                            │（当核心1修改同一 line 时）
    │                            │
    └──→ L2 ──→ L3 ──→ DRAM ←───┘
                      ↑
              cache line 失效通知
              + 数据回写（若 dirty）
</pre>

## 机制

### 冯·诺依曼架构

程序与数据共存于同一内存，CPU 通过共享总线访问。这一设计在 1945 年是天才之举，但它创造了一个根本矛盾：CPU 与内存之间的语义不对等——CPU 以 GHz 运行，内存带宽以 GB/s 计量，两者相差 2-3 个数量级。

**本质问题**：CPU 计算速度增长（摩尔定律）远超内存带宽增长，导致算力无法被充分利用。缓解手段是缓存层次结构。

**约束条件：**
- 内存访问带宽决定整机性能上限，CPU 算力无法完全发挥
- 总线争用导致 CPU 与 I/O 设备竞争访问权
- 指令流和数据流共享同一物理通道

**违反后果：**
- 当缓存命中率低于 95% 时，CPU 大部分时间在等待数据
- DMA 设备与 CPU 并发访问内存时，若无一致性协议协调，会导致缓存脏数据问题
- 多核时代缓存一致性协议（MESI/MESIF）成为性能瓶颈——核间通信延迟可达 100+ 周期

### 内存层级

容量与速度的物理折中由硅片面积和 DRAM 访问原理决定：SRAM 每个 bit 需要 6 个晶体管，DRAM 只需要 1 个电容+1个晶体管，但 DRAM 需周期性刷新且访问更慢。

**Inclusive vs Exclusive Cache：**
- Inclusive（Intel 常用）：L1d ∩ L2 = L1d，L3 包含所有上行数据。优点是 L1d/L2 未命中时可直接检查 L3，无需下探主存。
- Exclusive（AMD Zen 常用）：L1d ∪ L2 ∪ L3 = 全部数据，无重复。优点是总容量最大化，缺点是查询路径更长。

**缓存一致性协议（MESI/MESIF）：**
- **M（Modified）**：本核独占且已修改，脏数据，不共享
- **E（Exclusive）**：本核独占，未修改，与主存一致
- **S（Shared）**：多核共享，与主存一致
- **I（Invalid）**：行无效

总线嗅探（Bus Snooping）保证所有核看到一致的缓存状态。

**约束条件：**
- L1/L2/L3 延迟逐级递增（4/12/40 周期典型值）
- 容量逐级递增（32KB/1MB/64MB 典型值）
- 包含策略决定一致性协议复杂度

**违反后果：**
- 数据局部性差导致缓存未命中——每次未命中需从下级存储加载整个 cache line（通常 64 字节）
- 伪共享（False Sharing）：两个核修改同一 cache line 的不同字节，导致 MESI 协议下的写穿透，性能骤降数倍

### 存储层次

速度/容量/成本构成不可能三角：SSD 的延迟是 DRAM 的 1000 倍，HDD 的延迟是 SSD 的 1000 倍，但每 GB 成本也相应降低 1000 倍。

**NVMe 消除协议栈开销：**
传统 SATA SSD 需经过 南桥 → SAS/SATA 控制器 → AHCI 协议层，路径漫长。NVMe 直接挂在 PCIe 总线上，协议专为 NAND Flash 特性优化（多队列、并行化），延迟降低 50%+。

**约束条件：**
- HDD 机械寻道限制吞吐量（~100MB/s 顺序读，~1MB/s 随机读）
- SSD 无机械部件但写入寿命有限（TBW：500 TBW/1TB 典型）
- NVMe PCIe 直连消除协议开销（5-7 GB/s vs SATA SSD 的 550 MB/s）

**违反后果：**
- 数据库等随机访问场景用 HDD 导致性能灾难——1 次查询需要 10+ ms 寻道
- 写入密集型应用（日志、缓存淘汰）忽略 TBW 导致固态衰减到只读

### NUMA 架构

多路服务器中，每个 CPU 核心有本地 DRAM，访问远程 DRAM 需要通过 CPU 互连（如 QPI/UPI）。

**NUMA 距离感知编程：**
- 线程应优先在本地节点分配内存
- 关键数据结构的内存布局应匹配访问模式
- libnuma / hwloc 工具可用于查询 NUMA 拓扑

**约束条件：**
- 跨节点内存访问延迟比本地访问高 1.5-2 倍
- 跨节点 cache line 传输需要更复杂的一致性协议
- 内存分配策略（first-touch vs numa_balancing）影响性能

**违反后果：**
- 不感知 NUMA 的程序在 4 路服务器上可能有 2-3 倍性能差距
- 关键路径跨节点访问成为瓶颈

## 参考存根

```bash
# Linux 查看 CPU 信息
cat /proc/cpuinfo | grep "model name" | head -1
uname -m

# Linux 查看内存层次
cat /proc/meminfo | head -5
lscpu | grep -E "L1d|L2|L3"

# 查看 NUMA 拓扑
numactl --hardware
lstopo

# 测量内存延迟（lmbench）
# lat_mem_rd -N 1 -M 64 1024
```

```powershell
# Windows 查看 CPU
Get-CimInstance Win32_Processor | Select-Object Name, MaxClockSpeed

# Windows 查看缓存
Get-CimInstance Win32_CacheMemory | Select-Object Level, Size

# Windows 查看 NUMA
Get-CimInstance Win32_ComputerSystem | Select-Object NumberOfProcessors, TotalPhysicalMemory
```
