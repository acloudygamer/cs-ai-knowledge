# 内核与系统调用

## 概念

**内核**是操作系统的核心，运行在特权模式，直接管理硬件。**系统调用**是用户程序请求内核服务的接口。

```
用户程序
    ↓ (系统调用)
内核空间 ←→ 硬件
```

## 关系

**关键连接**：
- 系统调用 → **内核**：用户程序通过系统调用进入内核
- 内核 → **硬件**：内核直接控制硬件资源
- 库函数 → **系统调用**：glibc等库封装系统调用为易用接口
- 容器 → **内核**：容器依赖Linux内核的namespace/cgroup隔离

## 内核职责

| 职责 | 说明 | 相关机制 |
|------|------|----------|
| 进程调度 | 分配CPU时间片 | CFS调度器 |
| 内存管理 | 虚拟内存、页面置换 | slab分配器 |
| 文件系统 | 文件的读写和管理 | VFS、ext4 |
| 设备驱动 | 与硬件通信 | 字符/块设备驱动 |
| 网络堆栈 | TCP/IP协议实现 | netfilter |

## Linux 内核结构

```
┌─────────────────────────────────────┐
│           用户空间                   │
│  glibc / 应用进程                   │
└──────────────┬──────────────────────┘
               │ 系统调用接口 (syscall)
┌──────────────▼──────────────────────┐
│              内核空间                 │
│  ┌──────────────────────────────┐   │
│  │  系统调用层 (sys_call_table)  │   │
│  └──────────────┬───────────────┘   │
│  ┌──────────────▼───────────────┐   │
│  │     内核核心 (进程调度等)     │   │
│  └──────────────┬───────────────┘   │
│  ┌──────────────▼───────────────┐   │
│  │   驱动程序 (字符/块/网络)    │   │
│  └──────────────┬───────────────┘   │
│  ┌──────────────▼───────────────┐   │
│  │     硬件 (CPU/内存/磁盘)     │   │
│  └──────────────────────────────────┘│
└─────────────────────────────────────┘
```

## 系统调用流程

```c
// 应用程序调用系统调用示意
#include <unistd.h>

// glibc封装的write函数
ssize_t write(int fd, const void* buf, size_t count) {
    // 内部触发软中断或syscall指令
    return syscall(SYS_write, fd, buf, count);
}
```

**触发系统调用的方式**：
- x86: `int 0x80` (旧) 或 `syscall` 指令 (新)
- ARM: `svc 0` 指令

## POSIX 标准

POSIX定义了Unix系统的API标准，保证程序可移植性：

```c
// POSIX标准定义的系统调用
open(), close(), read(), write()
fork(), exec(), wait()
pthread_create(), pthread_mutex_lock()
mmap(), munmap()
socket(), connect(), accept()
```

```bash
# Linux 系统调用跟踪
strace -e trace=write ls /tmp

# 输出示例
write(1, "file1\nfile2\n", 13) = 13
```

## 常用系统调用

### 进程相关
```c
#include <unistd.h>
#include <sys/wait.h>

pid_t pid = fork();          // 创建子进程
if (pid == 0) {
    execlp("ls", "ls", NULL);  // 执行新程序
} else {
    wait(NULL);             // 等待子进程
}
```

### 文件相关
```c
#include <fcntl.h>
#include <unistd.h>

int fd = open("file.txt", O_RDONLY);  // 打开文件
char buf[100];
read(fd, buf, 100);                    // 读取
close(fd);                             // 关闭
```

### 内存相关
```c
#include <sys/mman.h>

void* addr = mmap(NULL, 4096,
    PROT_READ | PROT_WRITE,
    MAP_PRIVATE | MAP_ANONYMOUS,
    -1, 0);           // 映射内存

munmap(addr, 4096);   // 解除映射
```

## 内核态 vs 用户态

| 特性 | 内核态 | 用户态 |
|------|--------|--------|
| 特权级别 | Ring 0 (最高) | Ring 3 |
| 访问硬件 | 直接 | 受限 |
| 内存空间 | 全部 | 仅自身进程 |
| 代码崩溃 | 系统崩溃 | 可恢复 |

## 容器与内核

容器（如Docker）依赖Linux内核的隔离机制：

```bash
# Linux命名空间隔离
unshare --mount --pid --fork bash

# cgroup资源限制
cgcreate -g cpu:/limited
cgclassify -g cpu:/limited $$  # 把当前进程加入cgroup
```

**容器不是虚拟机**：
- 虚拟机：完整OS内核，完全隔离
- 容器：共享宿主机内核，轻量但隔离弱

## 面试要点

1. **系统调用流程**：用户态→内核态切换的触发方式
2. **POSIX标准**：为什么重要，常见POSIX接口
3. **内核态特权**：与用户态的区别，为什么需要
4. **Linux系统调用**：常用系统调用分类
5. **容器隔离原理**：namespace和cgroup的作用
