# 缓存与认证

## 定义

缓存的本质是**时间局部性原理的应用**：最近访问的数据很可能再次被访问，通过存储计算结果或复制数据副本，减少重复计算或远程请求。

认证的本质是**可信身份声明的验证**：客户端证明持有密钥（密码/TOTP/私钥），或持有令牌（JWT/cookie），服务端验证后建立会话。

## 数学模型

**缓存命中率**：
$$
hit\_rate = \frac{hits}{hits + misses} = \frac{hits}{total\_requests}
$$

**缓存淘汰策略的数学模型**：

- **LRU（Least Recently Used）**：基于访问时间的局部性，淘汰最久未使用的项。

  设访问时间戳为 $t_{last}(item)$，淘汰目标：$\arg\max_{item} (now - t_{last}(item))$

- **LFU（Least Frequently Used）**：基于访问频率的局部性，淘汰访问次数最少的项。

  设访问计数为 $count(item)$，淘汰目标：$\arg\min_{item} count(item)$

- **Random**：随机淘汰，无额外计算开销，适用于极端场景。

**bcrypt 密钥拉伸成本**：bcrypt 的计算复杂度为 $O(cost \times 2^{cost})$。设 cost factor $= r$，则每次哈希计算耗时约 $0.1 \times 2^r$ 秒（近似值）。cost=12 时单次计算约 250ms，暴力破解成本显著提高。

| cost | 耗时（近似） | 安全性 |
|------|-------------|--------|
| 10 | ~60ms | 最低容忍 |
| 12 | ~250ms | 推荐（Node.js 24+ 默认） |
| 14 | ~1s | 高安全场景 |

**JWT 签名验证**：JWT 的 Signature 部分使用 HMAC-SHA256（或 RSA）计算，公式为：
$$
\text{Signature} = \text{HMAC-SHA256}(base64url(Header) + "." + base64url(Payload), secret)
$$

验证时重新计算签名并与提供的 Signature 比较，常量时间比较防止时序攻击。

**JWT 的三个部分**：
$$
JWT = base64url(Header) + "." + base64url(Payload) + "." + Signature
$$

Header 和 Payload 是 Base64URL 编码的 JSON，签名是 HMAC 摘要。

## 数据流

<pre>
缓存请求流程：
请求 ──▶ 缓存层 ──▶ 未命中 ──▶ 数据源
              │                      │
              └── 命中 ───────────────┘

认证流程：
注册 ──▶ 盐 + 哈希存储 ──▶ 登录验证
                        │
                     Token/JWT
                        │
                   请求携带 ──▶ 中间件验证 ──▶ 业务逻辑
</pre>

**缓存数据形态变换**：
1. 数据源（原始对象）
2. 序列化（JSON/二进制）
3. 缓存存储（内存/Redis/磁盘）
4. 反序列化（读取时）
5. 返回原始对象

## 机制

**缓存的物理约束**：
- 缓存空间有限（内存或磁盘），无法无限存储
- 当缓存满时，需要淘汰策略（LRU/LFU/Random）
- 缓存与数据源的一致性：更新数据时需考虑是否更新/失效缓存

**缓存一致性的数学约束**：

设数据源版本为 $V_{source}$，缓存版本为 $V_{cache}$。一致性要求：

$$
V_{cache} = V_{source} \quad \text{或} \quad V_{cache} \leftarrow \text{失效}
$$

违反此约束会导致**脏读**：读取到过期数据。

**HTTP 强缓存 vs 协商缓存**：
- 强缓存（`Cache-Control: max-age=3600`）：浏览器直接使用本地缓存，不发任何请求到服务器。适用于不经常变化的静态资源。
- 协商缓存（`ETag/Last-Modified`）：每次请求仍到服务器验证，若未变化返回 304 Not Modified。适用于可能变化的资源。

**ETag 的数学定义**：
$$
ETag = hash(content) \quad \text{或} \quad ETag = version
$$

**违反约束的后果**：
- 强缓存资源更新后，浏览器因缓存未过期而不获取新版本 → 解决方法：版本化 URL（文件名含 hash）或设置 `no-cache`
- 缓存未设置过期时间且数据源已更新 → 缓存与数据源不一致

**JWT 的设计约束**：
- JWT 内容（除签名外）是 Base64 编码的明文，任何人都可解码——不能存储敏感信息
- JWT 签名验证后才能信任 payload
- JWT 一旦签发在过期前无法撤销（无状态），解决方法是加入黑名单机制或缩短过期时间

**bcrypt 的 salt 机制**：
- 每次哈希使用随机 salt，相同密码每次哈希结果不同
- salt 存储在哈希字符串中（`$2b$12$...`），验证时取出使用
- 这防止了彩虹表攻击：攻击者无法预计算所有常见密码的哈希表

**bcrypt 哈希格式**：
$$
\$2b\$\$12\$\$22chars\_salt + 31chars\_hash = 60chars
$$
- `$2b$`：版本标识
- `$12$`：cost factor
- `22chars`：Base64 编码的 salt（128 位）
- `31chars`：Base64 编码的 hash（184 位）

## HTTP 缓存

HTTP 缓存通过 Header 协商控制资源生命周期：**强缓存**（Cache-Control/Expires）直接使用本地副本，无需服务器确认；**协商缓存**（ETag/Last-Modified）每次仍需服务器验证。

```javascript
res.set('Cache-Control', 'public, max-age=3600, must-revalidate');
res.set('ETag', 'v1.0.0');
```

**Cache-Control 指令的数学语义**：
- `max-age=N`：缓存有效期为 N 秒
- `no-cache`：每次使用前必须验证
- `no-store`：禁止缓存
- `private`：只能被浏览器缓存，不能被 CDN 缓存

---

## JWT 结构

JWT 的本质是**自包含的签名字符串**：Header.Payload.Signature 三部分，签名保证内容未被篡改，令牌本身可被任何人解析，但只有服务端能验证。

**JWT 的生命周期**：
```
签发 (sign) ──▶ 传输 ──▶ 验证 ──▶ 业务逻辑
                    │
                 过期检查
                    │
                 权限验证
```

Node.js 24+ 推荐使用 `jose` 库替代 `jsonwebtoken`，因为 jose 支持 WebCrypto API，实现更好的跨平台一致性。

```javascript
const jwt = require('jsonwebtoken');
jwt.sign({ sub: userId }, secret, { expiresIn: '15m' });
jwt.verify(token, secret);
```

---

## 密码存储

密码存储的本质是**密钥拉伸**：bcrypt 通过 salt 防止彩虹表攻击，通过 cost factor 提高暴力破解成本。Node.js 24+ 推荐 cost factor 12。

```javascript
const bcrypt = require('bcrypt');
bcrypt.hash(password, 12);
bcrypt.compare(password, hash);
```

**密码哈希的数学约束**：
- 哈希函数 $H$ 必须是单向的：给定 $H(p)$ 难以推断 $p$
- salt $s$ 随机生成：$H_s(p) \neq H_{s'}(p)$ 即使 $p = p'$
- cost factor $c$ 增加计算难度：$T(c) \approx O(2^c)$

---

## Session vs Token

Session 的本质是**服务端状态存储**：会话 ID 存在 cookie 中，会话数据存在内存/Redis；Token 的本质是**无状态声明**：所有认证信息编码在令牌本身，服务端无需存储状态。

**Session vs Token 的权衡**：

| 维度 | Session | Token |
|------|---------|-------|
| 存储 | 服务端（内存/Redis） | 客户端 |
| 验证速度 | 需查表 | 仅签名验证 |
| 撤销 | 立即（删除会话） | 困难（需黑名单） |
| 扩展性 | 多服务器需共享存储 | 无状态，易扩展 |

Session 更适合需要立即撤销权限的场景；Token 适合跨服务认证和移动端场景。

---

## 参考存根

*展示 JWT 和 bcrypt 的最简可执行证明：*

```javascript
// 依赖：npm install jsonwebtoken bcrypt
const jwt = require('jsonwebtoken');
const bcrypt = require('bcrypt');

const secret = 'test-secret';
const payload = { sub: 'user-123', role: 'admin' };

// JWT 签名与验证
const token = jwt.sign(payload, secret, { expiresIn: '1h' });
console.log('token:', token.length, 'chars');
try {
  const decoded = jwt.verify(token, secret);
  console.log('verified:', decoded);
} catch (e) { console.log('invalid:', e.message); }

// bcrypt 哈希与验证
async function demo() {
  const hash = await bcrypt.hash('password123', 10);
  console.log('hash:', hash);
  const match = await bcrypt.compare('password123', hash);
  console.log('match:', match);
}
demo();
```
