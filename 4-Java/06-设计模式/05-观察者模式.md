# 观察者模式

## 定义

观察者模式是一种定义对象间一对多依赖关系，当一个对象（主题）状态改变时，所有依赖它的对象（观察者）都会自动收到通知的响应型设计模式。

## 数学模型

### 订阅关系的集合论建模

设主题集合 $S$，观察者集合 $O$。订阅关系是笛卡尔积的子集：
$$\text{Subscribe} \subseteq S \times O$$

主题 $s$ 广播通知时：
$$\forall o \in O: (s, o) \in \text{Subscribe} \Rightarrow o.\text{notify}()$$

订阅/取消订阅操作：
$$\text{subscribe}(s, o) \Rightarrow \text{Subscribe} \cup \{(s, o)\}$$
$$\text{unsubscribe}(s, o) \Rightarrow \text{Subscribe} \setminus \{(s, o)\}$$

### 通知顺序的不确定性

在并发环境下，观察者集合的迭代顺序不确定：
$$\text{order}(\text{observers}) \neq \text{deterministic}$$

不应依赖观察者的通知顺序——这是 **时序耦合** 的陷阱。

### 发布-订阅的拓扑结构

观察者模式与发布-订阅模式的根本区别在于 **拓扑结构**：

| 维度 | 观察者模式 | 发布-订阅 |
|------|-----------|----------|
| 拓扑 | 星型（主题-观察者直接通信） | 中间件介导的网状拓扑 |
| 耦合 | 同步，紧耦合 | 异步，解耦 |
| 消息路径 | $O(n)$ 直接通知 | $O(n)$ 经由 Broker |
| 位置关系 | 同进程 | 可跨进程/机器 |

发布-订阅是观察者模式的 **分布式变体**，通过引入消息中间件（Kafka、RabbitMQ）解耦发布者和订阅者。

## 数据流

<pre>
观察者模式的发布-订阅流程
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Subject 状态变化
    │
    ├─▶ for each observer in observers
    │        │
    │        └─▶ observer.update(event)
    │                  │
    │                  ├─▶ EmailObserver
    │                  ├─▶ SmsObserver
    │                  └─▶ LogObserver
    │
    └─ 全部通知完成（顺序不确定）

线程安全的观察者注册（CopyOnWriteArrayList）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

注册/注销操作：
    使用 CopyOnWriteArrayList
    → 写时复制，读不加锁
    → 读操作无锁，性能高
    → 写操作：复制整个数组，替换引用
    → 适用于读多写少场景

通知过程（迭代快照）：
    for (Observer o : observers)  // 迭代的是快照
        o.update()

    ⚠️ 若迭代期间有注销，
       可能通知已被移除的观察者
       （快照一致性）
</pre>

## 机制

### 推模式 vs 拉模式

**推模式（Push）**：主题主动推送变化数据
```java
void update(Data data) { // 接收推送的数据
    this.data = data;
}
```

**拉模式（Pull）**：主题只通知变化，观察者按需获取
```java
void update() {
    Data data = subject.getData(); // 主动拉取
}
```

| 维度 | 推模式 | 拉模式 |
|------|--------|--------|
| 效率 | 高（推送具体数据） | 低（可能多次查询） |
| 耦合 | 紧（观察者需知道数据结构） | 松（只依赖主题接口） |
| 适用 | 数据量小、变化频繁 | 数据量大、变化少 |

### 发布-订阅与观察者模式的区别

| 维度 | 观察者模式 | 发布-订阅 |
|------|-----------|----------|
| 通信方式 | 直接通信（同步） | 通过中间件（可异步） |
| 拓扑 | 星型（主题-观察者） | 网状（发布者-订阅者-中间件） |
| 部署 | 同进程 | 可跨进程/机器 |

发布-订阅是观察者模式的 **分布式变体**，中间件（如 Kafka）解耦了发布者和订阅者。

### Java 的 Observable 废弃原因

`java.util.Observable` 存在设计缺陷：
1. **非线程安全**：观察者列表的修改在并发下不安全
2. **单继承限制**：`Observable` 是类，Java 单继承导致使用时必须继承它
3. **方法 protected**：只有子类能调用 `setChanged()`，无法在外部控制

**推荐替代**：`PropertyChangeSupport` 或自己实现观察者接口。

### 观察者的责任与取消订阅

观察者的 **单一职责**：观察者只负责响应通知，不应承担额外业务逻辑。

**取消订阅的重要性**：
$$\text{若 } o \in \text{Subscribe} \text{ 但不再需要通知}$$
$$\text{则 } o.\text{notify}() \text{ 浪费计算资源}$$

长期不取消订阅会导致 **内存泄漏**（主题持有观察者引用，主题不死则观察者无法 GC）。

## 参考存根

```java
// 展示观察者模式的完整实现
import java.util.*;
import java.util.concurrent.CopyOnWriteArrayList;

interface Observer {
    void update(String message);
}

class Subject {
    private final List<Observer> observers = new CopyOnWriteArrayList<>();

    public void subscribe(Observer o) {
        observers.add(o);
    }

    public void unsubscribe(Observer o) {
        observers.remove(o);
    }

    public void notifyObservers(String message) {
        for (Observer o : observers) {
            o.update(message);
        }
    }
}

class NewsAgency implements Observer {
    private final String name;

    NewsAgency(String name) { this.name = name; }

    public void update(String message) {
        System.out.println(name + " received: " + message);
    }
}

// 使用
Subject subject = new Subject();
Observer alice = new NewsAgency("Alice");
Observer bob = new NewsAgency("Bob");

subject.subscribe(alice);
subject.subscribe(bob);
subject.notifyObservers("Breaking: Java 21 released!");
// 输出顺序不确定

// PropertyChangeSupport 实现（推荐替代 Observable）
public class ObservableBean {
    private final PropertyChangeSupport support = new PropertyChangeSupport(this);
    private String value;

    public void addPropertyChangeListener(PropertyChangeListener l) {
        support.addPropertyChangeListener(l);
    }

    public void removePropertyChangeListener(PropertyChangeListener l) {
        support.removePropertyChangeListener(l);
    }

    public void setValue(String newValue) {
        String old = this.value;
        this.value = newValue;
        support.firePropertyChange("value", old, newValue);
    }
}
```
