# 装饰器模式

## 定义

装饰器模式是一种动态地给对象添加额外职责，通过在对象外包裹装饰器类来增强对象行为，比继承更灵活的 structural 设计模式。其本质是**组合优于继承**——通过对象组合将功能模块化，而非通过类层次继承。

## 数学模型

### 类爆炸的组合数学

设可选功能集合 $F = \{f_1, f_2, \ldots, f_n\}$ ，每种功能可以开启或关闭： ，每种功能可以开启或关闭：

 $\text{组合数} = 2^n$ 

若用继承实现，每种组合需要一个类：
 $|\text{ClassFamily}| = 2^n$ 

当 $n=5$ 时，需要 32 个类； $n=10$ 时，需要 1024 个类。 时，需要 32 个类； $n=10$ 时，需要 1024 个类。 时，需要 1024 个类。

装饰器模式将组合从**编译时**移到**运行时**，只需 $n+1$ 个类（1 个基础类 + $n$ 个装饰器）。 个类（1 个基础类 + $n$ 个装饰器）。 个装饰器）。

### 委托链的函数式建模

装饰器的委托链可建模为**函数组合**：

设基础对象 $B$ 的方法 $m$ 的行为为 $f_B$ ： 的方法 $m$ 的行为为 $f_B$ ： 的行为为 $f_B$ ： ：
 $f_B: X \rightarrow Y$ 

装饰器 $D_1, D_2, \ldots, D_n$ 的行为分别是对 $f_B$ 的包装： 的行为分别是对 $f_B$ 的包装： 的包装：
 $f_{D_i}(x) = g_i(f_{\text{next}}(x))$ 

其中 $f_{\text{next}}$ 是链中下一个对象的函数。 是链中下一个对象的函数。

最终行为：
 $f_{\text{total}} = f_{D_1} \circ f_{D_2} \circ \cdots \circ f_{D_n} \circ f_B$ 

**结合律成立**：
 $f_{D_1} \circ (f_{D_2} \circ f_{D_3}) = (f_{D_1} \circ f_{D_2}) \circ f_{D_3}$ 

装饰器链满足结合律，但**交换律不成立**（顺序影响最终结果）。

## 数据流

<pre>
装饰器的委托链
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────┐
│ 委托调用路径                                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Client ──→ WhipDecorator.operation()                      │
│                    │ delegate                               │
│                    ▼                                        │
│              SugarDecorator.operation()                     │
│                    │ delegate                               │
│                    ▼                                        │
│              MilkDecorator.operation()                      │
│                    │ delegate                               │
│                    ▼                                        │
│              SimpleCoffee.operation()                       │
│                    │                                        │
│                    ▼                                        │
│              返回结果，沿委托链逐层返回                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ getCost() 的累加计算                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  WhipDecorator.getCost()                                    │
│       │                                                     │
│       ├── 2.8 ← SugarDecorator.getCost()                   │
│       │        │                                            │
│       │        ├── 2.5 ← MilkDecorator.getCost()           │
│       │        │        │                                   │
│       │        │        ├── 2.0 ← SimpleCoffee.getCost()  │
│       │        │        │                                   │
│       │        │        └── 返回 2.0                        │
│       │        │                                            │
│       │        └── 返回 2.0 + 0.5 = 2.5                     │
│       │                                                     │
│       └── 返回 2.5 + 0.3 = 2.8                             │
│                                                             │
│  WhipDecorator                                              │
│       └── 返回 2.8 + 0.2 = 3.0                             │
└─────────────────────────────────────────────────────────────┘
</pre>

**数据所有权**：
- 每个装饰器持有被装饰对象的引用，但不持有其所有权
- 所有权通过调用链传递，最终由基础对象持有
- 装饰器在委托前后可添加额外行为（前置/后置处理）

## 机制

### 装饰器的委托机制

装饰器持有被装饰对象的引用（组合而非继承）：

```java
public class Decorator implements Component {
    protected Component wrapped;  // 持有被装饰对象

    public Decorator(Component wrapped) {
        this.wrapped = wrapped;
    }

    public String operation() {
        return wrapped.operation();  // 委托给被装饰对象
    }
}

public class LoggingDecorator extends Decorator {
    public LoggingDecorator(Component wrapped) {
        super(wrapped);
    }

    public String operation() {
        System.out.println("Before operation");
        String result = super.operation();  // 调用被装饰对象
        System.out.println("After operation");
        return result;
    }
}
```

**委托链的数学结构**：形成**函数组合**：
 $R = D_1 \circ D_2 \circ \cdots \circ D_n \circ B$ 

### 装饰器 vs 代理

| 维度 | 装饰器 | 代理 |
|------|--------|------|
| **意图** | 动态增强功能 | 控制对对象的访问 |
| **接口关系** | 实现相同接口 | 实现相同接口 |
| **组合方式** | 可无限嵌套 | 通常单一代理 |
| **增强时机** | 运行时 | 编译时或运行时 |
| **典型场景** | I/O 流、咖啡订单 | 远程代理、虚代理 |

**关键区别**：
- 装饰器：**增加**功能（组合）
- 代理：**控制**访问（不一定要增强）

### Java I/O 库的装饰器模式

Java I/O 类库是装饰器模式的经典应用：

```
BufferedInputStream
    └─ FilterInputStream（装饰器基类）
            └─ InputStream（被装饰对象）

FileInputStream (被装饰对象)
DataInputStream (装饰器)
BufferedInputStream (装饰器，可嵌套)
GZIPInputStream (装饰器，可嵌套)
```

这允许任意组合：

```java
// 缓冲 + 压缩 + 文件
InputStream in = new BufferedInputStream(
    new GZIPInputStream(
        new FileInputStream("data.gz")
    )
);
```

**设计意图**：Java I/O 设计于 1997 年（Java 1.1），装饰器模式解决了继承层次膨胀问题，使 I/O 流可以灵活组合。

### 装饰器的执行顺序敏感性

装饰器的执行顺序可能影响最终结果：

```java
// 顺序 1：先加奶，再加糖
Coffee c1 = new SugarDecorator(new MilkDecorator(base)); // 2.0+0.5+0.3

// 顺序 2：先加糖，再加奶
Coffee c2 = new MilkDecorator(new SugarDecorator(base)); // 2.0+0.3+0.5
```

对于**可交换的装饰器**（如加奶、加糖）：
 $cost(D_1 \circ D_2) = cost(D_2 \circ D_1)$ 

但对于**有副作用的装饰器**（如日志装饰器、计时装饰器），顺序会影响：
- 日志输出顺序
- 计时累计方式

## 参考存根

```java
// 咖啡订单的装饰器实现（Java 21）
interface Coffee {
    String getDescription();
    double getCost();
}

class SimpleCoffee implements Coffee {
    public String getDescription() { return "Coffee"; }
    public double getCost() { return 2.0; }
}

abstract class CoffeeDecorator implements Coffee {
    protected Coffee decorated;
    public CoffeeDecorator(Coffee coffee) { this.decorated = coffee; }
}

class MilkDecorator extends CoffeeDecorator {
    public MilkDecorator(Coffee coffee) { super(coffee); }
    public String getDescription() {
        return decorated.getDescription() + ", Milk";
    }
    public double getCost() {
        return decorated.getCost() + 0.5;
    }
}

class SugarDecorator extends CoffeeDecorator {
    public SugarDecorator(Coffee coffee) { super(coffee); }
    public String getDescription() {
        return decorated.getDescription() + ", Sugar";
    }
    public double getCost() {
        return decorated.getCost() + 0.2;
    }
}

class WhipDecorator extends CoffeeDecorator {
    public WhipDecorator(Coffee coffee) { super(coffee); }
    public String getDescription() {
        return decorated.getDescription() + ", Whip";
    }
    public double getCost() {
        return decorated.getCost() + 0.3;
    }
}

// 使用
Coffee coffee = new WhipDecorator(
    new SugarDecorator(
        new MilkDecorator(
            new SimpleCoffee()
        )
    )
);
// 描述：Coffee, Milk, Sugar, Whip
// 费用：2.0 + 0.5 + 0.2 + 0.3 = 3.0

// 运行时条件组合
class ConditionalDecorator {
    static Coffee decorate(Coffee base, Set<String> extras) {
        Coffee result = base;
        if (extras.contains("milk")) result = new MilkDecorator(result);
        if (extras.contains("sugar")) result = new SugarDecorator(result);
        if (extras.contains("whip")) result = new WhipDecorator(result);
        return result;
    }
}

// Java I/O 装饰器示例
class FileProcessor {
    static String readCompressedFile(String path) throws IOException {
        try (BufferedReader reader = new BufferedReader(
                new InputStreamReader(
                    new GZIPInputStream(
                        new FileInputStream(path)
                    )
                )
        )) {
            return reader.lines().collect(Collectors.joining("\n"));
        }
    }
}
```
