# 参数化测试

## JUnit 5 参数化测试

### 启用参数化测试

需要添加 JUnit 5 Jupiter 参数化支持：

```xml
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter-params</artifactId>
    <version>5.10.0</version>
    <scope>test</scope>
</dependency>
```

## @ValueSource

### 基础用法

```java
@ParameterizedTest
@ValueSource(ints = {1, 2, 3, 4, 5})
void testIsPositive(int number) {
    assertTrue(number > 0);
}

@ParameterizedTest
@ValueSource(strings = {"hello", "world", "test"})
void testIsNotBlank(String input) {
    assertNotNull(input);
    assertFalse(input.isBlank());
}

@ParameterizedTest
@ValueSource(doubles = {0.1, 0.5, 0.9})
void testPercentage(double value) {
    assertTrue(value >= 0 && value <= 1);
}
```

### 支持的类型

```java
// 整数数组
@ValueSource(ints = {1, 2, 3})

// 长整数数组
@ValueSource(longs = {1L, 2L, 3L})

// 字符串数组
@ValueSource(strings = {"a", "b", "c"})

// double 数组
@ValueSource(doubles = {1.0, 2.0, 3.0})
```

## @NullSource 和 @EmptySource

```java
@ParameterizedTest
@NullSource
void testNull(String input) {
    assertNull(input);
}

@ParameterizedTest
@EmptySource
void testEmpty(String input) {
    assertTrue(input.isEmpty());
}

// 组合
@ParameterizedTest
@NullAndEmptySource
void testNullOrEmpty(String input) {
    assertTrue(input == null || input.isEmpty());
}
```

## @CsvSource

### 逗号分隔值

```java
@ParameterizedTest
@CsvSource({
    "1, 2, 3",
    "0, 0, 0",
    "-1, -2, -3"
})
void testAdd(int a, int b, int expected) {
    assertEquals(expected, calculator.add(a, b));
}
```

### 自定义分隔符

```java
@ParameterizedTest
@CsvSource(value = {
    "Alice: 30: alice@example.com",
    "Bob: 25: bob@example.com"
}, delimiter = ':')
void testUser(String name, int age, String email) {
    User user = new User(name, age, email);
    assertNotNull(user.getName());
}
```

### 空值处理

```java
@ParameterizedTest
@CsvSource(value = {
    "'Alice', 30",
    "'', 0",          // 空字符串
    "Bob, null"       // null 值
})
void testUser(String name, Integer age) {
    // 处理空字符串和 null
}
```

## @CsvFileSource

### 外部 CSV 文件

```java
@ParameterizedTest
@CsvFileSource(resources = "/data/calculator.csv")
void testFromCsvFile(int a, int b, int expected) {
    assertEquals(expected, calculator.add(a, b));
}
```

### CSV 文件格式

```csv
a,b,expected
1,2,3
0,0,0
-1,-2,-3
```

### resources 路径

```
src/
└── test/
    └── resources/
        └── data/
            ├── calculator.csv
            └── user-test.csv
```

## @MethodSource

### 静态工厂方法

```java
@ParameterizedTest
@MethodSource("provideStringsForIsBlank")
void testIsBlank(String input, boolean expected) {
    assertEquals(expected, input.isBlank());
}

static Stream<Arguments> provideStringsForIsBlank() {
    return Stream.of(
        Arguments.of("   ", true),
        Arguments.of("hello", false),
        Arguments.of("", true),
        Arguments.of("  hello  ", false)
    );
}
```

### 基本类型方法

```java
@ParameterizedTest
@MethodSource("intRange")
void testIntRange(int number) {
    assertTrue(number >= 0 && number < 100);
}

static IntStream intRange() {
    return IntStream.range(0, 100);
}
}

// 多参数
@ParameterizedTest
@MethodSource("provideUserData")
void testUser(String name, int age, String email) {
    User user = new User(name, age, email);
    assertNotNull(user);
}

static Stream<Arguments> provideUserData() {
    return Stream.of(
        Arguments.of("Alice", 30, "alice@example.com"),
        Arguments.of("Bob", 25, "bob@example.com")
    );
}
```

### 外部类方法

```java
class TestDataProvider {

    static Stream<Arguments> userData() {
        return Stream.of(
            Arguments.of("Alice", 30),
            Arguments.of("Bob", 25)
        );
    }
}

class UserTest {
    @ParameterizedTest
    @MethodSource("com.example.TestDataProvider#userData")
    void testUser(String name, int age) {
        // ...
    }
}
```

## @ArgumentsSource

### 自定义 ArgumentsProvider

```java
public class CustomArgumentsProvider implements ArgumentsProvider {
    @Override
    public Stream<? extends Arguments> provideArguments(ExtensionContext context) {
        return Stream.of(
            Arguments.of("hello", 5),
            Arguments.of("world", 5)
        );
    }
}

@ParameterizedTest
@ArgumentsSource(CustomArgumentsProvider.class)
void test(String input, int expectedLength) {
    assertEquals(expectedLength, input.length());
}
```

## 多个参数源

```java
@ParameterizedTest
@NullSource
@ValueSource(strings = {"hello", "world"})
void testMultiple(String input) {
    // 运行 3 次：null, "hello", "world"
}
```

## 参数转换

### 自动转换

```java
@ParameterizedTest
@ValueSource(strings = {"2024-01-01", "2024-12-31"})
void testDate(LocalDate date) {
    assertNotNull(date);
}

@ParameterizedTest
@ValueSource(strings = {"alice", "bob"})
void testEnum(String role) {
    UserRole userRole = UserRole.valueOf(role.toUpperCase());
    assertNotNull(userRole);
}
```

### 自定义 Converter

```java
public class UserConverter extends SimpleArgumentConverter {

    @Override
    public Object convert(Object input, Type targetType) {
        if (input instanceof String && targetType == User.class) {
            String[] parts = ((String) input).split(":");
            return new User(parts[0], Integer.parseInt(parts[1]));
        }
        throw new IllegalArgumentException();
    }
}

@ParameterizedTest
@CsvSource({
    "Alice: 30",
    "Bob: 25"
})
void testUser(
    @ConvertWith(UserConverter.class) User user
) {
    assertNotNull(user.getName());
}
```

## DisplayName 自定义

```java
@ParameterizedTest
@ValueSource(ints = {1, 2, 3})
@DisplayName("测试计算器 add 方法")
void testAdd(int number) {
    assertTrue(calculator.add(number, 0) >= 0);
}

// 每个参数显示
@ParameterizedTest(name = "[{index}] {0} + 0 = {0}")
@ValueSource(ints = {1, 2, 3})
void testAddWithName(int number) {
    assertEquals(number, calculator.add(number, 0));
}
```

## 常用场景

### 边界值测试

```java
@ParameterizedTest
@ValueSource(ints = {Integer.MIN_VALUE, -1, 0, 1, Integer.MAX_VALUE})
void testBoundary(int value) {
    // 边界值测试
}
```

### 等价类划分

```java
@ParameterizedTest
@CsvSource({
    "alice@example.com, true",   // 正常邮箱
    "bob@test.org, true",
    "invalid, false",             // 无 @
    "@test.com, false",          // 无用户名
    "test@, false"               // 无域名
})
void testEmailValidation(String email, boolean expected) {
    assertEquals(expected, validator.isValid(email));
}
```

### 日期格式测试

```java
@ParameterizedTest
@ValueSource(strings = {
    "2024-01-01",
    "2024-12-31",
    "2024-06-15"
})
void testDateParsing(String dateStr) {
    LocalDate date = LocalDate.parse(dateStr);
    assertNotNull(date);
    assertEquals(2024, date.getYear());
}
```

## 常见问题

### 空值和 null

```java
@ParameterizedTest
@NullSource
@EmptySource
@ValueSource(strings = {"hello"})
void testWithNullEmptyAndValue(String input) {
    // 处理所有情况
}
```

### 性能考虑

```java
// 参数化测试默认串行执行
// 如需并行，添加
@Execution(ExecutionMode.CONCURRENT)
@ParameterizedTest
@ValueSource(ints = {1, 2, 3, 4, 5})
void testParallel(int value) {
    // 并行执行
}
```

### 超长参数列表

```java
// 使用 @MethodSource 外部化
@ParameterizedTest
@MethodSource("com.example.DataFactory#largeDataset")
void testWithLargeData(String input1, String input2, int value) {
    // ...
}
```
