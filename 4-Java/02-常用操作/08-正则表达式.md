# 正则表达式

## 概述

正则表达式是用于匹配字符串模式的工具，Java 通过 `java.util.regex` 包提供支持。

## 基本语法

### 元字符

| 元字符 | 说明 | 示例 |
|--------|------|------|
| `.` | 任意单个字符 | `a.c` 匹配 "abc" |
| `\d` | 数字 [0-9] | `\d+` 匹配 "123" |
| `\D` | 非数字 | `\D+` 匹配 "abc" |
| `\w` | 单词字符 [a-zA-Z0-9_] | `\w+` 匹配 "hello_1" |
| `\W` | 非单词字符 | `\W` 匹配 "@" |
| `\s` | 空白字符 | `\s+` 匹配空格 |
| `\S` | 非空白字符 | `\S+` 匹配非空格 |

### 量词

| 量词 | 说明 |
|------|------|
| `*` | 0 次或多次 |
| `+` | 1 次或多次 |
| `?` | 0 次或 1 次 |
| `{n}` | 恰好 n 次 |
| `{n,}` | 至少 n 次 |
| `{n,m}` | n 到 m 次 |

### 边界匹配

`^` 字符串开始，`$` 字符串结束，`\b` 单词边界，`\B` 非单词边界。

### 分组与捕获

`(...)` 捕获组，`(?:...)` 非捕获组，`(?<name>...)` 命名捕获组，`\1` 引用第 1 个捕获组。

## Java API

### Pattern 和 Matcher

## 常用方法

- `matches()` - 整个字符串匹配
- `split()` - 按正则分割
- `replaceAll()` - 替换所有
- `replaceFirst()` - 替换第一个

## 常见场景

### 邮箱验证

### 手机号验证

### URL 提取

### HTML 标签内容提取

### 密码强度验证

## 性能优化

### 预编译

预编译正则表达式避免重复编译开销。

### 非贪婪匹配

使用 `.*?` 而非 `.*` 避免过度匹配。

### 避免回溯

明确量词范围避免灾难性回溯。

## 参考样例

```java
// Pattern 和 Matcher
String text = "Hello, my email is test@example.com";
Pattern pattern = Pattern.compile("\\w+@[a-zA-Z]+\\.[a-zA-Z]+");
Matcher matcher = pattern.matcher(text);

while (matcher.find()) {
    System.out.println("Found: " + matcher.group());
}
```

```java
// 常用方法
boolean isMatch = Pattern.matches("\\d+", "123");
String[] parts = "a,b;c d".split("[,;\\s]+");
String result = "abc123def".replaceAll("\\d+", "#");
```

```java
// 捕获组
Pattern p = Pattern.compile("(\\d{4})-(\\d{2})-(\\d{2})");
Matcher m = p.matcher("2024-04-13");
if (m.matches()) {
    System.out.println("Year: " + m.group(1));
}

// 命名捕获组
Pattern p = Pattern.compile("(?<year>\\d{4})-(?<month>\\d{2})-(?<day>\\d{2})");
```

```java
// 邮箱验证
public static boolean isValidEmail(String email) {
    String regex = "^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$";
    return Pattern.matches(regex, email);
}
```

```java
// 手机号验证
public static boolean isValidPhone(String phone) {
    String regex = "^1[3-9]\\d{9}$";
    return Pattern.matches(regex, phone);
}
```

```java
// 密码强度验证
public static boolean isStrongPassword(String password) {
    String regex = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]{8,}$";
    return Pattern.matches(regex, password);
}
```

```java
// 预编译（推荐）
private static final Pattern PATTERN = Pattern.compile("\\d{6}");
public boolean validate(String input) {
    return PATTERN.matcher(input).matches();
}
```

```java
// 非贪婪匹配
Pattern greedy = Pattern.compile("<div>.*</div>");
Pattern nonGreedy = Pattern.compile("<div>.*?</div>");
```

```java
// 常用正则模式
"^\\d+$"                    // 整数
"^\\d*\\.\\d+$"            // 正小数
"^[a-zA-Z]+$"              // 纯字母
"^\\d{4}-\\d{2}-\\d{2}$"  // 日期：YYYY-MM-DD
"^\\d{17}[\\dXx]$"         // 18位身份证
```
