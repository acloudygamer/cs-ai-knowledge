# 日期时间

> **旧版 API（Date/Calendar）**：Java 8 之前使用，存在线程安全和设计问题。`java.time` API（Java 8+）是当前标准。

## Java 8+ 日期时间 API

### 核心类

| 类 | 说明 |
|----|------|
| Instant | 时间戳（UTC） |
| LocalDate | 日期（年-月-日） |
| LocalTime | 时间（时:分:秒） |
| LocalDateTime | 日期时间 |
| ZonedDateTime | 带时区的日期时间 |
| Duration | 时间段（秒/纳秒） |
| Period | 日期间隔（年/月/日） |

## 格式化与解析

### DateTimeFormatter

### 常用格式模式

| 模式 | 说明 | 示例 |
|------|------|------|
| yyyy | 4位年份 | 2024 |
| MM | 2位月份 | 04 |
| dd | 2位日期 | 13 |
| HH | 2位小时(24h) | 14 |
| mm | 2位分钟 | 30 |
| ss | 2位秒 | 45 |
| EEEE | 星期几 | 星期六 |
| Z | 时区偏移 | +0800 |

## 时区处理

## 与旧版 API 互转

## 线程安全

`DateTimeFormatter` 是线程安全的，可安全共享。

## 参考样例

```java
// LocalDate
LocalDate today = LocalDate.now();
LocalDate date = LocalDate.of(2024, 4, 13);
LocalDate date2 = LocalDate.parse("2024-04-13");
int year = date.getYear();
LocalDate nextWeek = today.plusWeeks(1);
boolean isAfter = date1.isAfter(date2);
```

```java
// LocalTime
LocalTime now = LocalTime.now();
LocalTime time = LocalTime.of(14, 30, 0);
```

```java
// LocalDateTime
LocalDateTime now = LocalDateTime.now();
DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
String formatted = dt.format(formatter);
```

```java
// Instant
Instant instant = Instant.now();
long millis = instant.toEpochMilli();
```

```java
// Duration 和 Period
Duration duration = Duration.between(start, end);
Period period = Period.between(from, to);
```

```java
// ZonedDateTime
ZonedDateTime zonedNow = ZonedDateTime.now();
ZonedDateTime converted = instant.atZone(ZoneId.of("Europe/London"));
```

```java
// 格式化
DateTimeFormatter f1 = DateTimeFormatter.ofPattern("yyyy-MM-dd");
DateTimeFormatter f2 = DateTimeFormatter.ofPattern("yyyy年MM月dd日 HH:mm:ss");
```

```java
// 时区转换
LocalDateTime beijingTime = LocalDateTime.of(2024, 4, 13, 20, 0);
ZonedDateTime beijing = beijingTime.atZone(ZoneId.of("Asia/Shanghai"));
ZonedDateTime newYork = beijing.withZoneSameInstant(ZoneId.of("America/New_York"));
```

```java
// 计算年龄
public static int calculateAge(LocalDate birthDate) {
    return Period.between(birthDate, LocalDate.now()).getYears();
}
```

```java
// 判断工作日
public static boolean isWeekday(LocalDate date) {
    DayOfWeek dow = date.getDayOfWeek();
    return dow != DayOfWeek.SATURDAY && dow != DayOfWeek.SUNDAY;
}
```

```java
// Date -> LocalDateTime
Date date = new Date();
Instant instant = date.toInstant();
LocalDateTime ldt = LocalDateTime.ofInstant(instant, ZoneId.systemDefault());
```
