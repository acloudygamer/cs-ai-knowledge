# 安全编码实践

## 概述

安全编码是防止常见安全漏洞的关键实践。OWASP Top 10 概述了 Web 应用最主要的安全风险。

### OWASP Top 10（Web 安全）

```
1. 注入（SQL Injection）
2. 失效的身份认证
3. 敏感数据泄露
4. XML 外部实体（XXE）
5. 失效的访问控制
6. 安全配置错误
7. 跨站脚本（XSS）
8. 跨站请求伪造（CSRF）
9. 使用已知漏洞的组件
10. 不足的日志记录
```

## SQL 注入

### 问题本质

SQL 注入源于将用户输入直接拼接到 SQL 语句中。攻击者通过构造特殊输入，突破 SQL 语义边界，实现未授权的数据访问或操作。

### 解决方案

使用参数化查询（PreparedStatement）确保用户输入作为字面值处理，而非 SQL 语法的一部分。

```java
// 危险：直接拼接 SQL
String query = "SELECT * FROM users WHERE name = '" + username + "'";
Statement stmt = connection.createStatement();
ResultSet rs = stmt.executeQuery(query);  // 可能被注入

// 使用参数化查询
String query = "SELECT * FROM users WHERE name = ?";
PreparedStatement ps = connection.prepareStatement(query);
ps.setString(1, username);
ResultSet rs = ps.executeQuery();
```

### JPA 与 MyBatis

ORM 框架提供参数化查询支持。

```java
// JPA 参数化查询
@Query("SELECT u FROM User u WHERE u.username = :username")
Optional<User> findByUsername(@Param("username") String username);
```

```xml
<!-- MyBatis 使用参数 #{} 而非 ${} -->
<select id="findByUsername" resultMap="UserResultMap">
    SELECT * FROM users WHERE username = #{username}
</select>
```

### 动态表名/列名

动态表名列名无法参数化，必须使用白名单验证。

```java
// 使用白名单验证
private static final Set<String> ALLOWED_SORT_FIELDS =
    Set.of("id", "name", "email", "createTime");

public List<User> search(String sortField, String direction) {
    if (!ALLOWED_SORT_FIELDS.contains(sortField)) {
        throw new IllegalArgumentException("Invalid sort field");
    }
}
```

## XSS（跨站脚本攻击）

### 问题本质

XSS 攻击将恶意脚本注入网页，当其他用户访问时被执行。根本原因是未对用户输入进行适当编码就输出到 HTML。

### 解决方案

输出编码是防止 XSS 的核心措施。不同上下文需要不同的编码方式。

```java
// 危险：直接输出用户输入
response.getWriter().write("<div>" + userInput + "</div>");

// HTML 编码
import org.owasp.encoder.Encode;

String safeOutput = Encode.forHtml(userInput);
response.getWriter().write("<div>" + safeOutput + "</div>");
```

### 框架级防护

现代 Web 框架通常提供自动转义。

```html
<!-- Thymeleaf 默认自动转义 HTML -->
<div th:text="${userInput}"></div>

<!-- Angular/Vue 也默认自动转义 -->
<div>{{userInput}}</div>
```

## CSRF（跨站请求伪造）

### 攻击原理

攻击者诱导用户访问恶意页面，该页面自动发起对目标站点的请求，利用用户的认证状态执行非预期操作。

```
攻击者网页：
<form action="https://bank.com/transfer">
    <input name="to" value="hacker"/>
    <input name="amount" value="10000"/>
</form>
<script>document.forms[0].submit();</script>
```

### 解决方案

CSRF Token 和 SameSite Cookie 是主要防护手段。

```java
// Spring Security CSRF Token 配置
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf
                .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
                .ignoringRequestMatchers("/api/public/**")  // 公共 API 禁用
            );

        return http.build();
    }
}
```

```yaml
# SameSite Cookie 配置
server:
  servlet:
    session:
      cookie:
        same-site: strict  # 严格模式
```

## 敏感数据处理

### 密码存储

密码必须使用强哈希算法存储，绝不能明文。BCrypt 是 Java 首选，Argon2 更安全但需要额外库。

```java
// 错误：明文存储
user.setPassword(password);

// 错误：MD5/SHA-1（已破解）
user.setPassword(hash(password));

// 正确：BCrypt
@Service
public class PasswordService {

    @Autowired
    private PasswordEncoder passwordEncoder;

    public void encodePassword(User user) {
        user.setPassword(passwordEncoder.encode(user.getPassword()));
    }

    public boolean matches(String rawPassword, String encodedPassword) {
        return passwordEncoder.matches(rawPassword, encodedPassword);
    }
}

@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public PasswordEncoder passwordEncoder() {
        // BCrypt（推荐）
        return new BCryptPasswordEncoder();

        // Argon2（更安全但需要额外库）
        // return Argon2PasswordEncoder.defaultsForSpringSecurity_v5_8();

        // SCrypt
        // return SCryptPasswordEncoder.defaultsForSpringSecurity_v5_8();
    }
}
```

### 敏感数据加密

使用 AES-GCM 等带认证的加密算法，确保机密性同时防止篡改。

```java
// 使用 AES 加密敏感数据
@Service
public class EncryptionService {

    private static final String ALGORITHM = "AES/GCM/NoPadding";
    private static final int GCM_TAG_LENGTH = 128;
    private static final int GCM_IV_LENGTH = 12;

    @Value("${encryption.key}")
    private byte[] secretKey;

    public String encrypt(String plaintext) {
        byte[] iv = new byte[GCM_IV_LENGTH];
        new SecureRandom().nextBytes(iv);

        Cipher cipher = Cipher.getInstance(ALGORITHM);
        GCMParameterSpec parameterSpec = new GCMParameterSpec(GCM_TAG_LENGTH, iv);
        cipher.init(Cipher.ENCRYPT_MODE, new SecretKeySpec(secretKey, "AES"), parameterSpec);

        byte[] ciphertext = cipher.doFinal(plaintext.getBytes(StandardCharsets.UTF_8));

        byte[] encrypted = ByteBuffer.allocate(iv.length + ciphertext.length)
            .put(iv)
            .put(ciphertext)
            .array();

        return Base64.getEncoder().encodeToString(encrypted);
    }

    public String decrypt(String encrypted) {
        byte[] decoded = Base64.getDecoder().decode(encrypted);

        ByteBuffer buffer = ByteBuffer.wrap(decoded);
        byte[] iv = new byte[GCM_IV_LENGTH];
        buffer.get(iv);
        byte[] ciphertext = new byte[buffer.remaining()];
        buffer.get(ciphertext);

        Cipher cipher = Cipher.getInstance(ALGORITHM);
        GCMParameterSpec parameterSpec = new GCMParameterSpec(GCM_TAG_LENGTH, iv);
        cipher.init(Cipher.DECRYPT_MODE, new SecretKeySpec(secretKey, "AES"), parameterSpec);

        return new String(cipher.doFinal(ciphertext), StandardCharsets.UTF_8);
    }
}
```

### 日志脱敏

日志中不得出现敏感数据，必须进行脱敏处理。

```java
// 敏感信息脱敏工具
public class DataMaskingUtils {

    public static String maskEmail(String email) {
        if (email == null) return null;
        int atIndex = email.indexOf('@');
        if (atIndex <= 2) return "***" + email.substring(atIndex);
        return email.substring(0, 2) + "***" + email.substring(atIndex);
    }

    public static String maskPhone(String phone) {
        if (phone == null || phone.length() < 7) return "***";
        return phone.substring(0, 3) + "****" + phone.substring(phone.length() - 4);
    }

    public static String maskIdCard(String idCard) {
        if (idCard == null || idCard.length() < 10) return "***";
        return idCard.substring(0, 4) + "**********" + idCard.substring(idCard.length() - 4);
    }

    public static String maskBankCard(String cardNumber) {
        if (cardNumber == null || cardNumber.length() < 8) return "***";
        return cardNumber.substring(0, 4) + " **** **** " +
               cardNumber.substring(cardNumber.length() - 4);
    }
}
```

## 输入验证

### Bean Validation

在系统边界进行输入验证，使用标准化的验证注解。

```java
@Entity
public class User {

    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 20, message = "用户名长度必须在 3-20 之间")
    @Pattern(regexp = "^[a-zA-Z][a-zA-Z0-9_]*$", message = "用户名必须以字母开头")
    private String username;

    @NotBlank(message = "邮箱不能为空")
    @Email(message = "邮箱格式不正确")
    private String email;

    @NotBlank(message = "密码不能为空")
    @Size(min = 8, message = "密码长度至少 8 位")
    @Pattern(regexp = "^(?=.*[A-Za-z])(?=.*\\d)[A-Za-z\\d]{8,}$",
             message = "密码必须包含字母和数字")
    private String password;

    @Pattern(regexp = "^1[3-9]\\d{9}$", message = "手机号格式不正确")
    private String phone;
}
```

### 自定义验证器

通过组合注解和 ConstraintValidator 实现复杂验证逻辑。

```java
// 自定义注解
@Target({ElementType.FIELD, ElementType.PARAMETER})
@Retention(RetentionPolicy.RUNTIME)
@Constraint(validatedBy = StrongPasswordValidator.class)
@Documented
public @interface StrongPassword {
    String message() default "密码强度不足";
    Class<?>[] groups() default {};
    Class<? extends Payload>[] payload() default {};
}

public class StrongPasswordValidator implements
        ConstraintValidator<StrongPassword, String> {

    @Override
    public boolean isValid(String value, ConstraintValidatorContext context) {
        if (value == null) return true;

        boolean hasUpper = false, hasLower = false, hasDigit = false;

        for (char c : value.toCharArray()) {
            if (Character.isUpperCase(c)) hasUpper = true;
            else if (Character.isLowerCase(c)) hasLower = true;
            else if (Character.isDigit(c)) hasDigit = true;

            if (hasUpper && hasLower && hasDigit) break;
        }

        return hasUpper && hasLower && hasDigit && value.length() >= 8;
    }
}
```

## XXE（XML 外部实体）

### 问题本质

XXE 攻击利用未正确配置的 XML 解析器，通过外部实体引用读取本地文件或发起内部请求。

```java
// 危险：未安全配置的 XML 解析
DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance();
DocumentBuilder builder = dbf.newDocumentBuilder();
Document doc = builder.parse(new FileInputStream(xmlFile));
```

### 解决方案

禁用 DTD 和外部实体，启用安全特性。

```java
// 安全配置 XML 解析器
DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance();
dbf.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, true);
dbf.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
dbf.setFeature("http://xml.org/sax/features/external-general-entities", false);
dbf.setFeature("http://xml.org/sax/features/external-parameter-entities", false);
dbf.setFeature("http://apache.org/xml/features/nonvalidating/load-external-dtd", false);
dbf.setNamespaceAware(true);
```

## 安全配置

### Spring Security 最佳配置

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .headers(headers -> headers
                .frameOptions(frame -> frame.deny())
            )
            .csrf(csrf -> csrf
                .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
                .ignoringRequestMatchers("/api/public/**", "/api/health")
            )
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.IF_REQUIRED)
                .sessionFixation().migrateSession()
                .maximumSessions(1)
                .maxSessionsPreventsLogin(false)
            )
            .headers(headers -> headers
                .contentTypeOptions().and()
                .xssProtection().and()
                .cacheControl().and()
                .httpStrictTransportSecurity(hsts -> hsts
                    .includeSubDomains(true)
                    .maxAgeInSeconds(31536000)
                )
            )
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/public/**", "/api/health").permitAll()
                .requestMatchers("/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            )
            .exceptionHandling(ex -> ex
                .authenticationEntryPoint((request, response, authException) -> {
                    response.sendError(HttpServletResponse.SC_UNAUTHORIZED);
                })
                .accessDeniedHandler((request, response, accessDeniedException) -> {
                    response.sendError(HttpServletResponse.SC_FORBIDDEN);
                })
            );

        return http.build();
    }
}
```

### CORS 配置

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public CorsConfigurationSource corsConfigurationSource() {
        CorsConfiguration config = new CorsConfiguration();
        config.setAllowedOrigins(Arrays.asList("https://example.com"));
        config.setAllowedMethods(Arrays.asList("GET", "POST", "PUT", "DELETE"));
        config.setAllowedHeaders(Arrays.asList("*"));
        config.setAllowCredentials(true);
        config.setMaxAge(3600L);
        config.setExposedHeaders(Arrays.asList("Authorization", "X-Request-Id"));

        UrlBasedCorsConfigurationSource source =
            new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/api/**", config);
        return source;
    }
}
```

## 依赖安全

### 检查依赖漏洞

使用 OWASP Dependency-Check 定期扫描依赖中的已知漏洞。

```bash
# 使用 Maven
mvn org.owasp:dependency-check-maven:check

# 使用 Gradle
gradle dependencyCheckAnalyze
```

### 最小权限原则

只引入需要的依赖，指定明确版本，避免使用 starter-all。

```xml
<!-- 只引入需要的依赖，指定版本 -->
<dependencies>
    <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>2.16.1</version>
    </dependency>
</dependencies>
```

## 日志与监控

### 安全日志

记录安全相关事件，便于审计和溯源。

```java
@Slf4j
@Service
public class SecurityAuditService {

    public void logLoginSuccess(String username, String ip) {
        log.info("SECURITY AUDIT: Login success - user: {}, ip: {}, time: {}",
            username, ip, LocalDateTime.now());
    }

    public void logLoginFailure(String username, String ip, String reason) {
        log.warn("SECURITY AUDIT: Login failure - user: {}, ip: {}, reason: {}, time: {}",
            username, ip, reason, LocalDateTime.now());
    }

    public void logAccessDenied(String username, String resource, String action) {
        log.warn("SECURITY AUDIT: Access denied - user: {}, resource: {}, action: {}, time: {}",
            username, resource, action, LocalDateTime.now());
    }

    public void logSensitiveOperation(String username, String operation, Object details) {
        log.info("SECURITY AUDIT: Sensitive operation - user: {}, operation: {}, details: {}",
            username, operation, details);
    }
}
```

### 异常处理不泄露信息

对外只返回通用错误消息，详细信息记录到日志。

```java
// 错误：泄露内部信息
catch (SQLException e) {
    throw new RuntimeException("Database error: " + e.getMessage());
}

// 正确：通用错误消息
catch (SQLException e) {
    log.error("Database error", e);
    throw new RuntimeException("操作失败，请稍后重试");
}
```

## 参考样例

```java
// SQL 注入防护 - PreparedStatement
String query = "SELECT * FROM users WHERE name = ?";
PreparedStatement ps = connection.prepareStatement(query);
ps.setString(1, username);
ResultSet rs = ps.executeQuery();
```

```java
// JPA 参数化查询
@Query("SELECT u FROM User u WHERE u.username = :username")
Optional<User> findByUsername(@Param("username") String username);
```

```java
// XSS 防护 - HTML 编码
import org.owasp.encoder.Encode;
String safeOutput = Encode.forHtml(userInput);
```

```java
// CSRF 防护 - Spring Security 配置
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf
                .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
                .ignoringRequestMatchers("/api/public/**")
            );
        return http.build();
    }
}
```

```java
// 密码哈希 - BCrypt
@Service
public class PasswordService {
    @Autowired
    private PasswordEncoder passwordEncoder;

    public void encodePassword(User user) {
        user.setPassword(passwordEncoder.encode(user.getPassword()));
    }

    public boolean matches(String raw, String encoded) {
        return passwordEncoder.matches(raw, encoded);
    }
}
```

```java
// AES-GCM 加密
@Service
public class EncryptionService {
    private static final String ALGORITHM = "AES/GCM/NoPadding";
    private static final int GCM_TAG_LENGTH = 128;
    private static final int GCM_IV_LENGTH = 12;

    public String encrypt(String plaintext) {
        byte[] iv = new byte[GCM_IV_LENGTH];
        new SecureRandom().nextBytes(iv);
        Cipher cipher = Cipher.getInstance(ALGORITHM);
        cipher.init(Cipher.ENCRYPT_MODE, new SecretKeySpec(secretKey, "AES"),
            new GCMParameterSpec(GCM_TAG_LENGTH, iv));
        byte[] ciphertext = cipher.doFinal(plaintext.getBytes(StandardCharsets.UTF_8));
        byte[] encrypted = ByteBuffer.allocate(iv.length + ciphertext.length)
            .put(iv).put(ciphertext).array();
        return Base64.getEncoder().encodeToString(encrypted);
    }
}
```

```java
// 数据脱敏工具
public class DataMaskingUtils {
    public static String maskEmail(String email) {
        if (email == null) return null;
        int atIndex = email.indexOf('@');
        if (atIndex <= 2) return "***" + email.substring(atIndex);
        return email.substring(0, 2) + "***" + email.substring(atIndex);
    }
    public static String maskPhone(String phone) {
        if (phone == null || phone.length() < 7) return "***";
        return phone.substring(0, 3) + "****" + phone.substring(phone.length() - 4);
    }
}
```

```java
// Bean Validation 示例
@Entity
public class User {
    @NotBlank @Size(min = 3, max = 20)
    @Pattern(regexp = "^[a-zA-Z][a-zA-Z0-9_]*$")
    private String username;

    @NotBlank @Email
    private String email;
}
```

```java
// XXE 防护 - 安全 XML 解析器
DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance();
dbf.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, true);
dbf.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
dbf.setFeature("http://xml.org/sax/features/external-general-entities", false);
dbf.setNamespaceAware(true);
```

```bash
# 依赖漏洞检查
mvn org.owasp:dependency-check-maven:check
gradle dependencyCheckAnalyze
```

```java
// 安全日志记录
@Slf4j
@Service
public class SecurityAuditService {
    public void logLoginSuccess(String username, String ip) {
        log.info("SECURITY AUDIT: Login success - user: {}, ip: {}", username, ip);
    }
}
```

```java
// 异常处理不泄露信息
catch (SQLException e) {
    log.error("Database error", e);
    throw new RuntimeException("操作失败，请稍后重试");
}
```
