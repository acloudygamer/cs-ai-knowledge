# 并发与异步

## 概述

Java 并发体系：

```
Thread / Runnable → ExecutorService → CompletableFuture → Virtual Threads
Concurrent Collections → Synchronizers → 原子类
```

## 线程基础

### 创建线程

### 线程状态

| 状态 | 说明 |
|------|------|
| NEW | 创建但未启动 |
| RUNNABLE | 可运行（可能正在运行）|
| BLOCKED | 等待获取锁 |
| WAITING | 无限期等待 |
| TIMED_WAITING | 有限期等待 |
| TERMINATED | 已终止 |

### 线程同步

## ExecutorService

线程池类型：FixedThreadPool、SingleThreadExecutor、CachedThreadPool、ScheduledThreadPool。

### 线程池类型

### 提交任务

### 批量提交

## CompletableFuture

### 创建

### 链式操作

### 组合

## Concurrent Collections

### 常用并发集合

### 生产者-消费者模式

## Synchronizers

### CountDownLatch

等待一组操作完成。

### Semaphore

控制并发访问资源的数量。

### CyclicBarrier

让线程相互等待。

### Phaser

更灵活的屏障，支持动态注册。

## 原子类

## ThreadLocal

线程本地变量，每个线程有独立的副本。

## 参考样例

```java
// 创建线程
Thread thread = new Thread(() -> System.out.println("Running"));
thread.start();

Thread.ofPlatform().start(() -> System.out.println("Platform thread"));
```

```java
// synchronized
public void increment() {
    synchronized (this) {
        count++;
    }
}
```

```java
// ExecutorService
ExecutorService fixed = Executors.newFixedThreadPool(4);
Future<Integer> future = executor.submit(() -> {
    return 42;
});
Integer result = future.get();
```

```java
// CompletableFuture
CompletableFuture<String> future = CompletableFuture
    .supplyAsync(() -> "Hello")
    .thenApply(s -> s + " World")
    .thenApply(String::toUpperCase);

CompletableFuture.allOf(f1, f2, f3).join();
```

```java
// ConcurrentHashMap
ConcurrentHashMap<String, Integer> map = new ConcurrentHashMap<>();
map.putIfAbsent("b", 2);
map.computeIfAbsent("c", k -> compute(k));
```

```java
// 生产者-消费者
BlockingQueue<Integer> queue = new LinkedBlockingQueue<>(10);
queue.put(i);  // 队列满时阻塞
Integer value = queue.take();  // 队列空时阻塞
```

```java
// CountDownLatch
CountDownLatch latch = new CountDownLatch(3);
latch.countDown();
latch.await();
```

```java
// Semaphore
Semaphore semaphore = new Semaphore(3);
semaphore.acquire();
accessResource();
semaphore.release();
```

```java
// 原子类
AtomicInteger counter = new AtomicInteger(0);
counter.incrementAndGet();
counter.updateAndGet(x -> x * 2);
```

```java
// ThreadLocal
ThreadLocal<String> threadLocal = new ThreadLocal<>();
threadLocal.set("value");
String value = threadLocal.get();
threadLocal.remove();
```

```java
// 最佳实践
ExecutorService executor = Executors.newFixedThreadPool(4);
CompletableFuture.supplyAsync(() -> doStuff(), executor);
ConcurrentHashMap<> map = new ConcurrentHashMap<>();
AtomicInteger counter = new AtomicInteger(0);
```
