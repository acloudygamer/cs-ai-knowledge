# cs-ai-knowledge 重写工程交接

本仓库正在对 `1-数据结构与算法/`、`2-Python/`、`3-C++/` 三个文件夹做**全文提质重写**。本文件是上下文交接，接手者读此即可继续。

## 工程目标

把三文件夹改写为**更好理解的知识**——用户原话"之前对数学写的太多了""实际真正影响速度的是进入缓存的命中率，我希望更偏工程化一点""核心数学就留在数学模型里面就好了，其他地方的数学只有在必要的情况下把它写上去，所谓必要就是可以帮助理解"。用户已授予内容大改权（合并/删除/调位均可）。

## 质量标准（用户确认，已存入记忆 knowledge-writing-style）

- **工程化优先，清晰好理解为先**。旧宪法 `prompts/constitution.md` 已删除作废（不要去找它）。
- **必须保留**："数学模型"与"数据流"两块。其余结构自由。
- **章节命名**：用"本质"（简洁解释性优先，不强调"一句话"）、"代码示例"（不用"参考存根"）。
- **数学取舍（关键）**：核心数学集中在"数学模型"块；其他地方只在"能帮助理解"时才补数学，不为显深而塞公式。删除"归约终点"式收尾（旧宪法残留），要点睛可用一句"> **洞察**：..."。
- **工程化手段按需**：缓存命中率/预取器失效只用于"确实受缓存影响"的概念（如链表遍历），不强制每个概念都讲，它属于工程化优先的一种手段，合适才放。
- **代码示例**：给注释 + 简易输入输出（含预期结果）。
  - 文件夹1（数据结构与算法）：Python 3.12 + C++20 **双语言并列**（spec 硬要求）。
  - 文件夹2：Python 3.12 stable（latest=3.14，专门讲新特性的章节保留并标注）。
  - 文件夹3：C++20 stable（latest=C++23/26，专用文档保留）。
- **每个文件的标准结构**（试点 `1-数据结构与算法/01-基础数据结构/02-链表.md` 是基线模板，先读它对齐尺度）：
  `# 标题` → `> 版本基准` → `## 本质` → `## 数学模型`（含洞察）→ `## 数据流`（ASCII `<pre>` 图）→ `## 对比参照`（表格，可选）→ `## 机制` → `## 代码示例`（Python + C++20）

## 两份规范（修改时的硬约束）

- `versions.json`（项目根）：版本基准。文件夹1=Python 3.12+C++20（代码最简/算法最优）；2=stable Python 3.12/latest 3.14；3=stable C++20/latest C++23+26。策略 **stable 为主 + latest 标注**。
- `公式排版规范.md`（项目根）：三铁律。① `\text{}` 内禁 `_` 改 `-`（但 Python 魔术方法 `__enter__`、C++ 标准库 `shared_ptr` 等标识符**绝不能**改连字符，应**出公式为行内代码**——这是用户明确指示"能出公式就出公式"）；② `%` 写 `$` 外或 `\\%`，禁单 `\%`；③ 含公式文字斜体用 `>` 或 `<i>`，禁 `*...*`（仓库无此违规）。

## 已完成的阶段

- **阶段 -1 ✅** 删除 `prompts/constitution.md`、`prompts/example.md`，清理根 README + 文件夹 0/4/5/6 README 中失效宪法引用。
- **阶段 0 ✅** 试点 `02-链表.md` 重写，质量基线已确认。
- **阶段 1 ✅** 公式铁律修正（53 文件）。铁律一 `\text{}` 含 `_` 共 189 处、铁律二单 `\%` 7 处，全部清零。处理方式：标识符出公式为行内代码（约 77 处）、纯分隔符型连字符化（约 96 处）、随重复损坏删除若干。已跑 formula-spacer 复排。
- **阶段 2 ✅** 文件夹2版本标注：5 处 `# Python 3.14:` 误标改正（os.preadv 3.7+/http 标准库/orjson 三方/UTC 3.11+/importlib.resources 3.9+）；修 README `版本.json`→`versions.json`。
- **阶段 3 ✅** 文件夹3代码降级（7+3 文件）：stable 段 C++23/26 特性（`std::print/println`/`expected`/`ranges::to`/`generator`/`move_only_function`）降级为 C++20 等价（`cout`/`cerr`+`format`、`variant`+`get_if`、手写 promise_type、`std::function`），整节主题性者加 `> **latest 预览**` 标注块。`08-C++23新特性.md`/`14-C++26新特性.md` 是 latest 专用文档，保留不动。
- **阶段 4 ✅** 文件夹1补 C++20 + 提质。全部完成：
  - `01-基础数据结构` 5/5 ✅：01-数组与字符串、02-链表、03-栈与队列、04-哈希表、05-字符串模式匹配
  - `02-高级数据结构` 13/13 ✅：01-树与二叉树、02-堆与优先队列、03-图、04-并查集、05-前缀树、06-线段树与树状数组、07-跳表、08-RMQ问题、09-KD-Tree、10-Treap、11-HLD、12-Link-Cut-Tree、13-字符串索引体系
  - `03-算法思想` 11/11 ✅：01-排序、02-二分搜索、03-动态规划、04-贪心、05-回溯、06-位运算、07-递归与分治、08-单调栈、09-分支限界、10-滑动窗口、11-双指针
  - `04-复杂度分析` 1/1 ✅：01-复杂度分析

## 剩余工作

**✅ 阶段 5（文件夹2、3 正文提质）已全部完成。** 无剩余工作。以下为历史记录备查。

**阶段 5（文件夹2、3 正文提质）：**
- `2-Python/` 约 51 个 md。**不需补 C++**，只提质 + 清重复损坏 + 去归约终点 + 按"核心数学集中、余处按需"修正。重复损坏文字（同一句重复多遍）大量残留，是本阶段主战场。
- `3-C++/` 约 41 个 md。同上，阶段3已处理超版本，提质时清重复损坏 + 去归约终点。

### 阶段5 已完成进度

**✅ 阶段5 全部完成。**

文件夹2 `2-Python/`：
- ✅ `00-简介` 3/3、✅ `01-基础` 6/6、✅ `02-常用操作` 13/13、✅ `03-高级用法` 8/8、✅ `04-测试` 5/5、✅ `05-设计模式` 1/1、✅ `06-Web开发` 3/3、✅ `07-数据分析与ML` 1/1、✅ `08-代码质量工具` 1/1（共 41 内容文件）

文件夹3 `3-C++/`：
- ✅ `01-基础` 9/9、✅ `02-常用操作` 12/12、✅ `03-高级用法` 14/14、✅ `04-测试` 1/1（共 36 内容文件）
- 标题批量改名（`## 定义`→`## 本质`、`## 参考存根`→`## 代码示例`，76 处/36 文件）+ 逐文件清重复损坏 + spacer + 验证三连全部完成
- `08-C++23新特性.md`/`14-C++26新特性.md`（latest 专用）已纳入清重复损坏，顶部 `> **版本关系**`/`> **版本注解**` latest 标注块保留

全局回归（全三文件夹）：铁律一 `\text{}` 含 `_` = 0、单 `\%` = 0、标题残留 `## 定义`/`## 参考存根` = 0、归约终点 = 0、挤行（段后/列表后引用）= 0。各文件剩余 dup-detector 命中均经核实为合法并列结构（如"prvalue→.../xvalue→.../lvalue→..."、`强符号 ⊕ 强符号`、"A 要求 C≥… / B 要求 C≥…"对仗），非损坏。

### 阶段5 批量已做的机械变换（勿重复）

本会话已对文件夹2全部内容文件、文件夹3全部内容文件执行：
1. **标题**：`## 定义`→`## 本质`、`## 参考存根`→`## 代码示例`（文件夹2 用 `tools/stage5-mech.js`；文件夹3 用 `tools/rename-cpp-titles.js` 批量补做，76 处/36 文件——stage5-mech.js 原本未覆盖文件夹3）
2. **版本基准行**：顶部插入 `> **版本基准**：Python 3.12 stable（latest=3.14，新特性章节保留并标注）`（`stage5-mech.js`；已用 `tools/dedup-ver.js` 去重重复行）
3. **归约终点→洞察**：`**归约终点**：`→`> **洞察**：`（行首）/`**洞察**：`（行内，仅 3-C++/01-语言简介 1 处）（`tools/stage5-guiyue.js`，29 文件 39 处）
4. **删尾部冗余版本标注块**：`---\n\n**版本标注**：本文内容为 Python 通用知识，无版本特定差异。`（`tools/stage5-verblock.js`）

剩余每个文件只需：**清重复损坏**（手动）+ 跑 spacer + 验证三连。归约终点/标题/版本行已批量处理完毕。

> **⚠️ 批量漏网**：`2-Python/03-高级用法/08-Python3.14新特性.md` 的 `## 定义`/`## 参考存根` 标题未被 stage5-mech.js 改名（latest 专用文档疑似漏批），已手动改为 `## 本质`/`## 代码示例`。接手者对其他 latest 专用文档（含 `3-C++/08-C++23新特性.md`、`14-C++26新特性.md`）留意同类标题残留。该 08 文件顶部的 `> **版本注解**：...` 块即"latest 标注"，保留不替换为标准版本基准行。

### 阶段5 单文件处理流程（必读）

1. **跑两个检测器**定位损坏行：
   ```
   f="2-Python/03-高级用法/03-性能优化.md"
   node tools/dup-detector.js "$f"   # 主检测器：剥公式、L≥8、需中文
   node tools/dup-detector2.js "$f"  # 补检测器：不剥公式、L≥10、需中文(抓"短中文+公式"型重复)
   ```
   两检测器行号去重合并即全部嫌疑行。**约 30-40% 是误报**（如"Python 对象"x3、"惰性数据"x2、表格行结构、"约束： $..$"在公式内出现两次属合理），需逐行 awk 核实。
2. **⚠️ Read 工具会截断超长损坏行！** 不要信 Read 的显示。用 `awk 'NR==行号{print NR": "$0}' "$f"` 取损坏行**原文精确串**。
3. **Read 文件 limit=1 即满足 Edit 前置**（省 token）：`Read(path, limit=1)` 后即可用 awk 取到的精确串做 `old_string`、清理后做 `new_string` 批量 Edit。
4. **清理规则**：损坏行 = 正确前缀 + 重复后缀片段。保留首个完整句、删掉所有重复尾巴。注意顺手补未闭合 backtick（如 09-日志记录 `propagate=False。`→`propagate=False`。）、修表格尾余列（`| ... | | $O(n)$ | |`→`| ... | $O(n)$ |`）。
5. **跑 spacer**：`node tools/formula-spacer.js "$f"`。
6. **验证三连**（同文件夹1）：
   - 铁律一：`grep -nE '\\text\{[^}]*_[^}]*\}'` 应 0
   - 挤行（段后引用）：`grep -nE '[^>[:space:]]> \*\*|^- .*> \*\*'` 应 0
   - 标题残留：`grep -nE '^## (定义|参考存根)'` 应 0；归约终点：`grep '归约终点'` 应 0
   - dup 复查：`node tools/dup-detector.js "$f" | grep -c '^[0-9]'` —— 剩余命中若经核实全为合理重复即可接受。

### 阶段5 关键坑（已踩）

1. **Read 截断超长行**：损坏行（重复多遍）极长，Read 显示只给前半，导致 Edit `old_string` 不匹配。**一律用 awk 取原文**。
2. **归约终点→`> **洞察**` 的挤行陷阱**：当 `> **洞察**：` 前一段以行内数学 `$...$` 结尾时，spacer 会吞空行把段落与引用块挤到同一行（如 `...$\square$> **洞察**`）。**规避**：若前段以数学结尾，洞察改用加粗段落 `**洞察**：...`（不带 `>`），不要用引用块。跑完 spacer 用上面的挤行 grep 复查。本会话 09-分支限界.md 已踩并改加粗段落规避。
3. **dup-detector 漏"短中文+公式"型**：如 `约束： $n \leq ...$ 约束： $n \leq ...$` 因剥公式后中文片段<8 字符漏报。**必须用 dup-detector2.js（不剥公式）补扫**。
4. **Git Bash 内联 `node -e` 吃反斜杠**：Windows Git Bash（MSYS）会把内联 `node -e '...'` 参数里的 `\\` 吃成 `\`，JS 再把未识别转义 `\l`/`\t` 等当字面量丢杠 → LaTeX 命令 `\log`/`\times` 反斜杠丢失。**含反斜杠的字符串一律不要放进内联 `node -e`**。改用 Write 工具写 `.js` 脚本文件再 `node file.js`，或把含杠数据放 JSON（Write 直写）后内联脚本只做 `lines[i]=map[k]`。本会话 03-性能优化.md 第 20 行 `$O(n \log n)$` 因此丢杠、第 22 行洞察被误加 `$>` 前缀，已用脚本文件方式修复。详见记忆 [[msys-backslash-trap]]。

**节奏**：用户要求"维持逐文件深度"。每完成一个子目录主动停顿汇报一次。

**顺手修**（已在本会话完成）：`1-.../03-栈与队列.md:130` 单 `\%` 已改行内代码 `(tail+1) % k != head`。全三文件夹 `\%` 现已 0。

## 每个文件的处理流程（文件夹1 用，阶段5 见上）

1. **Read 原文**，判断：数学是否正确、有无重复损坏、有无归约终点、是否缺 C++20（仅文件夹1）。
2. **按试点标准重写**（本质+数学模型+数据流+机制+对比参照+代码示例）。保留正确数学，去归约终点换"洞察"，清重复损坏，补 C++20（文件夹1）。
3. **Write 落盘**后跑 `node tools/formula-spacer.js <文件>`，再验证三连（铁律一/挤行/块公式）。

## 工具陷阱（已踩过，务必规避）

- `tools/formula-spacer.js` 只管公式与中文空格，**不执行三铁律**，且会引入两类损坏：
  1. **转义美元符冲突**：正文里别用 `$\$` 表"美元符字面量"，改用行内代码 `` `$` ``。
  2. **列表项/段后引用挤行**：`- 列表项` 或以 `$...$` 结尾的段落 后紧跟 `> **洞察**：` 会被挤到同一行。重写时让前段后空一行再接引用块，或改加粗段落；跑完 spacer 用挤行 grep 复查。
  3. **块公式 `$$` 后插空行**：某些 `$$...$$` 块公式 spacer 会在 `$$` 后插空行导致断裂。跑完抽查。
- 工作区有**会话前遗留改动**（公式空格规范化痕迹、`versions.json` 改双语言等），均符合目标方向，**不要回退**。`git status` 显示的改动不全是本会话所做。

## 验证全局回归

阶段完成后回归：
```
grep -rnE '\\text\{[^}]*_[^}]*\}' "1-数据结构与算法" "2-Python" "3-C++" --include=*.md   # 应 0
grep -rnE '(^|[^\\])\\\%' "1-数据结构与算法" "2-Python" "3-C++" --include=*.md          # 应 0（本会话已 0）
node tools/formula-spacer.js "1-数据结构与算法" "2-Python" "3-C++"                      # 全量复排
git diff --stat                                                                          # 复核改动未越界到文件夹4/5/6
```

## 记忆

`C:\Users\zyq\.claude\projects\D--cs-ai-knowledge\memory\knowledge-writing-style.md` 存了完整质量标准，`formula-spacer.md` 记了挤行陷阱（含本会话新增的"段后引用挤行"变体），`MEMORY.md` 是索引。接手时自动加载。

## 当前进度速查

- ✅ 阶段-1 清理、✅ 阶段0 试点、✅ 阶段1 公式铁律、✅ 阶段2 版本标注、✅ 阶段3 C++降级
- ✅ 阶段4 文件夹1补C++20+提质（30 文件全完成）
- ✅ 阶段5 文件夹2、3 提质全部完成（文件夹2 41 内容文件 + 文件夹3 36 内容文件）。全局回归五项均 0，无越界。

**全工程已完成。** 三文件夹 `1-数据结构与算法`/`2-Python`/`3-C++` 正文提质重写收尾。后续如需进一步工作（如补充版本基准行、深化某文件），请单独指明。
