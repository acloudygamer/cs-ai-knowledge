# 函数

## 定义

Go 函数的核心价值在于**多返回值机制**——特别是 `(result, error)` 模式，使错误处理成为函数签名的显式组成部分，而非隐藏的异常机制。defer 关键字实现 LIFO 语义的资源清理，闭包允许函数捕获并持久化外部变量状态。Go 函数满足 lambda 演算规则：函数是值，可赋值、可传递、可返回。

## 数学模型

### 函数作为一等公民的 lambda 演算

Go 函数类型满足 **lambda 演算**的基本规则：

设函数类型为 $T \to U$（输入 T，输出 U），则 Go 的函数类型是**闭包**：

$$\text{Closure} = \lambda x. \text{body} + \text{captured\_variables}$$

Go 函数是 lambda 演算的实现：
- **函数是值**：可赋值给变量，作为参数传递，作为返回值
- **词法作用域**：闭包捕获定义时外部变量，而非调用时
- **引用透明**：无副作用的函数可安全求值

### defer 的栈帧模型

defer 调用存储在**调用者栈帧**中，按 LIFO 顺序执行：

$$\text{DeferStack} = \{ d_1, d_2, \ldots, d_n \} \text{ where } d_i \text{ added at position } i$$

执行顺序：$d_n, d_{n-1}, \ldots, d_1$

**复杂度**：
- defer 注册：$O(1)$（仅追加到链表头）
- defer 执行：$O(n)$（函数返回时遍历执行）

**空间复杂度**：defer 结构体占用固定大小（函数指针 + 栈指针 + 链接指针），n 个 defer 占用 $O(n)$ 空间。

### 闭包捕获的代数语义

闭包捕获变量时，被捕获变量的**引用**被闭包持有：

$$\text{Closure}(f, \text{captured}) = \lambda x. f(x, \text{captured})$$

**生命周期约束**：
- 闭包内部修改被捕获变量 → 影响外部变量（共享状态）
- 被捕获变量的生命周期 ≥ 闭包的生命周期
- 闭包阻止 GC 回收被捕获变量

## 数据流

<pre>
函数调用数据流
═══════════════════════════════════════════════════════════════════════

调用者栈帧：
┌─────────────────────────────────────────────────────────────────────┐
│  参数 (arg1, arg2, ...)     ← 调用者局部变量，按值传递（副本）       │
│  返回地址 (RA)              ← 控制流标记，跳转返回位置                 │
│  ─────────────────────────────────────────────────────────────────  │
│  defer 结构体链表           ← 延迟调用记录，LIFO 执行                 │
│  ─────────────────────────────────────────────────────────────────  │
│  局部变量                   ← 函数作用域数据                          │
│  命名返回值 slot           ← 具名返回值的存储位置                    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼ 数据形态转移
被调用者执行：
  1. 保存调用者 PC 到 RIP（控制流）
  2. 分配被调用者栈帧
  3. 复制参数（按值传递，副本在栈上）
  4. 执行函数体
  5. （可选）将返回值写入调用者栈帧
  6. 执行 defer (LIFO)
  7. 恢复调用者栈帧，跳转回 RA
</pre>

<pre>
闭包捕获数据流
═══════════════════════════════════════════════════════════════════════

func adder() func(int) int {
    sum := 0  // 数据形态：adder 栈帧上的局部变量
    return func(x int) int {
        sum += x  // 闭包内部修改外部变量
        return sum
    }
}
        │
        ▼ 数据形态转移：值 → 引用（逃逸分析决定堆分配）
        │
        ▼
闭包对象: {
    fn: func(x int) int,
    captured: { sum: &堆地址 }  // sum 逃逸到堆
}
        │
        ▼
pos := adder()
pos(1)  // sum=1，堆上 sum 被修改
pos(2)  // sum=3，sum 持久化（闭包持有引用）
</pre>

## 机制

### 为什么 Go 选择多返回值

多返回值的设计满足**错误传播的组合律**：

$$\text{Result} = \text{Ok} \oplus \text{Err}$$

其中 $\oplus$ 表示结果和错误的正交组合——正常值和错误不会互相覆盖。

相比异常机制的优势：
- **显式处理约束**：错误必须被显式处理，不能忽略（编译器可检测）
- **路径可见性**：错误路径和正常路径在代码中同等地位
- **局部性**：错误可以逐层传播或就地处理

### 为什么 defer 执行顺序是 LIFO

defer 的 LIFO 执行顺序保证**资源释放的反向顺序**：

```go
defer file.Close()      // 后打开先关闭（file 依赖 fd）
defer mu.Unlock()       // 后加锁先解锁（锁保护临界区）
```

这符合资源管理的**嵌套语义**：内层资源依赖外层资源，内层先获取，外层后释放。LIFO 顺序保证先释放内层，再释放外层。

**违反约束的后果**：若顺序错误，可能导致 use-after-free（文件已关闭但仍被引用）或死锁（锁未释放但已等待）。

### defer 在 panic 时的行为约束

defer 在 panic 时仍然执行，这使得：
- 即使程序崩溃，也能保证资源释放（文件系统句柄、锁状态）
- 可以用 `recover()` 捕获 panic，实现类似异常的处理

**约束**：`recover()` 仅在 defer 中有效，且只在 panic 传播过程中有效。已恢复的 panic 不会继续传播。

**违反约束**：过度使用 panic/recover 破坏 Go 的显式错误处理哲学，使控制流难以追踪。

### 闭包 vs 函数指针

| 维度 | 函数指针 | 闭包 |
|------|----------|------|
| 捕获环境 | 否（仅代码地址） | 是（代码+环境） |
| 状态持久化 | 否 | 是 |
| 内存分配 | 栈（编译时确定） | 堆（逃逸分析决定） |
| 适用场景 | C 回调、插件架构 | 迭代器、工厂函数、延迟求值 |

## 多返回值

函数可返回多个值，error 作为最后一个返回值是 Go 的惯用约定：

```go
func divide(a, b int) (int, error) {
    if b == 0 { return 0, errors.New("division by zero") }
    return a / b, nil
}
result, err := divide(10, 2)
if err != nil { /* 处理错误 */ }
```

## 命名返回值

返回值作为命名变量在函数体中使用，return 省略变量名：

```go
func split(sum int) (x, y int) {
    x = sum * 4 / 9
    y = sum - x
    return  // 隐式返回 x, y
}
```

**机制**：命名返回值在函数入口初始化为该类型的零值，在函数体中可像局部变量一样使用。

## defer 延迟执行

defer 在函数退出前按 LIFO 顺序执行：

```go
defer fmt.Println("1")
defer fmt.Println("2")
defer fmt.Println("3")
// 输出: 3 2 1
```

**常用场景**：文件关闭、锁解锁、连接关闭、计时器取消。

## 函数式选项模式

用函数配置结构体，支持可选参数和默认值：

```go
type Option func(*Server)

func WithPort(port int) Option {
    return func(s *Server) { s.port = port }
}

func NewServer(opts ...Option) *Server {
    s := &Server{port: 8080}  // 默认值
    for _, opt := range opts { opt(s) }
    return s
}

server := NewServer(WithPort(9090))
```

**数学本质**：Option 是函数复合 $f \circ g$ 的应用，通过函数式组合替代了建造者模式的链式调用。

## 参考存根

```go
// defer 实现核心结构：src/runtime/runtime2.go
type g struct {
    _defer *scade  // 指向 defer 链表
}

type scade struct {
    fn       *funcval  // 延迟调用的函数
    sp       uintptr   // 注册时的栈指针
    link     *scade    // 链表 next
}

// defer 注册：编译时转换
// defer fmt.Println("x")
// → jmp deferproc → 将 defer 结构体插入链表头

// defer 执行：src/runtime/panic.go
// 函数返回前调用 deferprocext → 遍历执行 defer 链表
```
