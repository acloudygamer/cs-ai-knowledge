# 控制流程

## 定义

Go 的控制流程遵循**极简主义**：仅有一种循环语法 `for`，`switch` 默认 break（不贯穿），必须显式 `fallthrough` 才能贯穿。这种设计通过**语法约束**消除了 C 系语言中常见的漏写 break 导致的行为错误，同时 for range 提供对容器类型的统一遍历抽象。

## 数学模型

### for 循环的复杂度

标准 for 循环：
 $T(n) = O(1) + n \times O(\text{body})$ 

for range 循环（切片/数组）：
 $T(n) = O(1) + n \times O(1) = O(n)$ 

for range 循环（map）：
 $T(n) = O(n) \text{（遍历顺序不确定，哈希桶随机起始）}$ 

for range 循环（channel）：
 $T(n) = O(\text{元素数}) \text{ 或 } O(\infty) \text{（阻塞直到 channel 关闭）}$ 

### switch 的模式匹配复杂度

switch 表达式求值 + case 匹配：
 $T(\text{switch}) = O(\text{expr}) + O(\text{matched\_case})$ 

**Go 编译器优化策略**：

| case 类型 | 编译器策略 | 复杂度 |
|-----------|-----------|--------|
| case 为有序常量（int/string）且 ≥ 4 个 | 二分查找 | $O(\log n)$ | |
| case 为无序常量或 < 4 个 | 线性查找 | $O(n)$ | |
| case 包含函数调用或复杂表达式 | if-else 链 | $O(n)$ | |

Go 1.21+ 编译器优化：若 case 为有序常量且可排序，使用二分查找，复杂度降为 $O(\log n)$ 。 。

### for range 整数循环的语义（Go 1.22+）

`for i := range n` 等价于：

```go
for i := 0; i < n; i++ { /* body */ }
```

**形式化语义**：整数 range 展开为：

```go
// 编译器展开（Go 1.22+）
for i := 0; i < n; i++ {
    i := i  // 每次迭代创建新变量（避免闭包捕获陷阱）
    /* body */
}
```

**约束条件**：
- 遍历范围 $[0, n)$ ，不包含 $n$ ，不包含 $n$ 
- 若 $n < 0$ ，循环体不执行（空区间） ，循环体不执行（空区间）
- $n$ 必须是整数类型（int），不接受浮点数 必须是整数类型（int），不接受浮点数

### 循环变量作用域修复（Go 1.22）

Go 1.21 及之前，for range 的循环变量是**函数级作用域**（共享的）：

```
for i := range 3 {
    go func() { fmt.Println(i) }()  // i 是共享的，goroutine 执行时 i=3
}
```

Go 1.22 修复后，每次迭代的 `i` 是**新变量**，等价于：

```
for i := 0; i < 3; i++ {
    i := i  // 每次迭代创建新变量
    go func() { fmt.Println(i) }()
}
```

**修复的形式化保证**：设循环执行 $n$ 次，则循环体内存在 $n$ 个不同的变量 $i_k$ （ $k = 0, 1, \ldots, n-1$ ），每个 $i_k$ 在对应迭代的词法作用域内绑定。 次，则循环体内存在 $n$ 个不同的变量 $i_k$ （ $k = 0, 1, \ldots, n-1$ ），每个 $i_k$ 在对应迭代的词法作用域内绑定。 个不同的变量 $i_k$ （ $k = 0, 1, \ldots, n-1$ ），每个 $i_k$ 在对应迭代的词法作用域内绑定。 （ $k = 0, 1, \ldots, n-1$ ），每个 $i_k$ 在对应迭代的词法作用域内绑定。 ），每个 $i_k$ 在对应迭代的词法作用域内绑定。 在对应迭代的词法作用域内绑定。

## 数据流

<pre>
switch 匹配数据流
═══════════════════════════════════════════════════════════════════════

switch expr {
case v1: 块1
case v2, v3: 块2  ← 多值匹配（OR 关系）
default: 块3
}
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│ 1. 求值 expr                                                 │
│ 2. 按声明顺序比较 case（Go 1.21+ 可排序时用二分）             │
│ 3. 匹配第一个 case → 执行块 → break                         │
│ 4. 无匹配 → 执行 default（若有）                            │
│ 5. 无 default → 不执行任何块                                 │
└───────────────────────────────────────────────────────────────┘
</pre>

<pre>
switch 编译策略
═══════════════════════════════════════════════════════════════════════

有序 case ≥ 4 个 ──→ 二分查找
    │
    │ (e.g., case 1, 2, 3, 5, 8, 13...)
    ▼
┌─────────────────────────────────────┐
│     mid = (lo + hi) / 2           │
│     if expr < case[mid] → hi = mid │
│     else if expr > case[mid] → lo  │
│     else → 匹配成功                │
└─────────────────────────────────────┘

case < 4 个或无序 ──→ 线性 if-else 链
    │
    ▼
┌─────────────────────────────────────┐
│ if expr == case[0] → 块0           │
│ else if expr == case[1] → 块1      │
│ ...                                │
└─────────────────────────────────────┘
</pre>

<pre>
for range 数据流
═══════════════════════════════════════════════════════════════════════

for i, v := range container {
    /* 使用 i, v */
}
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  容器类型        i 值                  v 值                   │
│  ──────────────────────────────────────────────────────────  │
│  数组/切片       索引 [0, len)        元素值（副本）          │
│  map             键（随机序）         值（副本）             │
│  string          字节索引              rune 值（Unicode）     │
│  channel         无索引（阻塞）        元素值                │
└───────────────────────────────────────────────────────────────┘
</pre>

<pre>
for range map 遍历数据流
═══════════════════════════════════════════════════════════════════════

遍历开始：
┌───────────────────────────────────────────────────────────────┐
│ 1. hmap 随机选择起始桶 B_start（与 B 的 hash 随机种子相关）  │
│ 2. 从 B_start 开始遍历所有 2^B 个桶                          │
│ 3. 每个桶内遍历 8 个 key-value 对，跳过空 tophash            │
│ 4. 若溢出桶非空，继续遍历溢出链表                             │
└───────────────────────────────────────────────────────────────┘

关键约束：
- 遍历顺序 = f(随机种子, 桶编号)
- 同一 map 每次遍历顺序不同
- 遍历过程中若发生扩容，部分元素可能重复或跳过（并发安全）
</pre>

## 对比参照

| 维度 | for（标准） | for range | switch | if |
|------|-------------|-----------|--------|-----|
| **适用场景** | 精确计数/自定义步进 | 遍历容器/通道 | 多值匹配/条件分支 | 单一条件判断 |
| **终止条件** | 布尔表达式为 false | 容器耗尽/通道关闭 | 首个匹配 case + break | else 分支执行 |
| **贯穿语义** | 不适用 | 不适用 | 默认不贯穿（需 fallthrough） | 不适用 |
| **默认 break** | 需要显式 | 需要显式（channel 除外） | 自动 | 自动 |
| **复杂度** | $O(n)$ 或 $O(\text{body})$ | $O(n)$ （map 除外） | $O(1)$~ $O(\log n)$ | $O(1)$ | 或 $O(\text{body})$ | $O(n)$ （map 除外） | $O(1)$~ $O(\log n)$ | $O(1)$ | | $O(n)$ （map 除外） | $O(1)$~ $O(\log n)$ | $O(1)$ | （map 除外） | $O(1)$ ~ $O(\log n)$ | $O(1)$ |~ $O(\log n)$ | $O(1)$ | | $O(1)$ | |

**for range 遍历语义的数学约束**：

| 容器类型 | 索引/键的数据形态 | 值的数据形态 | 遍历复杂度 | 顺序确定性 |
|----------|------------------|-------------|-----------|-----------|
| 数组 `[n]T` | `int`（0 到 n-1） | `T`（副本） | $O(n)$ | 确定性（索引顺序） | | 确定性（索引顺序） |
| 切片 `[]T` | `int`（0 到 len-1） | `T`（副本） | $O(n)$ | 确定性（索引顺序） | | 确定性（索引顺序） |
| map `map[K]V` | `K`（键类型） | `V`（副本） | $O(n)$ | 伪随机（哈希桶随机起始） | | 伪随机（哈希桶随机起始） |
| string | `int`（字节索引） | `rune`（Unicode 码点） | $O(n)$ | 确定性（字节顺序） | | 确定性（字节顺序） |
| channel `chan T` | 无索引 | `T`（接收值） | 阻塞直到关闭 | 不适用（串行） |

## 机制

### 为什么 Go 不需要 break（默认）

Go 的 switch 的**默认 break 语义**消除了 C 系语言的常见 bug：

```c
// C: 漏写 break 导致贯穿（常见 bug）
switch (x) {
    case 1: doOne();  // 没有 break！
    case 2: doTwo(); break;
}
// x=1 时执行 doOne() 和 doTwo()
```

```go
// Go: 默认不贯穿，必须显式 fallthrough
switch x {
case 1: doOne()      // 不会贯穿到 case 2
case 2: doTwo()
}
```

**设计约束**：fallthrough 是显式的，需要开发者明确声明。这使得代码意图更清晰——意图贯穿时必须写出来。

### fallthrough 的语义约束

fallthrough 不是"跳到下一个 case 执行"，而是**跳过下一个 case 的条件判断，直接执行下一个 case 的主体**：

```go
switch x {
case 1:
    doOne()
    fallthrough  // 无条件跳到 case 2 的主体
case 2:
    doTwo()  // 一定会执行
}
```

**约束**：fallthrough 不能用在最后一个 case 或 default 中。

### 为什么 map 遍历顺序不确定

map 的哈希表实现导致遍历顺序**伪随机**：
- 哈希桶按哈希值排序，而非插入顺序
- 每次遍历从随机桶开始
- 伪随机性防止程序对遍历顺序产生依赖（避免隐式顺序依赖 bug）

**约束**：若需要有序遍历，必须使用**切片**存储键并排序：

```go
keys := make([]string, 0, len(m))
for k := range m { keys = append(keys, k) }
sort.Strings(keys)
for _, k := range keys { fmt.Println(k, m[k]) }
```

### 为什么 channel range 会阻塞

`for v := range ch` 等价于：

```go
for {
    v, ok := <-ch
    if !ok { break }
    // 使用 v
}
```

当 channel 关闭且无数据时，`ok = false`，循环退出。若 channel 未关闭且无数据，接收操作阻塞。

**约束**：若 channel 永不为空且永有关闭，range 循环永远阻塞。

## if / else

条件必须是布尔值，不支持隐式转型。条件前可包含短语句（初始化）：

```go
if age := 30; age >= 18 {
    fmt.Println("成年")
} else {
    fmt.Println("未成年")
}
```

## for 循环

`for` 是 Go 唯一的循环语法：

```go
for i := 0; i < 10; i++ { fmt.Println(i) }
for i < 10 { i++ }                      // 省略初始和结束
for { /* 死循环 */ }                     // 省略所有
```

## for range

for range 遍历切片、数组、map、字符串、channel：

```go
nums := []int{1, 2, 3}
for i, v := range nums { fmt.Println(i, v) }

m := map[string]int{"go": 30}
for k, v := range m { fmt.Println(k, v) }

str := "Hello世界"
for i, r := range str { fmt.Println(i, r) }
```

## for range 整数（Go 1.22+）

Go 1.22 起支持整数 range，直接遍历 0 到 n-1：

```go
for i := range 10 { fmt.Println(i) }   // 0-9
```

## 参考存根

```go
// switch 编译：src/cmd/compile/internal/walk/switch.go
// 编译器优化：当 case 为有序常量时，使用二分查找

// switch 编译为 if-else 链的判定条件：
// - case < 4 个
// - case 包含函数调用
// - case 为 string 但编译器判断不可排序

// for range 编译：src/cmd/compile/internal/walk/range.go
// 整数 range 展开（Go 1.22+）
// for i := range n 展开为：
for i := 0; i < n; i++ { /* body */ }

// map range 展开（伪随机）
// for k, v := range m 展开为：
// 1. 随机选择哈希桶起始位置（与 hmap、B 相关）
// 2. 遍历所有 2^B 个桶，跳过空 tophash
// 3. 顺序不确定
```
