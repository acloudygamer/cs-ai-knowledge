# Go 新特性

**Go 每个版本都带来了实用的新特性。Go 1.21 ~ 1.24 是稳定版主力，Go 1.25 ~ 1.26 包含前沿特性（标注 `<latest>`）。**

## Go 1.21 新特性（stable）

### 内置 min/max/clear 函数

**内置函数无需 import，直接比较可排序类型——编译器内联，无函数调用开销。**

```go
fmt.Println(min(1, 2, 3))   // 1
fmt.Println(max(1, 2, 3))   // 3

s := []int{1, 2, 3}
clear(s)  // 元素变零值 []

m := map[string]int{"a": 1, "b": 2}
clear(m)  // 变成空 map
```

### 结构化日志 log/slog

**标准库内置结构化日志，支持 JSON/Text 两种格式，替代第三方日志库。**

```go
logger := slog.New(slog.NewJSONHandler(os.Stdout))
logger.Info("hello", "key", "value", "count", 42)
```

### slices/maps/cmp 包增强

```go
orig := []int{1, 2, 3}
dup := slices.Clone(orig)
slices.Contains([]int{1, 2, 3}, 2)  // true

m1 := map[string]int{"a": 1, "b": 2}
m2 := map[string]int{"b": 3, "c": 4}
maps.Copy(m1, m2)
```

## Go 1.22 新特性（stable）

### for range 循环变量作用域修复

**Go 1.22 修复了经典 bug：每次循环迭代创建新变量，而非共享同一个变量地址。**

```
Go 1.21 之前（bug）：
  for i := 0; i < 3; i++ {
      go func() { fmt.Println(i) }()  // 所有 goroutine 输出 3
  }

Go 1.22（修复）：
  for i := 0; i < 3; i++ {
      go func() { fmt.Println(i) }()  // 输出 0, 1, 2
  }
```

### for range 支持整数

```go
for i := range 10 {
    fmt.Println(i)  // 0, 1, 2, ..., 9
}
```

### math/rand/v2

```go
r := rand.New(rand.NewSource(42))
fmt.Println(r.IntN(100))
fmt.Println(r.Float64())
```

## Go 1.23 新特性（stable）

### iter 包（迭代器）

**iter.Seq 和 iter.Seq2 接口支持自定义迭代器，yield 模式替代闭包。**

```go
seq := iter.Seq[int](func(yield func(int) bool) {
    for _, n := range nums {
        if !yield(n) {
            return
        }
    }
})
for v := range seq {
    fmt.Println(v)
}

seq2 := iter.Seq2[int, string](func(yield func(int, string) bool) {
    for i, v := range nums {
        if !yield(i, fmt.Sprintf("val:%d", v)) {
            return
        }
    }
})
```

### slices.Backward / slices.Chunk

```go
for i := range slices.Backward(nums) {
    fmt.Println(nums[i])  // 5, 4, 3, 2, 1
}

chunks := slices.Chunk(nums, 2)  // [[1, 2], [3, 4], [5, 6], [7]]
```

### unique 包（值内化）

**相同值返回同一 Handle（指针相等），用于节省内存。**

```go
p1 := unique.Make(Point{1, 2})
p2 := unique.Make(Point{1, 2})
fmt.Println(p1 == p2)  // true
```

## Go 1.24 新特性（stable）

### 泛型类型别名

```go
type List[T any] = []T
type MapPair[K, V any] = map[K]V

nums := List[int]{1, 2, 3}
pairs := MapPair[string, int]{"a": 1, "b": 2}
```

### weak 包（弱指针）

**弱指针不阻止 GC，用于构建缓存等内存敏感结构。**

```go
obj := &Node{Value: 42}
ptr := weak.Make(obj)
if v := ptr.Value(); v != nil {
    fmt.Println(v.Value)
}
```

### Swiss Table（map 引擎升级）

**Go 1.24 引入 Swiss Table 实现，CPU 开销降低 2-3%，无需改写代码。**

### runtime.AddCleanup（改进的终结器）

**比 `SetFinalizer` 更灵活：支持每个对象多个 cleanup，支持循环引用。**

```go
obj := &MyResource{}
runtime.AddCleanup(obj, func() {
    fmt.Println("清理资源")
})
```

### os.Root（安全文件系统访问）

```go
root, _ := os.OpenRoot("/tmp/sandbox")
f, _ := root.Open("file.txt")  // 只能访问 /tmp/sandbox/file.txt
```

## Go 1.25 新特性（<latest>）

### Green Tea GC（实验性）

**自适应垃圾回收器，根据负载动态调整，GC 开销减少 10-40%。**

```bash
GOEXPERIMENT=greenteagc go build ./...
```

### Container-Aware GOMAXPROCS

**运行时自动感知 cgroup CPU 限制并调整。**

```go
runtime.SetDefaultGOMAXPROCS(0)  // 自动调整至 cgroup 限制
```

### testing/synctest（GA）

**虚拟化时间，用于测试并发代码。**

```go
synctest.Test(func(b *synctest.B) {
    b.Sleep(time.Hour)  // 立即执行
})
synctest.Wait()
```

## Go 1.26 新特性（<latest>）

### new 支持表达式

**`new` 不再只接受类型，现在可以接受表达式。**

```go
Age: new(yearsSince(born)),  // new 接受表达式
```

### 自引用泛型

**类型参数可以引用它所约束的泛型类型本身。**

```go
type Adder[A Adder[A]] interface {
    Add(A) A
}
```

### Green Tea GC（默认启用）

**Go 1.26 默认启用。**

```bash
GOEXPERIMENT=nogreenteagc go build  # 禁用
```

### crypto/hpke（RFC 9180）

**混合公钥加密，结合传统公钥与后量子算法。**

### errors.AsType（泛型错误解包）

```go
if err := errors.AsType[ValidationError](err); err != nil {
    fmt.Println(err.Field, err.Message)
}
```

### bytes.Buffer.Peek

**查看缓冲区内容而不推进位置。**

```go
head, _ := b.Peek(3)  // "hel"，位置不变
```

## 包变化总结

| 包 | Go 1.21 | Go 1.22 | Go 1.23 | Go 1.24 | Go 1.25 | Go 1.26 |
|----|--------|---------|---------|---------|---------|---------|
| 内置 | `min`, `max`, `clear` | `for range` 整数 | - | - | - | `new` 支持表达式 |
| `slices` | `Clone`, `Contains` | `Concat` | `Chunk`, `Backward` | - | - | - |
| `bytes` | - | - | - | `Lines` | - | `Buffer.Peek` |
| `maps` | `Clone`, `Copy`, `Equal` | - | `Collect`, `Insert` | - | - | - |
| `iter` | - | - | 新增 | 增强 | - | - |
| `weak` | - | - | - | 新增 | - | - |
| `testing` | - | - | - | `B.Loop` | `synctest` GA | - |
| `crypto` | - | - | - | `mlkem`, `hkdf` | - | `hpke` |
