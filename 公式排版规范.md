# 公式排版：Markdown 渲染管线的隐性陷阱

在 GitHub、Notion、Obsidian 等所有基于 CommonMark → HTML → MathJax 管线的编辑器里，LaTeX 公式与 Markdown 语法之间存在三类反复出现的"神仙打架"。本文逐坑推演完整渲染管线，给出全场景安全的工程对策。

## 一、管线速览

所有问题共享同一条数据流：

```
原始 Markdown 文本 → CommonMark 解析器 → HTML 中间态 → MathJax 扫描 $...$ 渲染公式
```

关键事实：CommonMark 规范本身**不识别** `$$` 和 `$` 为语法构件——它既不知道这是公式边界，也不会对其内部做特殊保护。`$$`/`$` 的识别由 GFM 的 math extension 在 inline/block 解析阶段注入。两者表现差异的根因在于 **emphasis（`*`/`_`）等结构级 inline 解析是否进入公式内部**：

- **块级 `$$...$$`**：math extension 将其识别为 math block 节点，内容作为 raw string 存入 AST。emphasis（`*`/`_`）、code span 等结构级 inline 解析**不进入** math block 内部，公式里的 `_` 不会与外部 `_` 参与 emphasis 配对。反斜杠转义等文本级处理仍按 CommonMark 规则发生（`\\` → 字面 `\`）。
- **行内 `$...$`**：math extension 虽将其识别为 math inline 节点，但 emphasis 扫描与 math inline 识别在 inline 解析中竞争。实际表现是 `$...$` 内部的 `_` 会被 emphasis 扫描器拿去与同段其他 `_` 配对消耗，破坏公式源码。

三重陷阱的根因全部指向这个"块级 emphasis 隔离 vs 行内 emphasis 竞争"的分歧。

## 二、坑一：`\text{}` 中的下划线陷阱

### 2.1 问题现场

在 `\text{per_channel}` 这样的公式中，我们需要在变量名里打印一个下划线。LaTeX 原教旨写法是 `\text{per\_channel}`。但在 GitHub 的 MathJax 管线上：

- **块级公式 `$$...$$`**：`\\_` 可用（emphasis 不进入，`\\_` 经反斜杠转义后完整送达 MathJax）。
- **行内公式 `$...$`**：`\\_` 全面溃败（emphasis 扫描进入，`_` 被拿去配对消耗，公式残缺）。

### 2.2 正常通路：块级公式 `$$...$$` 为什么 `\\_` 能活

以 `$$B_{\text{per\\_channel}}$$` 为例，逐级追踪：

**第 1 级：原始输入**

你在 Markdown 源码里敲了两个连续 ASCII 字符 `\` `\`（即写作 `\\`），随后跟一个 `_`。所以 `\\_` 是**三个 ASCII 字符**：反斜杠、反斜杠、下划线。

**第 2 级：CommonMark 反斜杠转义**

CommonMark spec 的反斜杠转义规则规定：反斜杠后跟任意 ASCII 标点字符，则输出该标点的字面量，反斜杠本身被消耗。据此，`\\` 中的第一个 `\` 转义第二个 `\`，输出一个字面反斜杠 `\`；紧跟的 `_` 不参与这次转义序列，原样保留。

解析后输出给 HTML 的中间态：

```
\_
```

关键认知：这是**两个独立 ASCII 字符**（字面反斜杠 + 字面下划线），**不是** LaTeX 的 `\_` 控制序列——后者是 MathJax 在 TeX 层才会识别的语法。

**第 3 级：math block 的 emphasis 隔离**

`$$...$$` 已被 math extension 识别为 math block 节点。emphasis 扫描器不进入该节点内部，因此 `\_` 中的 `_` 不会被外部 `_` 配对消耗，`\_` 完整保留在节点 raw string 中。

**第 4 级：MathJax 接手**

MathJax 从 math block 节点提取 raw string `\_`，在 TeX 层解析：`\` + `_` 构成合法控制序列 `\_`，语义为"在当前字体下打印一个下划线字符"。由于位于 `\text{}` 文本模式下，被正确渲染。

**第 5 级：渲染结果**

✅ 成功！

### 2.3 翻车现场：行内公式 `$...$` 为什么 `\\_` 不能活

**第 1-2 级（原始输入 → CommonMark 转义）**：反斜杠转义与块级一致——`\\` 输出字面 `\`，`_` 原样保留。HTML 中间态同样是 `\_`（字面反斜杠 + 字面下划线）。

**第 3 级（emphasis 扫描——分歧点）**：

行内 `$...$` 没有 emphasis 隔离。CommonMark inline 解析中，emphasis 扫描器将 `_` 视为 emphasis 开/闭候选标记，按 flanking rules 判定是否与同段其他 `_` 配对。关键机制：

- 公式 `B_{\text{per\\_channel}}` 中存在多个 `_`：下标结构的 `_` 和 `\text{per\\_channel}` 内的 `_`。
- 由于 CommonMark 不识别 `$` 为边界，这些 `_` 与同段其他位置的 `_`（可能是另一个行内公式、变量名或普通文本中的下划线）共同参与 emphasis 配对判定。
- 一旦某对 `_` 满足 flanking 条件配对成功，配对区间被包进 `<em>` 标签，参与配对的 `_` 被消耗。后果是公式源码被肢解：`$` 边界可能落在 `<em>` 标签内外，或 `_{\text{...}}` 结构因 `_` 缺失而断裂。

**第 4 级（MathJax 接手——收到脏数据）**：

MathJax 按 `$` 分隔提取公式时，拿到的是被 emphasis 处理破坏过的字符串——`_` 被消耗或 `<em>` 标签插入公式中间。MathJax 无法解析残缺的下标结构或被标签切断的公式，报错或静默失败。

**第 5 级：渲染结果**

❌ 崩溃！

### 2.4 块级 vs 行内对照

| 场景 | `\_` 原生 | `\\_` 绕过 | `-` 替代 | 根因 |
|------|----------|-----------|---------|------|
| `$$` 块级 | ❌ | ✅ | ✅ | emphasis 不进入，`\\_` 经转义后完整送达 MathJax |
| `$` 行内 | ❌ | ❌ | ✅ | emphasis 进入，`_` 被配对消耗，公式残缺 |

| 模板 | 块级 `$$` | 行内 `$` | 全场景 |
|------|----------|---------|--------|
| `\text{per\_channel}` | ❌ | ❌ | ❌ |
| `\text{per\\_channel}` | ✅ | ❌ | ❌ |
| `\text{per channel}`（空格） | ✅ | ✅ | ✅ |
| `\text{per-channel}`（连字符） | ✅ | ✅ | ✅ **首选** |

>注：\text{per\_channel}（单反斜杠）在全场景都会失败。因为 Markdown 的转义会吃掉 \，导致最后传给 MathJax 的只剩一个裸 _。而在 LaTeX 的 \text{} 文本环境中，想打印出真实的下划线字符必须严格使用 \_，直接收到裸 _ 会触发 TeX 的底层语法报错，导致渲染崩溃。

### 2.5 对策

> **在 `\text{}` 中，坚决不用下划线 `_`，统一用连字符 `-` 代替。**

| 方案 | 写法 | 块级 `$$` | 行内 `$` | 推荐度 |
|------|------|----------|---------|--------|
| 连字符替代 | `\text{per-channel}` | ✅ | ✅ | **首选** |
| 空格替代 | `\text{per channel}` | ✅ | ✅ | 可接受 |
| 双反斜杠绕过 | `\text{per\\_channel}` | ✅ | ❌ | **不推荐** |

## 三、坑二：百分号注释陷阱

### 3.1 犯罪现场还原

**触发条件**：在行内公式 `$...$` 中写下 `\%`。

**第 1 级：原始输入**

```markdown
- L1 拦住 90%（$M_1 = 10\%$）
```

你写的是 `\%`——LaTeX 标准写法，在 TeX 中表示一个百分号字面量。

**第 2 级：CommonMark 反斜杠转义——两条路径的分野**

CommonMark 反斜杠转义规则：反斜杠后跟 ASCII 标点，输出该标点字面量，反斜杠消耗。`%` 属于 ASCII 标点，因此 `\%` 会被转义处理。但 `\%` 与 `\\%` 的命运截然不同：

**路径 A：单反斜杠 `\%`（翻车）**

CommonMark 将 `\%` 转义为字面 `%`，反斜杠被消耗。HTML 中间态变成 `$M_1 = 10%$`。MathJax 收到裸 `%`，在 TeX 语法中 `%` 是注释符，从 `%` 到行尾的内容被当作注释吞掉——包括闭合 `$`。公式边界丢失，MathJax 报错。❌ 崩溃。

**路径 B：双重转义 `\\%`（成功）**

CommonMark 将 `\\` 转义为字面 `\`（第一个 `\` 转义第二个 `\`）。紧接着的 `%` 虽是 ASCII 标点，但前面已无反斜杠可转义它，`%` 原样保留。HTML 中间态为 `\%`。

MathJax 收到 `$M_1 = 10\%$`，在 TeX 层 `\%` 是标准的百分号字面量转义，正确渲染。✅ 成功。

> **为什么 `\\_` 在行内容易死，而 `\\%` 却活下来了？**
>
> 关键在于转义后残存的字符是否参与 emphasis 配对：
> - `\\_` 经转义后残存未转义的 `_`，`_` 是 emphasis 字符，会被 emphasis 扫描器拿去与同段其他 `_` 配对消耗，破坏公式。
> - `\\%` 经转义后残存 `%`，`%` 不是 emphasis 字符，不参与任何配对，稳定送达 MathJax。
>
> 两者反斜杠转义机制完全相同，差异只在残存字符的 emphasis 属性。

### 3.2 对策

> 与坑一不同：`\\%` 在全场景（块级 + 行内）都能稳定工作，因为 `%` 不是 emphasis 字符。但本仓库未来优先推荐方案一。

#### 方案一：把百分号请出公式（未来首选）

百分号本质上是"单位"，不需要数学字体的特殊渲染。把它留在 `$` 外面的纯文本世界里：

```markdown
- L1 拦住 90%（$M_1 = 10$%）
- L2 拦住剩下的 80%（$M_2 = 20$%）
- L3 再拦住剩下的 50%（$M_3 = 50$%）
```

✅ 彻底避开语法冲突，符合最佳排版规范（单位和数值分开处理），写法最干净。

#### 方案二：双重转义 `\\%`（同样可靠，第一章实际采用）

用两个反斜杠 `\\%`。CommonMark 将 `\\` 转义为字面 `\`，`%` 原样保留，`\%` 安全送达 MathJax：

```markdown
- L1 拦住 90%（$M_1 = 10\\%$）
- L2 拦住剩下的 80%（$M_2 = 20\\%$）
- L3 再拦住剩下的 50%（$M_3 = 50\\%$）
```

✅ 双重转义在此处逻辑完备，实战稳定。`01-硬件组成.md` 中即采用此写法。

## 四、坑三：全句斜体包裹公式引发的解析器截断

### 4.1 犯罪现场还原

**触发条件**：用 Markdown 原生斜体语法 `*...*` 包裹一段**含有行内公式**的文字。

```markdown
*(注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。)*
```

**第 1 级：原始输入**

你写了 `*`（斜体开始），然后是一段包含 `$N_{\text{channel}}$` 的句子，最后 `*`（斜体结束）。

**第 2 级：CommonMark emphasis 解析**

CommonMark 将外层 `*...*` 识别为 emphasis 区间候选，按 flanking rules 寻找配对的闭合 `*`。由于 CommonMark 不识别 `$` 为公式边界，emphasis 扫描器直接进入 `$...$` 内部，公式中的字符全部参与 emphasis 判定：

- `_`（如下标 `_{\text{...}}` 中的 `_`）：是 emphasis 字符，与同段其他 `_` 参与配对判定，可能被消耗或改变 flanking 上下文。
- `*`（若公式内含乘号 `*`）：是 emphasis 字符，可能与外层 `*` 配对，导致 emphasis 在公式内部提前闭合。
- `\`（反斜杠）：CommonMark 反斜杠转义会消耗 `\` 并吞掉紧跟的 ASCII 标点（如 `%`、`*`、`_`、`{`），破坏 LaTeX 控制序列。`\` 后跟字母的结构（如 `\text`）不受影响，但 `\%`、`\*`、`\_` 等会中招。

最终结果：emphasis 配对区间与公式边界错位——要么 emphasis 在公式内部提前闭合、把公式后半段排除在斜体外，要么配对跨越 `$` 边界把公式源码包进 `<em>` 标签。送交 MathJax 的字符串已被肢解。

**第 3 级：MathJax 接手——收到了垃圾**

公式边界被破坏、`_` 被消耗或 `<em>` 标签插入公式中间、LaTeX 控制序列被反斜杠转义截断——MathJax 无法解析。

**第 4 级：渲染结果**

❌ 公式显示为乱码，斜体范围出错，整段排版混乱。

### 4.2 对策

#### 方案一：改用引用块（最推荐，本仓库首选）

在 GitHub 和很多技术文档中，对于"注记"或"补充说明"，使用引用块 `>` 的视觉效果往往比斜体更好，而且**完全不会干扰公式渲染**。这也是本仓库实际采用的写法：

```markdown
> **注：**$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。
```

✅ 排版优雅 + 公式零风险。

#### 方案二：使用 HTML 标签（备选）

直接用网页原生的 HTML 斜体标签 `<i>` 或 `<em>`。CommonMark 将 `<i>`、`</i>` 识别为 inline HTML 节点，这些节点作为 emphasis flanking 判定的边界，阻断了 `_` 跨标签配对的可能。实测下公式得以原样保留：

```markdown
<i>（注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）</i>
```

✅ 跨平台一致渲染，公式完好无损。

#### 方案三：分段斜体（不推荐）

把公式前后的中文分别做斜体，把公式单独让出来：

```markdown
*（注：* $N_{\text{channel}}$ *由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）*
```

⚠️ 能工作，但看着别扭。

## 五、全场景铁律汇总

三条铁律，按触发频率排列：

> **铁律一**：`\text{}` 中绝不写 `_`。用 `-` 代替。`\text{per-channel}` 是你的免死金牌。
>
> **铁律二**：`%` 尽量写在 `$` 外面（`$M_1 = 10$%` 未来首选）。双重转义 `\\%` 同样可靠（`%` 非 emphasis 字符，第一章沿用），两者任选其一，禁用单反斜杠 `\%`。
>
> **铁律三**：需要斜体包裹含公式的文字时，用 `>` 引用块（本仓库首选）或 `<i>...</i>`，不用 Markdown 原生 `*...*`。

## 六、工具链适配

本仓库的 `tools/formula-spacer.js` 负责自动管理公式与中文之间的**空格**。它不处理上述三类陷阱。

未来可扩展的 lint 检查点：

- 扫描 `\text{}` 内是否包含 `_` 字符
- 扫描行内 `$...$` 内是否包含 `\%`
- 扫描 `*` 斜体范围内是否包含 `$...$`
- 在 CI/提交钩子中自动检测并告警
