# 公式排版：Markdown 渲染管线的隐性陷阱

在 GitHub、Notion、Obsidian 等所有基于 CommonMark → HTML → MathJax 管线的编辑器里，LaTeX 公式与 Markdown 语法之间存在三类反复出现的"神仙打架"。本文逐坑推演完整渲染管线，给出全场景安全的工程对策。

## 一、管线速览

所有问题共享同一条数据流：

```
原始 Markdown 文本 → CommonMark 解析器 → HTML 中间态 → MathJax 扫描 $...$ 渲染公式
```

关键事实：CommonMark 规范本身**不识别** `$$` 和 `$` 为语法构件——它既不知道这是公式边界，也不会对其内部做特殊保护。两者表现差异的根因在于**渲染管线的预处理阶段**：

- **块级 `$$...$$`**：GFM 渲染器在 CommonMark 解析前，先用正则把 `$$...$$` 整段切出来，作为 **raw block（原始块）** 透传给 HTML 中间态。CommonMark 拿到时这段已经是 raw HTML，内联扫描器不再进入，内部字符原封不动。
- **行内 `$...$`**：没有这层预处理保护。CommonMark 把 `$` 当普通字符，对其间的文本照常运行内联扫描器——加粗（`**`）、斜体（`*`）、下划线（`_`）、转义符（`\`）全部参与解析。

三重陷阱的根因全部指向这个"raw 块透传 vs 内联扫描"的分歧，而非 CommonMark 对 `$$`/`$` 有什么"双标"。

## 二、坑一：`\text{}` 中的下划线陷阱

### 2.1 问题现场

在 `\text{per_channel}` 这样的公式中，我们需要在变量名里打印一个下划线。LaTeX 原教旨写法是 `\text{per\_channel}`。但在 GitHub 的 MathJax 管线上：

- **块级公式 `$$...$$`**：`\\_` 可用（raw 块透传，内部 `\\_` 原样送达 MathJax）。
- **行内公式 `$...$`**：`\\_` 全面溃败（CommonMark 内联扫描器介入，`\\` 被吃成单 `\`，残留下划线参与斜体配对或裸露在 math mode 外，渲染崩溃）。

### 2.2 正常通路：块级公式 `$$...$$` 为什么 `\\_` 能活

以 `$$B_{\text{per\\_channel}}$$` 为例，逐级追踪：

**第 1 级：原始输入**

你在 Markdown 源码里敲了两个连续 ASCII 字符 `\` `\`（即写作 `\\`），随后跟一个 `_`。所以 `\\_` 是**三个 ASCII 字符**：反斜杠、反斜杠、下划线。

**第 2 级：CommonMark 解析器**

CommonMark spec 的反斜杠转义规则规定：反斜杠后跟任意 ASCII 标点字符，则输出该标点的字面量，反斜杠本身被消耗。据此，`\\` 中的第一个 `\` 转义第二个 `\`，输出一个字面反斜杠 `\`；紧跟的 `_` 不参与前面的转义序列，原样保留。

解析后输出给 HTML 的中间态：

```
\_
```

关键认知：这是**两个独立 ASCII 字符**（字面反斜杠 + 字面下划线），**不是** LaTeX 的 `\_` 控制序列——后者是 MathJax 在 TeX 层才会识别的语法。

**第 3 级：HTML 中间态**

`\_` 位于 `$$...$$` 内部——"安全区"，原封不动透传。

**第 4 级：MathJax 接手**

MathJax 在 TeX 中解析 `\_`——这是一个合法的控制序列："在当前字体下打印一个下划线字符"。由于位于 `\text{}` 文本模式下，被正确解析。

**第 5 级：渲染结果**

✅ 成功！

### 2.3 翻车现场：行内公式 `$...$` 为什么 `\\_` 不能活

**第 1-2 级（原始输入 → CommonMark）**：与块级一致，`\\_` 被转义为 `\_` 送入 HTML。

**第 3 级（HTML 中间态——分歧点）**：

行内 `$` 不是"安全区"。CommonMark 在这个区域内同时扫描：

- 加粗 `**`
- 斜体 `*`
- 普通下划线 `_`（Markdown 的斜体语法）
- 转义符 `\`

在这个大乱斗中，`_` 不是中性字符——CommonMark 会尝试将它与远处另一个 `_` 配对形成斜体。当解析器在 `$...$` 内部发现 `\_` 时：

- 可能把 `\` 当作转义前面的字符，吞掉了 `\`
- 可能把剩下的 `_` 拿去和公式外另一个 `_` 凑成斜体标记
- 或者 `\` 的字面量被 HTML Entity 二次编码，变成乱码

**第 4 级（MathJax 接手——收到了脏数据）**：

收到的不是干净的 `\_`，而是：
- 裸露的 `_`（`\` 被吞）→ `'_' allowed only in math mode`
- HTML Entity 乱码 → 无法解析
- 残缺序列 → 渲染中断

**第 5 级：渲染结果**

❌ 崩溃！

### 2.4 双重标准总结

| 场景 | `\_` 原生 | `\\_` 绕过 | `-` 替代 | 根因 |
|------|----------|-----------|---------|------|
| `$$` 块级 | ❌ | ✅ | ✅ | `$$` 视为安全区 |
| `$` 行内 | ❌ | ❌ | ✅ | `$` 不是安全区，`_` 参与斜体解析 |

| 模板 | 块级 `$$` | 行内 `$` | 全场景 |
|------|----------|---------|--------|
| `\text{per\_channel}` | ❌ | ❌ | ❌ |
| `\text{per\\_channel}` | ✅ | ❌ | ❌ |
| `\text{per channel}`（空格） | ✅ | ✅ | ✅ |
| `\text{per-channel}`（连字符） | ✅ | ✅ | ✅ **首选** |

### 2.5 对策

> **在 `\text{}` 中，坚决不用下划线 `_`，统一用连字符 `-` 代替。**

| 方案 | 写法 | 块级 `$$` | 行内 `$` | 推荐度 |
|------|------|----------|---------|--------|
| 连字符替代 | `\text{per-channel}` | ✅ | ✅ | **首选** |
| 空格替代 | `\text{per channel}` | ✅ | ✅ | 可接受 |
| 双反斜杠绕过 | `\text{per\\_channel}` | ✅ | ❌ | **不推荐** |

## 三、坑二：百分号注释陷阱

### 3.1 犯罪现场还原

**触发条件**：在行内公式 `$...$` 中写下 `\%`。

**第 1 级：原始输入**

```markdown
- L1 拦住 90%（$M_1 = 10\%$）
```

你写的是 `\%`——LaTeX 标准写法，在 TeX 中表示一个百分号字面量。

**第 2 级：CommonMark 解析器——两条路径的分野**

这里出现了两种写法，命运截然不同：

**路径 A：单反斜杠 `\%`（翻车）**

CommonMark 看到 `\%`，自说自话："哦，你想打一个普通的百分号，我帮你把反斜杠去掉吧！"

将代码改成了 `$M_1 = 10%$`。MathJax 拿到后看到裸 `%`，判定为 LaTeX 注释符，把后面的闭合 `$` 当成注释吞掉。❌ 崩溃。

**路径 B：双重转义 `\\%`（成功）**

CommonMark 看到 `\\`，执行规则："吃掉一个，吐出一个字面的 `\`"。接着它看到了 `%`——关键区别来了：**在 Markdown 语法中，`%` 不是任何格式控制符**（它不像 `_` 或 `*` 那样代表斜体/加粗）。所以 Markdown 对 `%` 毫无兴趣，直接原样放行。

输出给 HTML 的中间态：`\%`

MathJax 接手：看到 `$M_1 = 10\%$`。在标准 LaTeX 语法中，`\%` 正好就是最标准的百分号转义写法。

**最终结果**：✅ 完美渲染！

> **为什么同样的双重转义，`\\_` 在行内容易死，而 `\\%` 却活下来了？**
>
> 这暴露了 Markdown"欺软怕硬"的底层逻辑：`_` 是 Markdown 的斜体语法，CommonMark 看到它就"兴奋"，总想看看前后有没有其他 `_` 配对，在行内公式的混乱环境里容易乱点鸳鸯谱。而 `%` 对 Markdown 来说就是一个无意义的普通字符，它只乖乖处理前面的 `\\`，然后把纯净的 `\%` 完整无缺地交给 MathJax。

### 3.2 对策

> 与坑一不同：`\\%` 实际上在全场景（块级 + 行内）都能稳定工作，因为 `%` 不是 Markdown 格式控制符。但本仓库未来优先推荐方案一。

#### 方案一：把百分号请出公式（未来首选）

百分号本质上是"单位"，不需要数学字体的特殊渲染。把它留在 `$` 外面的纯文本世界里：

```markdown
- L1 拦住 90%（$M_1 = 10$%）
- L2 拦住剩下的 80%（$M_2 = 20$%）
- L3 再拦住剩下的 50%（$M_3 = 50$%）
```

✅ 彻底避开语法冲突，符合最佳排版规范（单位和数值分开处理），写法最干净。

#### 方案二：双重转义 `\\%`（同样可靠，第一章实际采用）

用两个反斜杠 `\\%`。Markdown 吃掉一个 `\` 后，`%` 对 Markdown 毫无意义直接放行，`\%` 安全送达 MathJax：

```markdown
- L1 拦住 90%（$M_1 = 10\\%$）
- L2 拦住剩下的 80%（$M_2 = 20\\%$）
- L3 再拦住剩下的 50%（$M_3 = 50\\%$）
```

✅ 双重转义在此处逻辑完备，实战稳定。`01-硬件组成.md` 中即采用此写法。

## 四、坑三：全句斜体包裹公式引发的解析器截断

### 4.1 犯罪现场还原

**触发条件**：用 Markdown 原生斜体语法 `*...*` 包裹一段**含有行内公式**的文字。

```markdown
*(注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。)*
```

**第 1 级：原始输入**

你写了 `*`（斜体开始），然后是一段包含 `$N_{\text{channel}}$` 的句子，最后 `*`（斜体结束）。

**第 2 级：CommonMark 解析器——大乱斗爆发**

CommonMark 在寻找 `*` 的闭合点时极度敏感。公式里的每一个字符都在被同时扫描：

- `_` 字符——即使它在合法的 `\text{}` 内部，或者作为表示下标的 `_{\text{...}}` 结构的一部分——被 CommonMark 误判为"又一个斜体标记开始"
- `$` 字符——CommonMark 分不清它是数学模式起止符还是普通美元符号
- `\` 字符——转义语义在 Markdown 和 LaTeX 之间互相干扰

最终结果：CommonMark 在这个句子里**提前截断**了斜体，或者把公式肢解后再拼起来，导致送给 MathJax 的代码已经面目全非。

**第 3 级：MathJax 接手——收到了垃圾**

公式边界被破坏、`_` 裸露在非预期位置、控制序列被截断——MathJax 无法解析。

**第 4 级：渲染结果**

❌ 公式显示为乱码，斜体范围出错，整段排版混乱。

### 4.2 对策

#### 方案一：改用引用块（最推荐，本仓库首选）

在 GitHub 和很多技术文档中，对于"注记"或"补充说明"，使用引用块 `>` 的视觉效果往往比斜体更好，而且**完全不会干扰公式渲染**。这也是本仓库实际采用的写法：

```markdown
> **注：**$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。
```

✅ 排版优雅 + 公式零风险。

#### 方案二：使用 HTML 标签（备选）

直接用网页原生的 HTML 斜体标签 `<i>` 或 `<em>`。Markdown 解析器对 HTML 标签内部的 LaTeX 公式通常"手下留情"：

```markdown
<i>（注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）</i>
```

✅ 跨平台一致渲染，公式完好无损。

#### 方案三：分段斜体（不推荐）

把公式前后的中文分别做斜体，把公式单独让出来：

```markdown
*（注：* $N_{\text{channel}}$ *由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）*
```

⚠️ 能工作，但看着别扭。

✅ 排版优雅 + 公式零风险。

## 五、全场景铁律汇总

三条铁律，按触发频率排列：

> **铁律一**：`\text{}` 中绝不写 `_`。用 `-` 代替。`\text{per-channel}` 是你的免死金牌。
>
> **铁律二**：`%` 尽量写在 `$` 外面（`$M_1 = 10$%` 未来首选）。双重转义 `\\%` 同样可靠（`%` 非 Markdown 控制符，第一章沿用），两者任选其一，禁用单反斜杠 `\%`。
>
> **铁律三**：需要斜体包裹含公式的文字时，用 `>` 引用块（本仓库首选）或 `<i>...</i>`，不用 Markdown 原生 `*...*`。


## 六、工具链适配

本仓库的 `tools/formula-spacer.js` 负责自动管理公式与中文之间的**空格**。它不处理上述三类陷阱。

未来可扩展的 lint 检查点：

- 扫描 `\text{}` 内是否包含 `_` 字符
- 扫描行内 `$...$` 内是否包含 `\%`
- 扫描 `*` 斜体范围内是否包含 `$...$`
- 在 CI/提交钩子中自动检测并告警

