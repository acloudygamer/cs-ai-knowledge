# 公式排版：Markdown 渲染管线的隐性陷阱

GitHub、Notion、Obsidian 等编辑器渲染公式时，LaTeX 公式与 Markdown 语法之间存在三类反复出现的"神仙打架"。本文的机制描述**全部以 GitHub markdown API（`POST /markdown`，mode=gfm）实测输出为准**，不依赖源码推断。所有结论附实测案例编号，可复现。

## 一、完整管线（实测确认）

### 1.1 渲染管线

GitHub 网页端 markdown 渲染的服务端管线（经 markdown API 实测确认）：

```
Markdown 文本
  → 服务端解析器（CommonMark + GFM 扩展，含 math extension）
     在段落等含内联内容的 block 节点上做 inline 解析：
       - emphasis delimiter 候选收集（*, _ 等登记，不立即配对）
       - math 识别（$...$、$$...$$ 识别为 math 节点）
       - 反斜杠转义（\ 后跟 ASCII 标点 → 字面标点，\ 消耗）
       上述处理在同一 inline 解析遍中交织，emphasis 配对与 math 识别存在竞争
     全部 inline 扫完 → process_emphasis 对 delimiter 候选按 flanking rules 配对
  → HTML 中间态：math 节点渲染为
     <math-renderer class="js-inline-math" style="display: inline-block"
                    data-run-id="<hash>">raw string</math-renderer>
  → 客户端浏览器加载 MathJax v3，渲染 <math-renderer> 标签内的 raw string
```

关键事实（实测确认）：

1. **HTML 输出标签是 `<math-renderer class="js-inline-math">`**，不是开源 comrak 的 `<span data-math-style>`。块级 `$$` 和行内 `$` 都用同一个 `js-inline-math` class，区别在 raw string 是否含 `$$` 分隔符（MathJax 在客户端区分 inline/display）。

2. **math raw string 经过反斜杠转义**。实测 CASE3：源码 `\text{per\_channel}` → raw string `\text{per_channel}`（`\_`→`_`，反斜杠消耗）。实测 CASE3b：源码 `\text{per\\_channel}` → raw string `\text{per\_channel}`（`\\_`→`\_`，一个反斜杠保留）。实测 CASE7：源码 `10\\%` → raw string `10\%`（`\\%`→`\%`）。

3. **emphasis 进入所有 math 内部**——这是与常见误解（"scanner 跳过 math 内部，emphasis 看不到 `*`/`_`"）的关键分歧。实测 CASE11/12 证明块级 `$$` 内部的 `_{x}` 也会被 emphasis 配对消耗，math 识别失败。issue [#1575](https://github.com/github/markup/issues/1575)/[#1709](https://github.com/github/markup/issues/1709) 在 2026 年实测仍未修复。

4. **是否被 emphasis 破坏取决于 flanking rules**：
   - 单个 `_` 无法配对，保留（实测 CASE1 `$x_1 + y_2$`、CASE3 `$$..._{...}$$` 单下标成功）。
   - 两个满足 flanking 的 `_` 配对消耗（实测 CASE2 `$V^{a}_{x} V^{a}_{x}$`、CASE11/12 块级多下标崩溃）。`_{x}` 形式（`_` 前后是标点 `}` `{`）满足 left/right-flanking，配对成功。
   - `*` 前后是空格不满足 flanking，不配对（实测 CASE13 `$A * B * C$` 成功）。

5. **外层 `*...*` emphasis 配对优先于内部 math 识别**。实测 CASE9 `*note $x_1$ end*` → `<em>note $x_1$ end</em>`，`$x_1$` 作为字面文本，math 识别失败。

### 1.2 块级 `$$` vs 行内 `$` 的实测差异

| 维度 | 块级 `$$...$$` | 行内 `$...$` |
|------|---------------|-------------|
| emphasis 是否进入内部 | **是**（CASE11/12 实测崩溃） | **是**（CASE2 实测崩溃） |
| 反斜杠转义是否作用于 raw string | 是（CASE3/3b 实测） | 是（CASE4/6/7 实测） |
| HTML 标签 | `<math-renderer class="js-inline-math">` | 同左 |
| raw string 内分隔符 | 保留 `$$`（如 `$$...$$`） | 保留 `$`（如 `$...$`） |

**结论：块级和行内机制一致**，emphasis 都进入、反斜杠转义都发生。块级在多下标场景同样崩溃（CASE11/12），不存在"块级 emphasis 隔离"的保护。常见误解（包括部分 AI 的描述）声称块级 `$$` 在 block 解析阶段就被整体切走、emphasis 不进入——**实测否定此说法**。

## 二、坑一：`\text{}` 中的下划线陷阱

### 2.1 问题现场

在 `\text{per_channel}` 这样的公式中，需要在变量名里打印下划线。在 GitHub 网页端：

- **块级 `$$...$$` 和行内 `$...$` 都有风险**——实测 CASE11/12 证明块级多下标也崩溃。
- 风险源于两层机制叠加：反斜杠转义 + emphasis 配对。

### 2.2 实测案例与机制

**CASE1 `$x_1 + y_2$`（成功）**：
```
raw string = x_1 + y_2
```
`x_1` 的 `_` 前是字母 `x` 后是数字 `1`，`y_2` 同理。按 CommonMark flanking rules，`_` 后跟数字（非标点非空白）是 left-flanking，前跟字母（非标点非空白）不是 right-flanking。两个 `_` 都是 left-flanking only，无 right-flanking closer，无法配对。`_` 保留，math 识别成功，MathJax 正常渲染下标。

**CASE2 `$V^{a}_{x} V^{a}_{x}$`（崩溃）**：
```
API 输出 = $V^{a}<em>{x} V^{a}</em>{x}$
```
`_{x}` 的 `_` 前是 `}`（标点）后是 `{`（标点），既 left-flanking 又 right-flanking，满足配对条件。两个 `_{x}` 的 `_` 配对，区间 `{x} V^{a}` 包进 `<em>`，两个 `_` 消耗。`$...$` 边界落在 `<em>` 内外，math 识别失败，`$` 作为字面文本。MathJax 无 math 节点可渲染。

**CASE3 `$$B_{\text{per\_channel}}$$`（MathJax 层崩溃）**：
```
raw string = B_{\text{per_channel}}
```
反斜杠转义把 `\_` 变成 `_`（反斜杠消耗）。公式内只有一个 `_{`，无法配对，`_` 保留。math 识别成功，raw string 送达 MathJax。但 MathJax 在 `\text{}` 文本模式下收到裸 `_`——`\text{}` 不支持下标命令，报错或渲染异常。

**CASE3b `$$B_{\text{per\\_channel}}$$`（成功）**：
```
raw string = B_{\text{per\_channel}}
```
反斜杠转义把 `\\_` 变成 `\_`（`\\`→字面 `\`，`_` 原样）。`\_` 中的 `_` 前是字面 `\`，emphasis 扫描时 `\_` 被当作反斜杠转义的字面 `_`，不参与配对。math 识别成功，raw string 送达 MathJax。MathJax 收到 `\text{per\_channel}`，`\_` 是合法的字面下划线转义，正确渲染。

**CASE11 `$$V^{a}_{x} V^{a}_{x}$$`（崩溃，块级）**：
```
API 输出 = $$V^{a}<em>{x} V^{a}</em>{x}$$
```
与 CASE2 同机制——块级 `$$` 的 emphasis 也进入内部，两个 `_{x}` 配对消耗，math 识别失败。**推翻"块级 emphasis 隔离"的说法**。

### 2.3 机制总结

坑一的破坏来自两层：

1. **反斜杠转义层**：`\_`→`_`（单反斜杠），`\\_`→`\_`（双反斜杠）。单反斜杠让 MathJax 在 `\text{}` 里收到裸 `_` 而崩溃。
2. **emphasis 配对层**：`_` 按 flanking rules 配对。`_{x}` 形式（标点相邻）满足 flanking，多个此类 `_` 配对消耗，math 识别失败。

两层独立但叠加：单反斜杠 `\_` 避免了 emphasis 配对（`\_` 是转义字面），但 MathJax 层崩溃；双反斜杠 `\\_` 既避免 emphasis 配对又让 MathJax 收到合法 `\_`，成功。但 `\\_` 写法丑陋且依赖反斜杠转义细节，不推荐。

### 2.4 对照表

| 写法 | 块级 `$$` | 行内 `$` | 全场景 | 说明 |
|------|----------|---------|--------|------|
| `\text{per\_channel}` | ❌ MathJax 崩溃 | ❌ MathJax 崩溃 | ❌ | 反斜杠转义→裸 `_`，`\text{}` 不支持下标 |
| `\text{per\\_channel}` | ✅ 实测（CASE3b） | ⚠️ 理论可行 | ⚠️ | `\\_`→`\_`，依赖转义细节 |
| `\text{per channel}`（空格） | ✅ | ✅ | ✅ | 无 `_`，无风险 |
| `\text{per-channel}`（连字符） | ✅ | ✅ | ✅ **首选** | 无 `_`，无风险，语义清晰 |

注：`_{x}` 形式的下标（不在 `\text{}` 里）用 `\_` 转义可避免 emphasis 配对（issue #1575 的 workaround），但 `\text{}` 里的 `_` 即便转义也因 MathJax 文本模式限制崩溃。

### 2.5 对策

> **在 `\text{}` 中，坚决不用下划线 `_`，统一用连字符 `-` 代替。**

| 方案 | 写法 | 块级 `$$` | 行内 `$` | 推荐度 |
|------|------|----------|---------|--------|
| 连字符替代 | `\text{per-channel}` | ✅ | ✅ | **首选** |
| 空格替代 | `\text{per channel}` | ✅ | ✅ | 可接受 |
| 双反斜杠绕过 | `\text{per\\_channel}` | ✅ | ⚠️ | **不推荐** |

## 三、坑二：百分号注释陷阱

### 3.1 实测案例与机制

**CASE6 `$M = 10\%$`（崩溃）**：
```
raw string = M = 10%
```
反斜杠转义把 `\%` 变成 `%`（反斜杠消耗）。math 识别成功，raw string 送达 MathJax。但 TeX 语法中 `%` 是注释符，从 `%` 到行尾被当注释吞掉——包括闭合 `$`。公式边界丢失，MathJax 报错。

**CASE7 `$M = 10\\%$`（成功）**：
```
raw string = M = 10\%
```
反斜杠转义把 `\\%` 变成 `\%`（`\\`→字面 `\`，`%` 原样）。raw string `M = 10\%` 送达 MathJax，`\%` 是标准的百分号字面量转义，正确渲染。

**CASE8 `$M = 10$%`（成功，百分号在公式外）**：
```
API 输出 = <math-renderer>$M = 10$</math-renderer>%
```
`$M = 10$` 识别为 math 节点，`%` 在 `$` 外作为普通字面文本。彻底避开公式内的反斜杠转义与 TeX 注释符问题。

### 3.2 机制总结

坑二的破坏来自反斜杠转义与 TeX 注释符的冲突：
- 单反斜杠 `\%`：转义后变裸 `%`，MathJax 视为 TeX 注释符吞掉闭合 `$`。
- 双反斜杠 `\\%`：转义后变 `\%`，MathJax 视为字面百分号，成功。

`%` 不是 emphasis 字符，不参与 emphasis 配对，因此坑二没有坑一那样的 emphasis 层风险，`\\%` 在块级和行内都稳定。

### 3.3 对策

> `\\%` 在全场景（块级 + 行内）实测稳定。但本仓库未来优先推荐方案一。

#### 方案一：把百分号请出公式（未来首选）

百分号本质上是"单位"，不需要数学字体的特殊渲染。把它留在 `$` 外面的纯文本世界里：

```markdown
- L1 拦住 90%（$M_1 = 10$%）
- L2 拦住剩下的 80%（$M_2 = 20$%）
- L3 再拦住剩下的 50%（$M_3 = 50$%）
```

✅ 彻底避开语法冲突，符合最佳排版规范（单位和数值分开处理），写法最干净。

#### 方案二：双重转义 `\\%`（同样可靠，第一章实际采用）

```markdown
- L1 拦住 90%（$M_1 = 10\\%$）
- L2 拦住剩下的 80%（$M_2 = 20\\%$）
- L3 再拦住剩下的 50%（$M_3 = 50\\%$）
```

✅ `01-硬件组成.md` 中即采用此写法，实测稳定。

## 四、坑三：全句斜体包裹公式引发的解析器截断

### 4.1 实测案例与机制

**CASE9 `*note $x_1$ end*`（math 识别失败）**：
```
API 输出 = <em>note $x_1$ end</em>
```
外层 `*...*` 被识别为 emphasis 区间，按 flanking rules 配对成功，整段包进 `<em>`。**配对过程中 `$x_1$` 没有被识别为 math 节点**——emphasis 配对优先于 math 识别，`$x_1$` 作为字面文本留在 `<em>` 内。MathJax 无 math 节点可渲染，公式显示为字面 `$x_1$`。

### 4.2 机制总结

坑三的破坏来自 emphasis 配对与 math 识别的竞争：
- 外层 `*...*` 满足 flanking，配对成功，整段包进 `<em>`。
- emphasis 配对在 math 识别之前或同时发生，`$...$` 内部字符被当作普通文本参与 emphasis 上下文，math 识别未能切出 math 节点。
- 结果：`$...$` 作为字面文本，MathJax 无 math 节点渲染。

注意：即使 `$x_1$` 中的单个 `_` 不满足配对（如 CASE9），外层 `*...*` 的配对本身就足以让 math 识别失败。这是 emphasis 跨 `$` 边界的破坏，不是 emphasis 进入 math 内部。

### 4.3 对策

#### 方案一：改用引用块（最推荐，本仓库首选）

对于"注记"或"补充说明"，使用引用块 `>` 的视觉效果往往比斜体更好，而且**完全不会干扰公式渲染**——`>` 是 block 级语法，在 block 解析阶段就切走，内部段落走正常 inline 解析，外层没有 emphasis 区间包裹公式。这也是本仓库实际采用的写法：

```markdown
> **注：**$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。
```

✅ 排版优雅 + 公式零风险。

#### 方案二：使用 HTML 标签（备选）

直接用 HTML 斜体标签 `<i>` 或 `<em>`。CommonMark 将 `<i>`、`</i>` 识别为 inline HTML 节点，这些节点作为 emphasis flanking 判定的边界，阻断了 `*` 跨标签配对的可能。实测下公式得以原样保留：

```markdown
<i>（注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）</i>
```

✅ 跨平台一致渲染，公式完好无损。

#### 方案三：分段斜体（不推荐）

把公式前后的中文分别做斜体，把公式单独让出来：

```markdown
*（注：* $N_{\text{channel}}$ *由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）*
```

⚠️ 能工作，但看着别扭。

## 五、全场景铁律汇总

三条铁律，按触发频率排列：

> **铁律一**：`\text{}` 中绝不写 `_`。用 `-` 代替。`\text{per-channel}` 是你的免死金牌。
>
> **铁律二**：`%` 尽量写在 `$` 外面（`$M_1 = 10$%` 未来首选）。双重转义 `\\%` 同样可靠（第一章沿用），禁用单反斜杠 `\%`。
>
> **铁律三**：需要斜体包裹含公式的文字时，用 `>` 引用块（本仓库首选）或 `<i>...</i>`，不用 Markdown 原生 `*...*`。

## 六、工具链适配

本仓库的 `tools/formula-spacer.js` 负责自动管理公式与中文之间的**空格**。它不处理上述三类陷阱。

未来可扩展的 lint 检查点：

- 扫描 `\text{}` 内是否包含 `_` 字符
- 扫描行内 `$...$` 内是否包含 `\%`
- 扫描 `*` 斜体范围内是否包含 `$...$`
- 在 CI/提交钩子中自动检测并告警

## 七、实测证据（GitHub markdown API）

所有机制描述以 `POST https://api.github.com/markdown`（mode=gfm）实测输出为准。以下是关键案例的输入与 API 返回 HTML：

| CASE | 输入 | API 输出（关键部分） | 结论 |
|------|------|---------------------|------|
| 1 | `$x_1 + y_2$` | `<math-renderer>$x_1 + y_2$</math-renderer>` | 单 `_` 不配对，成功 |
| 2 | `$V^{a}_{x} V^{a}_{x}$` | `$V^{a}<em>{x} V^{a}</em>{x}$` | 两个 `_{x}` 配对，math 识别失败 |
| 3 | `$$B_{\text{per\_channel}}$$` | raw=`B_{\text{per_channel}}` | `\_`→`_`，MathJax `\text{}` 崩溃 |
| 3b | `$$B_{\text{per\\_channel}}$$` | raw=`B_{\text{per\_channel}}` | `\\_`→`\_`，成功 |
| 6 | `$M = 10\%$` | raw=`M = 10%` | `\%`→`%`，MathJax 注释符崩溃 |
| 7 | `$M = 10\\%$` | raw=`M = 10\%` | `\\%`→`\%`，成功 |
| 8 | `$M = 10$%` | `<math-renderer>$M = 10$</math-renderer>%` | 百分号在公式外，成功 |
| 9 | `*note $x_1$ end*` | `<em>note $x_1$ end</em>` | `$x_1$` 字面，math 识别失败 |
| 11 | `$$V^{a}_{x} V^{a}_{x}$$` | `$$V^{a}<em>{x}...</em>{x}$$` | **块级也崩溃**，emphasis 进入 |
| 12 | `$$\mathbf{x}_{i,j}..._{i,j}$$` | `$$\mathbf{x}<em>{i,j}...</em>{i,j}$$` | 块级多下标崩溃 |
| 13 | `$A * B * C$` | `<math-renderer>$A * B * C$</math-renderer>` | `*` 前后空格不配对，成功 |

关键结论：

- **emphasis 进入所有 math 内部**（块级和行内），`_` 按 flanking rules 配对消耗。常见误解"块级 emphasis 隔离""scanner 跳过 math 内部"被实测否定。
- **math raw string 经过反斜杠转义**：`\X`→`X`（单反斜杠消耗），`\\X`→`\X`（双反斜杠保留一个）。
- **HTML 标签是 `<math-renderer class="js-inline-math">`**，非开源 comrak 的 `<span data-math-style>`。
- issue [#1575](https://github.com/github/markup/issues/1575)/[#1709](https://github.com/github/markup/issues/1709) 在 2026 年实测仍未修复（CASE2/11/12 复现崩溃）。

## 八、查证来源

- **GitHub markdown API `POST /markdown`**：本文所有实测案例的权威来源，mode=gfm 与网页端渲染管线一致。
- [GitHub Blog: Math support in Markdown](https://github.blog/2022-05-19-math-support-in-markdown/)：确认 GitHub 用 MathJax 渲染。
- [GitHub Docs: Writing mathematical expressions](https://docs.github.com/en/get-started/writing-on-github/working-with-advanced-formatting/writing-mathematical-expressions)：确认 MathJax 为渲染引擎，`$...$`/`$$...$$`/`` $`...`$ ``/` ```math ` 为分隔符语法。
- GitHub issue [#1575](https://github.com/github/markup/issues/1575)：行内公式多个下标被破坏的实测记录，2024-08-24 关闭但 2026 年实测仍复现。
- GitHub issue [#1709](https://github.com/github/markup/issues/1709)：行内/块级公式不可靠的实测记录，`\_` 转义 workaround。
- `github/markup` 仓库 `lib/github/markup/markdown.rb`：确认 GitHub 用 `commonmarker` 2.x，启用 `:math_dollars`/`:math_code` 扩展（但 HTML 输出与开源 comrak 不同，GitHub 有自定义渲染层）。
- `kivikakk/comrak` 仓库：开源 Rust 实现，输出 `<span data-math-style>`，与 GitHub 实际 `<math-renderer>` 不同——**源码推断不可靠，以 API 实测为准**。
