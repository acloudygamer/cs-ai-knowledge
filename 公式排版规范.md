# 公式排版：Markdown 渲染管线的隐性陷阱

GitHub、Notion、Obsidian 等编辑器渲染公式时，LaTeX 公式与 Markdown 语法之间存在三类反复出现的"神仙打架"。本文先补全完整管线，再据此逐坑推演根因，最后给出全场景安全的工程对策。

## 一、完整管线

### 1.1 两种公式识别路径

标准 CommonMark **不识别** `$`/`$$` 为语法构件。要渲染公式，必须由"解析阶段识别"或"客户端扫描"之一介入：

| 路径 | 谁识别 `$` | 公式何时结构化 | 典型实现 |
|------|-----------|--------------|---------|
| A 解析阶段识别 | Markdown 解析器的 math extension | 解析时，生成 math 节点存入 AST | GFM、Pandoc `tex_math_dollars`、remark-math |
| B 客户端扫描 | 浏览器端 JS | 解析后，JS 扫描 HTML 文本节点 | 传统 MathJax CDN 配置、裸 CommonMark + KaTeX auto-render |

**本规范讨论的三类坑均针对路径 A**（GFM 即属此路径）。路径 B 下 emphasis 已在解析阶段无约束地扫过 `$...$` 内部，公式源码更早被破坏，坑只会更严重，不在本文讨论范围。

### 1.2 路径 A 的完整数据流

```
Markdown 文本
  → [解析阶段]
     ├─ block 解析：math extension 识别 $$...$$ 为 math block 节点（raw string 存入 AST）
     ├─ inline 解析：对非 math 节点的段落文本，并行运行
     │    ├─ emphasis 扫描（*, _ 按 flanking rules 配对）
     │    ├─ code span 扫描（`...`）
     │    └─ math inline 扫描（$...$ 识别为 math inline 节点）
     └─ 反斜杠转义：\ 后跟 ASCII 标点 → 输出该标点字面量，\ 消耗
  → HTML 中间态（math 节点渲染为带 class 的 <span>/<div>，内容为转义后的 raw string）
  → MathJax/KaTeX 渲染器：从 math 节点提取 raw string，在 TeX 层解析渲染
```

关键时序：

1. **block 优先于 inline**：`$$...$$` 在 block 解析阶段就被切走，inline 阶段的 emphasis 扫描器**根本看不到**其内部字符。
2. **inline 阶段 emphasis 与 math inline 竞争**：`$...$` 在 inline 阶段才识别。emphasis 扫描与 math inline 识别在同一遍 inline 解析中竞争——若 emphasis 先把某段 `_` 配对消耗，math inline 识别时这段 `_` 已不在原始位置。
3. **反斜杠转义贯穿全阶段**：无论 block 还是 inline，`\` 后跟 ASCII 标点都会触发转义，`\` 被消耗、标点变字面量。

### 1.3 块级 vs 行内的根本分歧

| 维度 | 块级 `$$...$$` | 行内 `$...$` |
|------|---------------|-------------|
| 识别阶段 | block 解析 | inline 解析 |
| emphasis 是否进入内部 | **否**（block 阶段已切走） | **是**（inline 阶段 emphasis 与 math inline 竞争） |
| 内部 `_` 是否参与外部配对 | 否 | 是 |
| 反斜杠转义是否发生 | 是 | 是 |

三重陷阱的根因全部落在这个分歧上：**行内公式内部的 `_` 会被 emphasis 拿去配对，块级不会。** 反斜杠转义则两级都发生，是独立的第二层机制。

## 二、坑一：`\text{}` 中的下划线陷阱

### 2.1 问题现场

在 `\text{per_channel}` 这样的公式中，我们需要在变量名里打印一个下划线。LaTeX 原教旨写法是 `\text{per\_channel}`。但在 GFM 管线上：

- **块级公式 `$$...$$`**：`\\_` 可用（emphasis 不进入，仅反斜杠转义发生，`\\_` 转义后完整送达 MathJax）。
- **行内公式 `$...$`**：`\\_` 全面溃败（emphasis 进入，转义后残存的 `_` 被配对消耗，公式残缺）。

### 2.2 正常通路：块级公式 `$$...$$` 为什么 `\\_` 能活

以 `$$B_{\text{per\\_channel}}$$` 为例，按管线逐级追踪：

**第 1 级：原始输入**

Markdown 源码里 `\\_` 是**三个 ASCII 字符**：反斜杠、反斜杠、下划线。

**第 2 级：block 解析**

math extension 在 block 阶段识别 `$$...$$`，将 `B_{\text{per\\_channel}}` 作为 raw string 存入 math block 节点。emphasis 扫描器在后续 inline 阶段**不进入**该节点。

**第 3 级：反斜杠转义**

反斜杠转义规则：`\` 后跟 ASCII 标点 → 输出该标点字面量，`\` 消耗。`\\` 中第一个 `\` 转义第二个 `\`，输出字面 `\`；紧跟的 `_` 不参与这次转义，原样保留。math block 节点 raw string 变为 `B_{\text{per\_channel}}`。

关键认知：raw string 里的 `\_` 是**两个独立 ASCII 字符**（字面反斜杠 + 字面下划线），**不是** LaTeX 的 `\_` 控制序列——后者是 MathJax 在 TeX 层才识别的语法。

**第 4 级：MathJax 接手**

MathJax 从 math block 节点提取 raw string `B_{\text{per\_channel}}`，在 TeX 层解析：`\` + `_` 构成合法控制序列 `\_`，语义为"打印下划线字面量"。位于 `\text{}` 文本模式下，正确渲染。

**第 5 级：渲染结果**

✅ 成功！

### 2.3 翻车现场：行内公式 `$...$` 为什么 `\\_` 不能活

**第 1 级：原始输入**

同块级，`\\_` 是三个 ASCII 字符。

**第 2 级：inline 解析——emphasis 与 math inline 竞争**

`$...$` 在 inline 阶段才识别。CommonMark emphasis 扫描器将 `_` 视为 emphasis 开/闭候选，按 flanking rules 判定是否与同段其他 `_` 配对。由于此阶段 math inline 尚未被切走，公式内部的 `_` 与同段其他 `_`（另一行内公式、变量名、普通文本下划线）共同参与配对判定。

关键机制：

- 公式 `B_{\text{per\\_channel}}` 内有多个 `_`：下标结构的 `_{` 和 `\text{per\\_channel}` 内的 `_`。
- 若某对 `_` 满足 flanking 条件配对成功，配对区间包进 `<em>`，参与配对的 `_` 被消耗。后果：`$` 边界可能落在 `<em>` 标签内外，或 `_{\text{...}}` 结构因 `_` 缺失而断裂。

**第 3 级：反斜杠转义**

反斜杠转义仍发生：`\\` → 字面 `\`。但 emphasis 配对可能在转义前或转义过程中已破坏公式结构——转义产生的 `\_` 中的 `_` 同样可能被 emphasis 扫描器纳入配对候选（反斜杠转义消耗的是 `\`，不改变 `_` 的 emphasis 属性）。

**第 4 级：math inline 识别——拿到残缺源码**

math inline 扫描器按 `$` 分隔提取公式时，拿到的是被 emphasis 破坏过的字符串——`_` 被消耗或 `<em>` 标签插入公式中间。math inline 节点的 raw string 已非原始公式源码。

**第 5 级：MathJax 接手——收到脏数据**

MathJax 解析残缺的下标结构或被标签切断的公式，报错或静默失败。

**第 6 级：渲染结果**

❌ 崩溃！

### 2.4 块级 vs 行内对照

| 场景 | `\_` 原生 | `\\_` 绕过 | `-` 替代 | 根因 |
|------|----------|-----------|---------|------|
| `$$` 块级 | ❌ | ✅ | ✅ | emphasis 不进入，`\\_` 经转义后完整送达 MathJax |
| `$` 行内 | ❌ | ❌ | ✅ | emphasis 进入，`_` 被配对消耗，公式残缺 |

| 模板 | 块级 `$$` | 行内 `$` | 全场景 |
|------|----------|---------|--------|
| `\text{per\_channel}` | ❌ | ❌ | ❌ |
| `\text{per\\_channel}` | ✅ | ❌ | ❌ |
| `\text{per channel}`（空格） | ✅ | ✅ | ✅ |
| `\text{per-channel}`（连字符） | ✅ | ✅ | ✅ **首选** |

注：`\text{per\_channel}`（单反斜杠）在全场景失败，因为反斜杠转义把 `\` 消耗、输出裸 `_`，MathJax 在 `\text{}` 文本模式下收到裸 `_` 解析为下标命令，但文本模式不支持下标，崩溃。

### 2.5 对策

> **在 `\text{}` 中，坚决不用下划线 `_`，统一用连字符 `-` 代替。**

| 方案 | 写法 | 块级 `$$` | 行内 `$` | 推荐度 |
|------|------|----------|---------|--------|
| 连字符替代 | `\text{per-channel}` | ✅ | ✅ | **首选** |
| 空格替代 | `\text{per channel}` | ✅ | ✅ | 可接受 |
| 双反斜杠绕过 | `\text{per\\_channel}` | ✅ | ❌ | **不推荐** |

## 三、坑二：百分号注释陷阱

### 3.1 犯罪现场还原

**触发条件**：在行内公式 `$...$` 中写下 `\%`。

**第 1 级：原始输入**

```markdown
- L1 拦住 90%（$M_1 = 10\%$）
```

`\%` 是 LaTeX 标准写法，在 TeX 中表示百分号字面量。

**第 2 级：反斜杠转义——两条路径的分野**

反斜杠转义规则：`\` 后跟 ASCII 标点 → 输出该标点字面量，`\` 消耗。`%` 属 ASCII 标点。`\%` 与 `\\%` 命运截然不同：

**路径 A：单反斜杠 `\%`（翻车）**

反斜杠转义把 `\%` 变成字面 `%`，`\` 消耗。送交 math inline 的 raw string 变成 `M_1 = 10%`。MathJax 在 TeX 层收到裸 `%`——TeX 语法中 `%` 是注释符，从 `%` 到行尾被当注释吞掉，包括闭合 `$`。公式边界丢失，MathJax 报错。❌ 崩溃。

**路径 B：双重转义 `\\%`（成功）**

`\\` 中第一个 `\` 转义第二个 `\`，输出字面 `\`。紧跟的 `%` 前面已无 `\` 可转义它，原样保留。送交 math inline 的 raw string 为 `M_1 = 10\%`。MathJax 在 TeX 层解析 `\%` 为标准百分号字面量转义，正确渲染。✅ 成功。

> **为什么 `\\_` 在行内容易死，而 `\\%` 却活下来了？**
>
> 两者反斜杠转义机制完全相同，差异在转义后残存字符的 emphasis 属性：
> - `\\_` 转义后残存未转义的 `_`。`_` 是 emphasis 字符，会被 emphasis 扫描器拿去与同段其他 `_` 配对消耗，破坏公式（坑一机制）。
> - `\\%` 转义后残存 `%`。`%` 不是 emphasis 字符，不参与任何配对，稳定送达 MathJax。

### 3.2 对策

> 与坑一不同：`\\%` 在全场景（块级 + 行内）都能稳定工作，因为 `%` 不是 emphasis 字符。但本仓库未来优先推荐方案一。

#### 方案一：把百分号请出公式（未来首选）

百分号本质上是"单位"，不需要数学字体的特殊渲染。把它留在 `$` 外面的纯文本世界里：

```markdown
- L1 拦住 90%（$M_1 = 10$%）
- L2 拦住剩下的 80%（$M_2 = 20$%）
- L3 再拦住剩下的 50%（$M_3 = 50$%）
```

✅ 彻底避开语法冲突，符合最佳排版规范（单位和数值分开处理），写法最干净。

#### 方案二：双重转义 `\\%`（同样可靠，第一章实际采用）

用两个反斜杠 `\\%`。反斜杠转义把 `\\` 变字面 `\`，`%` 原样保留，`\%` 安全送达 MathJax：

```markdown
- L1 拦住 90%（$M_1 = 10\\%$）
- L2 拦住剩下的 80%（$M_2 = 20\\%$）
- L3 再拦住剩下的 50%（$M_3 = 50\\%$）
```

✅ 双重转义在此处逻辑完备，实战稳定。`01-硬件组成.md` 中即采用此写法。

## 四、坑三：全句斜体包裹公式引发的解析器截断

### 4.1 犯罪现场还原

**触发条件**：用 Markdown 原生斜体语法 `*...*` 包裹一段**含有行内公式**的文字。

```markdown
*(注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。)*
```

**第 1 级：原始输入**

外层 `*...*` 是 emphasis 区间，内部含行内公式 `$N_{\text{channel}}$`。

**第 2 级：inline 解析——emphasis 扫描进入公式内部**

CommonMark 将外层 `*...*` 识别为 emphasis 区间候选，按 flanking rules 寻找配对闭合 `*`。由于此阶段 math inline 尚未被切走，emphasis 扫描器直接进入 `$...$` 内部，公式中的字符全部参与 emphasis 判定：

- `_`（如下标 `_{\text{...}}` 中的 `_`）：是 emphasis 字符，与同段其他 `_` 参与配对判定，可能被消耗或改变 flanking 上下文。
- `*`（若公式内含乘号 `*`）：是 emphasis 字符，可能与外层 `*` 配对，导致 emphasis 在公式内部提前闭合。
- `\`（反斜杠）：反斜杠转义会消耗 `\` 并吞掉紧跟的 ASCII 标点（如 `%`、`*`、`_`、`{`），破坏 LaTeX 控制序列。`\` 后跟字母的结构（如 `\text`）不受影响，但 `\%`、`\*`、`\_` 等会中招。

**第 3 级：emphasis 配对结果——区间与公式边界错位**

emphasis 配对区间与公式边界错位：要么 emphasis 在公式内部提前闭合、把公式后半段排除在斜体外，要么配对跨越 `$` 边界把公式源码包进 `<em>` 标签。送交 math inline 识别的字符串已被肢解。

**第 4 级：math inline 识别——收到残缺源码**

math inline 扫描器按 `$` 分隔提取公式时，拿到的是被 emphasis 破坏过的字符串——`_` 被消耗或 `<em>` 标签插入公式中间、LaTeX 控制序列被反斜杠转义截断。

**第 5 级：MathJax 接手——收到垃圾**

MathJax 解析残缺的下标结构、被标签切断的公式或被反斜杠转义破坏的控制序列，无法解析。

**第 6 级：渲染结果**

❌ 公式显示为乱码，斜体范围出错，整段排版混乱。

### 4.2 对策

#### 方案一：改用引用块（最推荐，本仓库首选）

对于"注记"或"补充说明"，使用引用块 `>` 的视觉效果往往比斜体更好，而且**完全不会干扰公式渲染**——`>` 是 block 级语法，在 block 解析阶段就切走，内部段落走正常 inline 解析，外层没有 emphasis 区间包裹公式。这也是本仓库实际采用的写法：

```markdown
> **注：**$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。
```

✅ 排版优雅 + 公式零风险。

#### 方案二：使用 HTML 标签（备选）

直接用 HTML 斜体标签 `<i>` 或 `<em>`。CommonMark 将 `<i>`、`</i>` 识别为 inline HTML 节点，这些节点作为 emphasis flanking 判定的边界，阻断了 `_` 跨标签配对的可能。实测下公式得以原样保留：

```markdown
<i>（注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）</i>
```

✅ 跨平台一致渲染，公式完好无损。

#### 方案三：分段斜体（不推荐）

把公式前后的中文分别做斜体，把公式单独让出来：

```markdown
*（注：* $N_{\text{channel}}$ *由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）*
```

⚠️ 能工作，但看着别扭。

## 五、全场景铁律汇总

三条铁律，按触发频率排列：

> **铁律一**：`\text{}` 中绝不写 `_`。用 `-` 代替。`\text{per-channel}` 是你的免死金牌。
>
> **铁律二**：`%` 尽量写在 `$` 外面（`$M_1 = 10$%` 未来首选）。双重转义 `\\%` 同样可靠（`%` 非 emphasis 字符，第一章沿用），两者任选其一，禁用单反斜杠 `\%`。
>
> **铁律三**：需要斜体包裹含公式的文字时，用 `>` 引用块（本仓库首选）或 `<i>...</i>`，不用 Markdown 原生 `*...*`。

## 六、工具链适配

本仓库的 `tools/formula-spacer.js` 负责自动管理公式与中文之间的**空格**。它不处理上述三类陷阱。

未来可扩展的 lint 检查点：

- 扫描 `\text{}` 内是否包含 `_` 字符
- 扫描行内 `$...$` 内是否包含 `\%`
- 扫描 `*` 斜体范围内是否包含 `$...$`
- 在 CI/提交钩子中自动检测并告警
