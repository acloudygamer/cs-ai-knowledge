> **原则**：旧写法能渲染就别动；首选写法只管新写内容。

## 坑一：`\text{}` 中的下划线

**坑**：`\text{per\_channel}`（单反斜杠）全场景失败——Markdown 转义吃掉 `\`，裸 `_` 触发 TeX 语法报错；`\text{per\\_channel}`（双反斜杠）块级可用、行内失败。

**对策**：`\text{}` 中不写 `_`，统一用 `-` → `\text{per-channel}`（全场景安全，首选）；空格替代 `\text{per channel}` 亦可。

## 坑二：行内公式中的百分号

**坑**：`$M_1 = 10\%$` 行内崩溃——单反斜杠 `\%` 被 Markdown 转义消耗掉 `\`，裸 `%` 在 TeX 中是注释符，从 `%` 到行尾（含闭合 `$`）被吞掉，公式边界丢失。

**对策**：
- 首选：`%` 写在公式外 → `$M_1 = 10$%`（单位与数值分离，写法最干净）。
- 备选：双重转义 → `$M_1 = 10\\%$`（`%` 非 emphasis 字符，块级行内均安全；`01-硬件组成.md` 即采用此法）。

## 坑三：`*...*` 斜体包裹含公式的文字

**坑**：`*(注：$N_{\text{channel}}$ ……)*` 翻车——Markdown 不识别 `$` 为公式边界，公式被肢解，渲染乱码、斜体范围错乱。

**对策**：
- 首选：引用块 → `> **注：**$N_{\text{channel}}$ ……`（本仓库实际写法，公式零风险）。
- 备选：HTML 标签 → `<i>（注：$N_{\text{channel}}$ ……）</i>`（标签阻断）。
- 禁用：Markdown 原生 `*...*` 整句包裹含公式的文字。
