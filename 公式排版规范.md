# 公式排版：Markdown 渲染管线的隐性陷阱

GitHub、Notion、Obsidian 等编辑器渲染公式时，LaTeX 公式与 Markdown 语法之间存在三类反复出现的"神仙打架"。本文先补全完整管线，再据此逐坑推演根因，最后给出全场景安全的工程对策。

## 一、完整管线

### 1.1 GitHub 网页端实际使用的解析器

需要先澄清一个常见误解：GitHub 网页端**不使用** `github/cmark-gfm`（公开的 C 仓库，最新 tag `0.29.0.gfm.13`）。该仓库的 `extensions/` 目录只有 autolink、strikethrough、table、tagfilter、tasklist，**没有 math 源码**——grep `math`/`dollar` 全仓库为零。

GitHub 实际使用的是 **`commonmarker` 2.x**（Ruby 绑定），底层是 Rust 的 **`comrak`**。证据：`github/markup` 仓库的 `lib/github/markup/markdown.rb` 中，扩展列表显式启用 `:math_dollars` 和 `:math_code`。`comrak` 是 `github/cmark-gfm` 的 Rust 重写，math 支持在其 `src/parser/inlines.rs` 中。

后续机制描述基于 `comrak` 开源源码（`kivikakk/comrak` 仓库 main 分支）+ GitHub issue 实测记录。`commonmarker` 与 `comrak` 之间可能有 fork 差异，本文不声称 100% 与 GitHub 私有部署一致，只描述可验证的部分。

### 1.2 两种公式识别路径

标准 CommonMark **不识别** `$`/`$$` 为语法构件。要渲染公式，必须由以下路径之一介入：

| 路径 | 谁识别 `$` | 公式何时结构化 | 典型实现 |
|------|-----------|--------------|---------|
| A 解析阶段识别 | Markdown 解析器的 math extension | 解析时，生成 math 节点存入 AST | GFM（comrak `math_dollars`）、Pandoc `tex_math_dollars`、remark-math |
| B 客户端扫描 | 浏览器端 JS | 解析后，JS 扫描 HTML 文本节点 | 传统 MathJax CDN 配置、裸 CommonMark + KaTeX auto-render |

**本规范讨论的三类坑均针对路径 A**（GitHub 网页端属此路径）。路径 B 下 emphasis 在解析阶段无约束地扫过 `$...$` 内部，公式源码更早被破坏，坑只会更严重，不在本文讨论范围。

### 1.3 路径 A 的完整数据流（comrak 实现）

```
Markdown 文本
  → [block 解析阶段]
     生成段落、标题、代码块等 block 节点；此阶段不识别 $。
  → [inline 解析阶段] 对每个含内联内容的 block 节点，单遍逐字符扫描其内容：
     遇 $ → handle_dollars：吃连续 $ 计数 opendollars，
            调 scan_to_closing_dollar 找闭合 $，
            将 opendollars 与闭合之间的内容作为 raw string 存入 math 节点，
            scanner 跳到闭合 $ 之后——内部字符不再被主循环逐字符 dispatch。
     遇 * ' " _ 等 → handle_delim：登记为 emphasis delimiter 候选（不立即配对）。
     遇 \ → handle_backslash：反斜杠转义，\ 后跟 ASCII 标点则输出字面标点、\ 消耗。
     遇其他特殊字符 → 对应处理（code span、autolink 等）。
     全部扫完 → process_emphasis：对登记的 delimiter 候选按 flanking rules 配对。
  → HTML 中间态：math 节点渲染为 <span data-math-style="inline|display">...raw string...</span>
  → 客户端浏览器加载 MathJax，渲染上述 <span>
```

关键事实（来自 comrak 源码）：

1. **`$$` 和 `$` 都在 inline 阶段识别**——没有独立的 math block parser。`opendollars == 2` 时 math 节点标记 `display_math: true`，但仍是 inline 节点，不是 block 节点。源码：`src/parser/inlines.rs:358` `b'$' => Some(self.handle_dollars(...))`。

2. **math 内容原样透传，不做反斜杠转义**——`handle_dollars` 用 `scan_to_closing_dollar` 收集 raw string，`normalize_code`（仅用于 `$...$` 和 `` $`...`$ ``）只处理换行和首尾空格，不处理反斜杠。测试用例 `$1+2\\$$` 期望输出 `<math>1+2\\$</math>`，`\\$` 原样保留——证明反斜杠转义不作用于 math 内容。

3. **emphasis delimiter 候选只从被主循环逐字符 dispatch 的位置收集**——`handle_dollars` 找到闭合 `$` 后直接跳过内部字符，`handle_delim` 看不到 math 节点内部的 `*`/`_`。理论上 emphasis 配对无法消耗 math 内部的 `_`。

4. **块级 `$$` 与行内 `$` 的唯一代码差异**在 `scan_to_closing_dollar`：`opendollarlength == 1`（行内）有额外约束——开头 `$` 后不能跟空格、闭合 `$` 前不能是空格、闭合 `$` 后不能跟数字、遇 `\\` 跳过下一字符（防 `\$` 误判为闭合）。`opendollarlength == 2`（块级）无这些约束。

### 1.4 理论与实测的张力

按上述源码事实，emphasis 不应进入 math 内部，则行内 `$...$` 内的 `_` 应安全。但 GitHub issue [#1575](https://github.com/github/markup/issues/1575)（2022 年开，2024-08-24 关闭）和 [#1709](https://github.com/github/markup/issues/1709) 实测记录显示：行内公式中多个带下标的结构（如 `$V^{a}_{x} V^{a}_{x}$`）确实被破坏，用户推测是 `_` 被当作 emphasis 消耗，workaround 是 `\_` 转义或加空格。

这有三种可能：

- **可能一**：GitHub 私有部署的 commonmarker fork 与开源 comrak 有差异，emphasis 确实进入 math 内部。
- **可能二**：问题不在 emphasis 配对，而在 `scan_to_closing_dollar` 的闭合 `$` 误判——多个 `_` 与 `{` 的组合让 scanner 提前或错位找到闭合 `$`，导致 math 节点 raw string 切错位置。
- **可能三**：旧版 comrak 有此 bug，2024 年已修复，issue 关闭即修复完成。

本文不强行定论哪种。**可观测的结论**是：在 GitHub 网页端，行内 `$...$` 内的下划线确实存在被破坏的风险，块级 `$$...$$` 风险更低（实测报告多集中在行内）。后续三坑的机制描述以 comrak 开源源码为准，对 emphasis 是否进入 math 内部这一点，明确标注为"开源实现理论与 GitHub 实测存在张力"。

## 二、坑一：`\text{}` 中的下划线陷阱

### 2.1 问题现场

在 `\text{per_channel}` 这样的公式中，我们需要在变量名里打印一个下划线。LaTeX 原教旨写法是 `\text{per\_channel}`。在 GitHub 网页端：

- **块级公式 `$$...$$`**：`\\_` 可用，实测渲染正常。
- **行内公式 `$...$`**：`\\_` 风险高，实测有被破坏的报告（见 issue #1575）。

### 2.2 块级 `$$...$$` 为什么 `\\_` 能活

以 `$$B_{\text{per\\_channel}}$$` 为例，按 comrak 源码追踪：

**第 1 级：原始输入**

Markdown 源码里 `\\_` 是三个 ASCII 字符：反斜杠、反斜杠、下划线。

**第 2 级：inline 解析遇到 `$$`**

`handle_dollars` 调 `take_while(b'$')` 得 `opendollars = 2`，调 `scan_to_closing_dollar(2)` 找闭合 `$$`。`opendollarlength == 2` 分支无 `\\` 跳过逻辑，也无空格/数字约束，直接扫描到下一个 `$$` 为止。raw string 为 `B_{\text{per\\_channel}}`——**原样透传，反斜杠未转义**。

**第 3 级：math 节点生成**

`handle_dollars` 创建 `NodeMath { dollar_math: true, display_math: true, literal: "B_{\\text{per\\_channel}}" }`，scanner 跳到闭合 `$$` 之后。内部字符未被 `handle_delim` 看到，emphasis delimiter 候选中不包含此处的 `_`。

**第 4 级：HTML 输出**

math 节点渲染为 `<span data-math-style="display">B_{\text{per\\_channel}}</span>`，raw string 原样进 HTML。

**第 5 级：客户端 MathJax 渲染**

浏览器 MathJax 提取 `<span>` 内容 `B_{\text{per\\_channel}}`，在 TeX 层解析：`\\` 是 LaTeX 转义符，`\\` + `_` 构成控制序列 `\_`，语义为"打印下划线字面量"。位于 `\text{}` 文本模式下，正确渲染。

✅ 成功。

### 2.3 行内 `$...$` 为什么 `\\_` 有风险

**第 1-2 级：inline 解析遇到 `$`**

`handle_dollars` 得 `opendollars = 1`，调 `scan_to_closing_dollar(1)`。此分支有额外约束（见 1.3 关键事实 4）：遇 `\\` 跳过下一字符（防 `\$` 误判）。

**分歧点：`scan_to_closing_dollar(1)` 的 `\\` 跳过逻辑**

源码第 2147 行：`if opendollarlength == 1 && c == b'\\' { self.scanner.pos += 1; continue; }`——遇 `\\` 时跳过其后的字符。对于 `\\_`，第一个 `\\` 触发跳过，把 `_` 跳过。这意味着 `_` 不被当作普通字符扫描，但也不被当作闭合 `$` 检测——它被跳过，仍在 raw string 内。

**理论上**：`_` 被跳过但仍保留在 raw string 中，math 节点 raw string 应完整。emphasis 候选收集中 `handle_delim` 看不到 math 内部字符（`handle_dollars` 已跳过），emphasis 不应消耗此 `_`。

**实测张力**：issue #1575 记录多个行内公式带下标被破坏的案例。可能机制是 GitHub 私有 fork 与开源 comrak 行为不同，或旧版 comrak 的 `scan_to_closing_dollar` 逻辑有 bug 导致闭合 `$` 误判。本文不强行定论，**只记录可观测结论**：行内 `$...$` 内 `\\_` 存在风险，实测有破坏报告，需用更安全的写法。

### 2.4 块级 vs 行内对照

| 场景 | `\_` 原生 | `\\_` 绕过 | `-` 替代 | 实测结论 |
|------|----------|-----------|---------|---------|
| `$$` 块级 | ❌ | ✅ | ✅ | `\\_` 实测稳定 |
| `$` 行内 | ❌ | ⚠️ | ✅ | `\\_` 实测有破坏报告（issue #1575） |

| 模板 | 块级 `$$` | 行内 `$` | 全场景 |
|------|----------|---------|--------|
| `\text{per\_channel}` | ❌ | ❌ | ❌ |
| `\text{per\\_channel}` | ✅ | ⚠️ | ❌ |
| `\text{per channel}`（空格） | ✅ | ✅ | ✅ |
| `\text{per-channel}`（连字符） | ✅ | ✅ | ✅ **首选** |

注：`\text{per\_channel}`（单反斜杠）在全场景失败。comrak 源码中 `scan_to_closing_dollar` 对 `opendollarlength==1` 遇 `\\` 跳过下一字符，单反斜杠 `\_` 中的 `_` 被跳过，但 raw string 保留 `\_` 送 MathJax——MathJax 在 `\text{}` 文本模式收到 `\_` 解析为下标命令，但文本模式不支持下标，崩溃。块级 `opendollarlength==2` 不跳过，`\_` 同样送达 MathJax 崩溃。

### 2.5 对策

> **在 `\text{}` 中，坚决不用下划线 `_`，统一用连字符 `-` 代替。**

| 方案 | 写法 | 块级 `$$` | 行内 `$` | 推荐度 |
|------|------|----------|---------|--------|
| 连字符替代 | `\text{per-channel}` | ✅ | ✅ | **首选** |
| 空格替代 | `\text{per channel}` | ✅ | ✅ | 可接受 |
| 双反斜杠绕过 | `\text{per\\_channel}` | ✅ | ⚠️ | **不推荐** |

## 三、坑二：百分号注释陷阱

### 3.1 犯罪现场还原

**触发条件**：在行内公式 `$...$` 中写下 `\%`。

**第 1 级：原始输入**

```markdown
- L1 拦住 90%（$M_1 = 10\%$）
```

`\%` 是 LaTeX 标准写法，在 TeX 中表示百分号字面量。

**第 2 级：inline 解析——`$` 识别与 `\` 处理的时序**

关键问题：`handle_dollars` 的 `scan_to_closing_dollar(1)` 在遇 `\\` 时跳过下一字符（源码第 2147 行）。这意味着 math raw string 中 `\%` 的 `\` 触发跳过，`%` 被跳过但仍保留在 raw string 内——**`\%` 原样透传给 MathJax**，反斜杠转义不作用于 math 内容。

那为什么单反斜杠 `\%` 会崩溃？问题不在 comrak 解析，在 **MathJax 的 TeX 层**：`\%` 在 TeX 中是合法的百分号字面量转义，本应成功。但 comrak 测试用例 `$1+\\%2$` 期望输出 `<math>1+\\%2</math>`——注意是 `\\%`（双反斜杠）原样保留，不是 `\%`。

实测路径分析：

- **`\%`（单反斜杠）**：`scan_to_closing_dollar(1)` 遇 `\` 跳过 `%`，raw string 保留 `\%`。MathJax 收到 `\%`，TeX 层解析为百分号字面量，**应成功**。但若 `\%` 中的 `\` 在 comrak 的 `\\` 跳过逻辑中被误处理（单 `\` 后跟 `%`，跳过 `%`，`\` 是否保留进 raw string 取决于具体实现），可能出现 raw string 变成裸 `%` 的情况——MathJax 收到裸 `%`，TeX 注释符吞掉闭合 `$`，崩溃。
- **`\\%`（双反斜杠）**：`scan_to_closing_dollar(1)` 遇第一个 `\\`，按源码逻辑跳过下一字符（第二个 `\`），但 comrak 测试用例显示 `\\%` 原样保留进 raw string，说明实际实现中 `\\` 的跳过逻辑与单字符 `\\` 不同。raw string 为 `\\%`，MathJax 收到 `\\%`，TeX 层 `\\` 是换行命令、`%` 是注释——这反而会出问题。

**实测稳定写法**：本仓库 `01-硬件组成.md` 采用 `\\%` 且实测渲染正常，说明 GitHub 私有部署的行为与上述纯源码分析有出入。**可观测结论**：`\\%` 在 GitHub 网页端实测稳定，`\%` 实测有风险。机制层面的精确解释需要 GitHub 私有部署源码，本文不强行定论。

> **`\\_` 与 `\\%` 的差异**：两者在 comrak 源码中走相同的 `scan_to_closing_dollar` 路径，差异在 MathJax TeX 层——`\\_` 是合法的下划线字面量转义，`\\%` 是合法的百分号字面量转义，TeX 层都能解析。但 comrak 解析阶段对 `\\` 的处理可能让 raw string 变成 `\` + 字符 或 `\\` + 字符，取决于具体实现，这影响 MathJax 收到的内容。

### 3.2 对策

> `\\%` 在全场景（块级 + 行内）实测稳定。但本仓库未来优先推荐方案一。

#### 方案一：把百分号请出公式（未来首选）

百分号本质上是"单位"，不需要数学字体的特殊渲染。把它留在 `$` 外面的纯文本世界里：

```markdown
- L1 拦住 90%（$M_1 = 10$%）
- L2 拦住剩下的 80%（$M_2 = 20$%）
- L3 再拦住剩下的 50%（$M_3 = 50$%）
```

✅ 彻底避开语法冲突，符合最佳排版规范（单位和数值分开处理），写法最干净。

#### 方案二：双重转义 `\\%`（同样可靠，第一章实际采用）

```markdown
- L1 拦住 90%（$M_1 = 10\\%$）
- L2 拦住剩下的 80%（$M_2 = 20\\%$）
- L3 再拦住剩下的 50%（$M_3 = 50\\%$）
```

✅ `01-硬件组成.md` 中即采用此写法，实测稳定。

## 四、坑三：全句斜体包裹公式引发的解析器截断

### 4.1 犯罪现场还原

**触发条件**：用 Markdown 原生斜体语法 `*...*` 包裹一段**含有行内公式**的文字。

```markdown
*(注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死。)*
```

**第 1 级：原始输入**

外层 `*...*` 是 emphasis 区间候选，内部含行内公式 `$N_{\text{channel}}$`。

**第 2 级：inline 解析——emphasis delimiter 与 math 节点的时序**

`parse_inline` 单遍扫描：遇外层 `*` → `handle_delim` 登记为 emphasis delimiter 候选；遇 `$` → `handle_dollars` 识别 `$...$` 为 math 节点（raw string 为 `N_{\text{channel}}`，内部 `_` 不被 `handle_delim` 看到）；遇闭合 `*` → 再登记一个 delimiter 候选。全部扫完后 `process_emphasis` 对 delimiter 候选配对。

**理论上**：math 节点内部的 `_` 不参与 emphasis 配对（`handle_dollars` 已跳过内部字符）。但外层 `*...*` 的两个 delimiter 候选配对时，配对区间跨越 math 节点——math 节点作为 AST 节点被包进 `<em>` 标签，其 raw string 不变。MathJax 仍能从 `<span data-math-style>` 节点提取 raw string 渲染。

**实测破坏机制**：外层 `*...*` 的 emphasis 配对可能因 flanking rules 失败（如闭合 `*` 后是 `)`，不满足 right-flanking），导致 `*` 未配对，作为字面 `*` 输出，斜体失效但公式可能保留；或配对成功但区间错位，把 `$` 包进 `<em>` 内外，破坏 math 节点边界。comrak 的 `handle_dollars` 在 emphasis 配对之前已生成 math 节点，math 节点边界不应被 emphasis 破坏——但若 `handle_dollars` 因闭合 `$` 误判而切错 raw string（如 1.4 节可能二），emphasis 配对后的 AST 结构会进一步扭曲。

**可观测结论**：用 `*...*` 包裹含行内公式的文字，斜体范围与公式边界可能错位，公式可能被破坏。具体破坏机制在开源 comrak 与 GitHub 私有部署之间存在张力，本文不强行定论。

### 4.2 对策

#### 方案一：改用引用块（最推荐，本仓库首选）

对于"注记"或"补充说明"，使用引用块 `>` 的视觉效果往往比斜体更好，而且**完全不会干扰公式渲染**——`>` 是 block 级语法，在 block 解析阶段就切走，内部段落走正常 inline 解析，外层没有 emphasis 区间包裹公式。这也是本仓库实际采用的写法：

```markdown
> **注：**$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。
```

✅ 排版优雅 + 公式零风险。

#### 方案二：使用 HTML 标签（备选）

直接用 HTML 斜体标签 `<i>` 或 `<em>`。CommonMark 将 `<i>`、`</i>` 识别为 inline HTML 节点，这些节点作为 emphasis flanking 判定的边界，阻断了 `_` 跨标签配对的可能。实测下公式得以原样保留：

```markdown
<i>（注：$N_{\text{channel}}$ 由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）</i>
```

✅ 跨平台一致渲染，公式完好无损。

#### 方案三：分段斜体（不推荐）

把公式前后的中文分别做斜体，把公式单独让出来：

```markdown
*（注：* $N_{\text{channel}}$ *由 CPU 架构与主板布线物理焊死，绝不随 DIMM 插入数量的无脑增加而改变。）*
```

⚠️ 能工作，但看着别扭。

## 五、全场景铁律汇总

三条铁律，按触发频率排列：

> **铁律一**：`\text{}` 中绝不写 `_`。用 `-` 代替。`\text{per-channel}` 是你的免死金牌。
>
> **铁律二**：`%` 尽量写在 `$` 外面（`$M_1 = 10$%` 未来首选）。双重转义 `\\%` 同样可靠（第一章沿用），禁用单反斜杠 `\%`。
>
> **铁律三**：需要斜体包裹含公式的文字时，用 `>` 引用块（本仓库首选）或 `<i>...</i>`，不用 Markdown 原生 `*...*`。

## 六、工具链适配

本仓库的 `tools/formula-spacer.js` 负责自动管理公式与中文之间的**空格**。它不处理上述三类陷阱。

未来可扩展的 lint 检查点：

- 扫描 `\text{}` 内是否包含 `_` 字符
- 扫描行内 `$...$` 内是否包含 `\%`
- 扫描 `*` 斜体范围内是否包含 `$...$`
- 在 CI/提交钩子中自动检测并告警

## 七、查证来源

- `github/markup` 仓库 `lib/github/markup/markdown.rb`：确认 GitHub 用 `commonmarker` 2.x，启用 `:math_dollars`/`:math_code` 扩展。
- `github/cmark-gfm` 仓库（C 版，tag `0.29.0.gfm.13`）：`extensions/` 目录无 math 源码，grep `math`/`dollar` 全仓库为零，确认 GitHub 网页端不使用此仓库的 math 实现。
- `kivikakk/comrak` 仓库 main 分支 `src/parser/inlines.rs`：math 识别在 inline 阶段（`handle_dollars`，第 1246 行），`scan_to_closing_dollar` 第 2130 行，`scan_to_closing_dollar` 对 `opendollarlength==1` 的 `\\` 跳过逻辑第 2147 行。
- `kivikakk/comrak` `src/tests/math.rs`：测试用例 `$1+2\\$$` → `<math>1+2\\$</math>` 证明 math raw string 原样透传，反斜杠不转义。
- `kivikakk/comrak` `src/strings.rs` `normalize_code` 函数：只处理换行和首尾空格，不处理反斜杠。
- GitHub issue [#1575](https://github.com/github/markup/issues/1575)：行内公式多个下标被破坏的实测记录，2024-08-24 关闭。
- GitHub issue [#1709](https://github.com/github/markup/issues/1709)：行内/块级公式不可靠的实测记录，`\_` 转义 workaround。
- [GitHub Blog: Math support in Markdown](https://github.blog/2022-05-19-math-support-in-markdown/)：确认 GitHub 用 MathJax 渲染。
- [GitHub Docs: Writing mathematical expressions](https://docs.github.com/en/get-started/writing-on-github/working-with-advanced-formatting/writing-mathematical-expressions)：确认 MathJax 为渲染引擎，`$...$`/`$$...$$`/`` $`...`$ ``/` ```math ` 为分隔符语法。
