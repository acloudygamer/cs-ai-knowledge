# 坑四行内 `$` 边界规则 -- 全量验证（已定稿）

> **验证方法**（2026-08-22）：GitHub `POST /markdown` API 逐例渲染（与 github.com 网页端同一条服务端管线），判据 = 返回 HTML 是否出现 `<math-renderer>` 元素。
>
> 一维扫描设计：测开侧时闭侧固定空格、测闭侧时开侧固定空格；公式统一 `x+y`（排除坑一/斜体干扰）。

## 总结论

| 位置 | 合法（公式被提取） | 阻断（`$` 原样输出） |
|---|---|---|
| 开 `$` **前** | 空格、行首、半角 `(` | 其余一切：可见 ASCII 93 个（字母/数字/下划线/全部标点，含 `[` `{` `<`）、全角 `（`、CJK 汉字、CJK 标点、日韩文字与符号 |
| 闭 `$` **后** | 除 ASCII 词字符外的一切：ASCII 标点（含 `$` `*` `` ` ``）、CJK 汉字/标点、日韩文字符号、空格、行尾 | ASCII 字母 52 + 数字 10 + 下划线 1（共 63，恰为 `\w` 词字符集） |
| 开 `$` **后**（内侧 padding） | —— | 空白，无条件阻断（`$ x+y$`） |
| 闭 `$` **前**（内侧 padding） | 空白可容忍：闭 `$` 后是非空白字符（` $后`、` $。`）或行尾时存活 | 闭 `$` 前是空白**且**后也是空白且其后仍有文字（` $ ` 三明治）时阻断 |

计数：开侧 263 例仅 1 例存活（O-A-08 `(`）；闭侧 262 例存活 199、阻断 63。

## 分组明细（组内完全同质）

| 分组 | 例数 | 存活 | 阻断 |
|---|---|---|---|
| 开侧 × ASCII 可见字符 | 94 | 1（`(`） | 93 |
| 开侧 × 全角 `（` 对照 | 1 | 0 | 1 |
| 开侧 × 中文常用字 | 100 | 0 | 100 |
| 开侧 × 中文标点 | 25 | 0 | 25 |
| 开侧 × 日文文字 | 10 | 0 | 10 |
| 开侧 × 日文符号 | 11 | 0 | 11 |
| 开侧 × 韩文文字 | 10 | 0 | 10 |
| 开侧 × 韩文符号 | 12 | 0 | 12 |
| 闭侧 × ASCII 可见字符 | 94 | 31 | 63 |
| 闭侧 × 中文常用字 | 100 | 100 | 0 |
| 闭侧 × 中文标点 | 25 | 25 | 0 |
| 闭侧 × 日文文字 | 10 | 10 | 0 |
| 闭侧 × 日文符号 | 11 | 11 | 0 |
| 闭侧 × 韩文文字 | 10 | 10 | 0 |
| 闭侧 × 韩文符号 | 12 | 12 | 0 |

闭侧 ASCII 的 31 存活 = 28 个普通标点 + 3 个曾预判存疑的标点（`$`、`*`、`` ` ``）；63 阻断 = 62 字母数字 + `_`（下划线是词字符，与字母数字同命运）。

## 维度二：容器标签（同一文本 `前 $x+y$ 后`、干净边界）

| 提取被跳过（公式死） | 提取正常（公式活） |
|---|---|
| `em`（`*`/`_` 斜体的产物）、`b`、`a`（含 markdown 链接 `[..](..)`）、`code`（代码 span） | `i`、`strong`（`**`）、`del`（`~~`）、`span`、`u`、`s`、`sub`、`sup`、`mark`、`q`、`kbd`、`var`、`cite`、`abbr`、`small`、`li`、`blockquote`、表格单元格、标题、`p` |

em 死 i 活、b 死 strong 活——跳过集合与样式/语义无关，闭源实现按实测记录。

### 未闭合标签与未配对定界符（2026-08-22 API 实测）

| 写法 | 公式结果 |
|---|---|
| 单个 `*` / `**`（未配对） | 活（无配对对象，保持字面） |
| 未闭合 `<em>` / `<b>` | 标签之后全部死；公式在标签之前不受影响 |
| 未闭合 `<a>` | 仅标签所在段内死，不跨段 |
| 未闭合 `<i>`（非跳过集合） | 活 |
| 孤立的 `</em>`（无对应开启） | 活（解析器丢弃） |

机制（2026-08-24 实测补全）：补闭合发生在服务端，API 返回的 HTML 里直接可见，补法为"双补"——先在段尾补一个闭合，紧接着新开一个同名元素包住后续所有块，直到文档末尾再补闭合（实测：`<p>前 <em>斜体开头</em></p><em><p>二段 $x+y$ 公式</p>…</em>`）；em 实测覆盖段落/标题/列表三种块型，b 实测段落，i 同样包裹但无害；`<a>` 在段界补闭合后不再复开。落入跳过集合（em/b/a）的即全灭。

排查提示：手写标签后公式死亡，先看标签是否未闭合（本表），再看标签后是否紧邻全角标点（开侧规则）。后者是本轮 `<i>` 用例差点被误判的原因：测试文本里的 `注：` 全角冒号先触发了开侧阻断，与标签无关。

### 原始 HTML 死亡区复验（2026-08-24，5 例）

| 写法 | 公式结果 |
|---|---|
| `<div>前 $x+y$ 后</div>`（单行独占） | 死（整行 = HTML 块，不经行内解析） |
| `<div>`＋内容＋`</div>` 多行无空行 | 死（HTML 块到空行才结束） |
| `<div>`＋空行＋段落＋空行＋`</div>` | 活（空行分段，中间恢复正常段落） |
| `<span>前 $x+y$ 后</span>`（单行） | 活（span 非块级标签触发，整行是段落；span 也不在跳过集合） |
| `文字 <div>前 $x+y$ 后</div>`（**行首有文字**） | 死（**新发现**：整行虽是段落而非 HTML 块，公式夹在行内 `<div>` 标签之间仍不提取，机制闭源按实测记录） |

## 关键机制证据（供管线研讨引用）

- 合法边界时 API 返回 `<math-renderer class="js-inline-math">$x+y$</math-renderer>`，非法时 `$` 原样——**边界判定全部发生在服务端**，网页端 MathJax 只排版已包好的元素。
- display 块 `$$…$$`（独占段落）→ `<math-renderer class="js-display-math" style="display: block">$$…$$</math-renderer>`，同样服务端包好。
- `` $`x+y`$ `` 代码包裹语法 → 转为 `js-inline-math`，内容变为 `$x+y$`。
- Markdown 转义层在提取**之前**改写公式源码：`\_`→`_`、`\%`→`%`（坑一/坑二根源，公式照样被提取但 TeX 源已坏）；`\$`→`$` 会连带改写开侧边界字符（O-A-60 实测：`前\$x+y$ 后` 变 `前$x+y$ 后`，开侧成 CJK 而阻断）。
- 斜体对跨过公式（`前 _甲 $x_1$ 乙_ 后`）→ em 吞掉公式 → 死；斜体对先闭合（`前 _甲_ $x_1$ 乙 后`）→ 公式活（坑一的 em 路径）。

---

# 坑五块级 `$$` 公式 -- 专项验证（2026-08-23 第二轮，60 例 + 毒害追加实测）

> **验证方法**：同上（GitHub `POST /markdown` API 逐例渲染）。判据比行内版多两维：math-renderer 的 **class**（`js-display-math` / `js-inline-math`）与**送达内容形态**（定界符保留 / 被改写）。每例前后垫不含 `$` 的中性段落。矩阵：形态学 17 / 上下文 13 / 容器 9 / 转义配对 15 / 退化与行内对照 6。探针脚本 `display_probe.py` 可复跑。

## 总结论

**核心模型（解释全部 60 例）**：`$$` 块在 cmark 眼里**不是一个独立块类型**——它就是一个"整段内容恰为 `$$…$$`"的**普通段落**。Phase 2（转义/emphasis/断行）照常在段内运行；Phase 3.5 只把这种纯段落包成 `js-display-math`，并对内容做重建：`<em>`→`_`、行尾孤 `\`→`\\`、剥内容行缩进。由此推出全部规则：

| 维度 | 实测结论 |
|---|---|
| 独占段落 | 前后须真空行，或前一行是**自然结束的块**（标题行/围栏闭合行/分隔线）→ display |
| 并入邻段 | 前行是正文/列表项/引用行（无空行 = 懒延续）或后行是正文 → 死（多行形态）；单行形态**退化 inline** |
| 表格 | 前行是表格行 → `$$` 行被吞成表格单元格 → 死 |
| 块内空行 | 死：段落被拆散，`$$` 落在两个半残段落里；空行后 ≥4 缩进行还会被解析成缩进代码块 |
| 边界/padding | **全免**：`$$ x+y $$`、界行尾随空格、内容贴定界符行（`$$x+y`）、内容行缩进（送达时剥除）、整块 ≤3 空格缩进全部正常 display——坑四只管行内 `$` |
| 块内转义 | 转义层照吃一层：`\%`→`%`、`\_`→`_`、`\$`→`$`、`\&`→`&`、`\{`/`\}`→裸花括号（集合记号字面括号静默消失，行内同病）；**行尾 `\\` 存活**（塌成 `\` 后被重建为 `\\`，aligned 可用），行中 `\\` 塌成 `\`；`\\%`/`\\{`/`\\_` 修复写法实测有效 |
| 块内 emphasis | 照常解析但被重建：成对 `*` 重建为成对 `_`（乘号 `a*b*c` 静默变 `a_b_c`，TeX 双下标错）；成对 `_` 侥幸还原（只多无害空白）——**坑一配对路径在块内不致命**；未配对 `*` 原样 |
| 行内对照 | 公式内成对 `*`（`$a*b*c$`）→ em 切断 → 死；**单公式内成对可配对 `_`（`$_{i} + a_{j}$`）→ 死**（坑一配对路径的单公式内实例，此前未记录）；未配对 `*` 活 |
| 容器 | 引用内逐行 `>` 正常 display（引用文字紧邻 `> $$` 无 `>` 空行 = 懒延续 = 死；`$$` 段后直接跟裸文字行同样被懒延续吸入）；**列表项内不可靠**——"空行+缩进"写法单独成文有效，但混合文档中受文档级毒害支配（见下文追加实测）；表格单元格内退化 inline；em 容器内死（坑三适用 `$$`） |
| 退化 | 混排单行 `值 $$x+y$$ 值`（边界干净）→ 包成 `js-inline-math`、内容保留 `$$`——不报错、MathJax 收到 `$$…$$` 视觉异常，**最难发现的失败形态** |
| 定界符 | `$$$x+y$$$` → 提取但内容带多余 `$`（TeX 错）；空块 `$$$$` → 死 |

## 分组明细

| 组 | 例 | 结果 |
|---|---|---|
| A1 | 单行 `$$x+y$$` 独占段落 | display（基线） |
| A2a–A2c | 单行 + 开/闭/两侧 padding | display（免边界实证） |
| A3 | 多行标准 `$$⏎x+y⏎$$` | display |
| A4a/A4b | 内容贴开/闭定界符行 | display |
| A5 | 块内空行 | 死 |
| A6a | 块内空行 + 后续行缩进 4 空格 | 死（缩进行变缩进代码块） |
| A6b | 内容行缩进 4 空格（无空行） | display（缩进剥除） |
| A7 | 行尾 `\\` 换行 | display（`\\` 保留） |
| A8a | `$$$x+y$$$` | display，内容 `$$$x+y$$`（病理） |
| A8b | `$$$$` 空块 | 死 |
| A9/A10 | 开/闭 `$$` 行带尾随空格 | display（尾空白剥除） |
| A11 | 整块缩进 2 空格 | display |
| A12 | 块内未配对 `*` | display，原样 |
| B2 | 前行=正文（无空行） | 死 |
| B3 | 后行=正文（无空行） | 死 |
| B4/B4b | 前行=标题（多行/单行形态） | display |
| B5/B5b | 前行=列表项（多行/单行形态） | 死 / 退化 inline |
| B6 | 前行=引用行 | 死 |
| B7 | 前行=围栏闭合行 | display |
| B8 | 前行=分隔线（`---` 前垫空行避开 Setext） | display |
| B9 | 前行=表格行 | 死（`$$` 行被吞进表格） |
| B10/B10b | 开 `$$` 同行前缀文字（单行/多行形态） | 退化 inline / 死 |
| B11 | 闭 `$$` 同行尾巴文字 | 退化 inline（非死） |
| C1 | 列表项内多行块（无空行，缩进对齐） | 死 |
| C1b | 列表项内块前垫空行 | display（**单独成文时**；混合文档中受下文"文档级毒害"支配） |
| C2 | 列表项内单行（缩进） | 退化 inline |
| C3/C3b | 引用内多行块（块开头 / 文字+`>` 空行隔开） | display |
| C4 | 引用内单行 | display |
| C5 | 表格单元格内单行 | 退化 inline |
| C6 | em 容器内混排 | 死（坑三适用） |
| Q1 | 引用文字紧邻 `> $$`（无 `>` 空行） | 死（引用内懒延续） |
| D1 | 块内 `\%` | display，内容 `%` 裸（坑二同病实证） |
| D2 | 块内 `\text{per\_channel}` | display，`_` 裸（坑一转义路径实证） |
| D3a | 块内 intraword 下标 `x_i + y_j` | display，干净 |
| D3b | 块内双开侧 `_` 对 | display，干净 |
| D3b2 | 块内开+闭 `_` 对 | display，内容原样 + 空白扰动 |
| D3c | 块内成对 `*`（`a*b*c`） | display，送达 `a_b_c`（静默改写） |
| D4 | 块内 `\$` | display，`$` 裸 |
| D5a | 块内 aligned | display，干净（行尾 `\\` 保留） |
| D5b | 块内 `\&` | display，`&` 裸 |
| D6/D6b | 块内/行内 `\{x\}` | display/inline，送达 `{x}`（字面括号丢失，行内同病） |
| V1/V2 | 行内/块内行中 `a \\ b` | 均塌成 `a \ b` |
| F1/F2/F3 | 修复写法 `\\%` / `\\{` / `\\_`（块内） | display，送达 `\%` / `\{x\}` / `\text{per\_channel}`，全部正确 |
| E1 | 混排干净边界 `值 $$x+y$$ 值` | 退化 inline（内容 `$$x+y$$`） |
| E2 | 相邻两个 display 块 | display ×2 |
| I1 | 行内 `$a*b*c$` | 死（em 切断文本节点） |
| I2 | 行内 `$_{i} + a_{j}$` | 死（单公式内 `_` 配对） |
| I3 | 行内 `$x*y$` | inline，原样 |

## 追加实测：列表项内 `$$` 块的文档级毒害（2026-08-23，复跑可复现）

C1b 的"空行+缩进"写法在混合文档中翻车——fixture 验证时发现列表 display 块整体消失，专项消元后确立：

**规则**：列表项内的 display 块，只要**更早处（含所在列表项内）存在任何其他已被提取的公式**（行内 `$…$`、`` $`b`$ ``、退化 `$$…$$`、正文或引用的 display 块），就静默失效；**列表整体之后的公式无害**；正文与引用内的 display 块对该毒害免疫。

| 实验 | 结果 |
|---|---|
| 行内 `$b$` 在列表块前（正文段/同项文字行/前一个列表项/紧贴列表前段） | 列表块死 |
| 退化 `$$o+p$$`（列表项懒延续单行）在列表块前 | 列表块死 |
| 正文 display 块在列表块前（有无中间段落皆然） | 列表块死 |
| 引用 display 块（正确闭合）在列表块前 | 列表块死（引用块自身活） |
| 反引号代码包裹行内在列表块前 | 列表块死 |
| 同项内行内公式在列表块**之后** | 列表块死 |
| 列表整体**之后**的行内公式（紧邻/隔段） | 列表块活 |
| 未被提取的 `$`（如 `值为：$$g+h$$个`、`值 $5 元`）在列表块前 | 列表块活（未提取即无毒） |
| 全文仅此一个公式（对照） | 列表块活 |

机制定位：闭源。现象上"提取动作本身"改变了后续列表项内 `$$` 段落的识别（顺序敏感、跨块持续），与坑三容器、坑四边界无关。

**对策落点**：列表项内不放 `$$` 块（真实文档几乎必然含更早的行内公式），列表内用行内 `$…$`；块级展示放正文（最稳）或引用（注意引用内 `$$` 段后直接跟裸文字行会被懒延续吸入，须空行或 `>` 行闭合——本轮 D0 系用例的引用块死亡即此因，与毒害无关）。

## 关键机制证据（供管线研讨引用）

- **纯段落模型**：A5 块内空行返回 `<p>$$<br> x+y</p> <p>z<br> $$</p>`（拆成两个半残段落）；B9 表格把 `$$`/`x+y` 各行吞成 `<td>`；Q1 引用内 `<p>引用文字<br> $$<br> x+y<br> $$</p>`（懒延续）——"块"的生死完全由 Phase 1 段落结构决定。
- **em 重建（行内与块级分裂的根源）**：同一 emphasis，行内 `$a<em>b</em>c$` 切断文本节点 → 提取失败（I1 死）；块级提取则跨标签重装配，`<em>`/`</em>` 被替换回 `_`（D3c 送 `a*b*c` 收 `a_b_c`；D3b2 送 `_{i} + a_{j}` 原样还原）。
- **`\\` 行尾往返**：`\\`（行尾）经转义层塌成 `\`，块级重建时行尾孤 `\` 被加倍回 `\\`（A7/D5a 送达保留）；行中 `\\` 无此待遇（V2 塌成 `\`；行内 V1 同）。
- **转义塌缩集合**：`\%` `\_` `\$` `\&` `\{` `\}` 全部被吃一层反斜杠（与行内一致）；`\\X` 双反斜杠修复写法在块内实测有效（F1–F3）。

---
验证历史：2026-08-22 API 全量 525 例 + padding 探针 + 容器维度 + 机制 grounding + 未闭合标签探针，同日网页端复核例外结论（`<i>` 容器存活、闭 padding 贴中文/贴标点/行尾存活、` $ ` 三明治死亡），全部与 API 判定一致（人工还基本检查了大部分情况，与API一致）。2026-08-23 第二轮：块级 `$$` 专项 60 例（`display_probe.py`）确立纯段落模型与坑五全部规则；同日追加毒害消元实测（约 30 例，每例复跑验证可复现），确立列表项 `$$` 块的文档级毒害规则。2026-08-24：原始 HTML 死亡区复验 5 例，新发现行首带文字的行内 `<div>` 包夹公式同样死。
