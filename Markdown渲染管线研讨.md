# Markdown 渲染管线研讨

> 用途：从"原始文本 → 网页显示"的渲染管线出发，推导公式排版正文（`公式排版速查.md`）+ 单一调整脚本。本文档是协作底稿，边聊边改。
>
> 已定约束：
> - **可迁移性优先**：只使用跨渲染器（GitHub/MathJax、KaTeX 系工具、Pandoc 系）通用的语法。GitHub 专属语法（`` $`...`$ ``、```math 围栏块）不进入正文写法，仅作对照研究。
> - 引擎事实：GitHub 使用 MathJax（2022 changelog 与现行官方文档一致）。
> - 管线事实（2026-08-22 API 实验确立）：数学提取在服务端 HTML 后处理步骤完成（见 Phase 3.5），网页端 MathJax 只排版已包好的 `<math-renderer>` 元素；实测依据见 全量验证.md。

## Phase 0｜原始文本

一行说明：`.md` 文件的 UTF-8 字节流；此层没有任何结构概念，`#`、`>`、`$` 都只是普通字符。

## Phase 1｜块结构解析

cmark-gfm 逐行扫描，**看行首**把文档切成块序列；行内容（强调、链接、`$` 公式）这一阶段不看。
块分两类：**叶块**（装文字）和**容器块**（装别的块）。下面按语法从简到繁列出全部块。

### 1. ATX 标题（叶块，最简单）

- 作用：章节标题。
- 语法：行首 1–6 个 `#` + 空格 + 文字。
- 单行块：一行就是一个块，开始即结束。

### 2. 主题分隔线（叶块）

- 作用：内容分隔（渲染成 `<hr>`）。
- 语法：独占一行、≥3 个相同的 `-` / `*` / `_`（可夹空格）。
- 单行块，无内容。
- 注意：紧跟在段落后的 `---` 会被判为 Setext 标题线（见 4），不是分隔线。

### 3. 段落（叶块）

- 作用：普通正文。
- 语法：连续的非空行。
- 结束：空行；或被"打断白名单"的行首截断（标题/引用/围栏/分隔线可以；缩进代码不行；列表有条件：有序须 `1.` 开头且首项非空）。
- **打断只对段落存在**：别的块是"续不上就自然结束"，不存在被打断。

### 4. Setext 标题（叶块）

- 作用：h1/h2 的另一种写法。
- 语法：一个段落 + 下一行 `===`（h1）或 `---`（h2）。
- 依附段落：把前面那个段落变成标题，不是独立开始的块。

### 5. 缩进代码块（叶块）

- 作用：原样显示代码/文字。
- 语法：每行 4 空格缩进。
- 不能打断段落：前面必须有空行，否则这几行只是段落里的文字。

### 6. 围栏代码块（叶块）

- 作用：原样显示代码/文字，可标注语言名（如 ` ```python `）。
- 语法：` ``` ` 或 `~~~` 开始；同种字符、长度 ≥ 开始围栏的围栏行结束。
- 中间一切行（含空行、含 `#`/`>`/`$$` 行首）都原样显示，**没有任何行首能打断它**。

### 7. 表格（GFM，叶块）

- 作用：二维表格。
- 语法：表头行 + 分隔行（如 `|---|---|`）+ 数据行。
- 由段落转化而来：段落的下一行是合法分隔行时，段落变成表格。

### 8. HTML 块（叶块）

- 作用：直接嵌入 HTML（如 `<div>`、注释）。
- 语法：行首块级标签等 7 种触发；内容直到空行为止。

### 9. 引用块（容器块）

- 作用：引用、提示框。
- 语法：每行行首 `>`（前导空格 ≤3）。容器：剥掉每行的 `>` 后，内部重新分块，可有标题/列表/公式/嵌套引用（`>>`）。
- 结束：某行无 `>` 且不满足懒延续时关闭。懒延续 = 该行是普通文字、且内部有一个还没关闭的段落。
- `>` 空行：引用不关，内部段落关闭。真空行：引用和段落都关闭。
- 父死子亡 = 剪枝：某层容器关闭时，它里面所有还开着的块在同一时刻一并关闭（子块不被单独询问，不可能比父块多活一行）。

### 10. 列表（容器块）

- 作用：有序/无序列举。
- 语法：行首 `-` / `+` / `*` 或 `1.` / `1)`。
- 三层结构：列表 → 列表项 → 内容块。列表项是容器：缩进对齐的后续行归它。
- 新 marker：关闭当前列表项、开新列表项；列表本身不关。

### 11. `$$` 数学块（2026-08-23 专项实测修订）

- 作用：展示公式。
- 真实形态：**不是一个独立块类型**——`$$` 行不能打断段落（2026-08-22 实测），所谓"数学块"其实是一个**整段内容恰为 `$$…$$` 的普通段落**。Phase 2（转义/emphasis/断行）照常在段内运行；Phase 3.5 把这种纯段落包成 `js-display-math` 并做内容重建（见 Phase 3.5）。"块"的生死完全由本阶段的段落结构决定（60 例专项实测，见 `全量验证.md` 第二轮）：
  - 前后须真空行，或前一行是自然结束的块（标题/围栏闭合/分隔线）；前行是正文/列表项/引用行（懒延续）或后行是正文 → 并入邻段 → 死（多行形态）或退化 inline（单行形态）；前行是表格行 → 被吞进单元格。
  - 块内不能有空行（段落拆散即死；空行后 ≥4 缩进行变缩进代码块）。
  - 边界/padding 全免（`$$ x+y $$`、界行尾随空格、内容贴界、内容行缩进、整块 ≤3 空格缩进均活）——与行内坑四根本不同。
  - 列表项内不可靠：单独成文时空行+缩进有效（C1b），但存在**文档级毒害**——更早处（含同项内）有任何已提取公式即静默失效（追加实测，见 全量验证.md）；引用内逐行 `>`（引用文字紧邻 `> $$` 无 `>` 空行 = 懒延续 = 死；`$$` 段后直接跟裸文字行同样被吸入）。
- 非 CommonMark/GFM 标准；跨渲染器行为不统一。

### 数学块的另一个入口（出局，仅对照）

` ```math ` 围栏块 = info string 为 `math` 的围栏代码块，走第 6 节围栏规则，是规格上最稳的块；但 GitHub 专属，因可迁移性约束出局。

## Phase 2｜行内解析

块结构确定后，cmark-gfm 对每个块的内容做行内扫描。**关键事实（已核实）：github/cmark-gfm 的扩展目录只有 autolink/table/strikethrough/tagfilter/tasklist，没有 math 扩展，`$` 对解析器不是特殊字符**，公式原文原样穿过本阶段进入文本节点。

本阶段与本研讨相关的构造：

1. **反斜杠转义**：`\X`（X 为 ASCII 标点）→ 字面 `X`。落在公式内部时直接改写 TeX 源，且发生在一切公式判定之前：`\_` → 裸 `_`（坑一单反斜杠失败）、`\\_` → `\_`（坑一双反斜杠有效）、`\%` → 裸 `%`（坑二百分号静默消失）、`\$` → 裸 `$`（连带改写开侧边界字符）、`\{`/`\}` → 裸花括号（集合记号字面括号静默消失，块内行内同病，2026-08-23 实测）。实测：`$\text{per\_channel}$` 提取成功，但元素内容已是 `$\text{per_channel}$`。
2. **代码 span**：反引号内容完全字面，`$` 失去一切含义，是 `` $`…`$ `` 语法的地基（服务端会把它转回标准 inline math）。
3. **原始行内 HTML**：`<i>`、`<em>` 等成为 html_inline 节点，正文文本节点在标签处被切断。
4. **emphasis 定界符**：`*` `_` `~` 入定界符栈，段落扫描完后统一配对成 em/strong/del。配对可以跨过 `$`（解析器不知道公式存在）：斜体对把公式整段包进 em（坑三触发源）；`_` 对与公式内的 `_` 组合把公式吞进 em（坑一的斜体路径）。实测：`前 _甲 $x_1$ 乙_ 后` → `<em>甲 $x_1$ 乙</em>`，公式死；`前 _甲_ $x_1$ 乙 后` → 公式活。

输出：行内节点树（text / code / html_inline / em / strong / del / link / image / autolink……）。

## Phase 3｜HTML 生成与消毒

- cmark-gfm 把节点树序列化成 HTML；tagfilter 扩展拦截危险原始标签。
- GitHub 内部 html-pipeline 过滤器链继续处理（OSS 仓库可见的部分）：sanitization（实测会给 `<a>` 补 `rel="nofollow"`）、emoji、语法高亮等。
- OSS cmark-gfm 无 math 扩展、OSS html-pipeline 无 math 过滤器（两处目录均已核对）：数学提取是闭源内部步骤，插在 HTML 生成之后。

### Phase 3.5｜服务端数学提取（坑三/坑四的真正执行点）

遍历 HTML 文本节点寻找 `$…$`（行内）与 `$$…$$`（块级），判定规则全部经 API 实测（525 例一维矩阵 + 容器维度 + padding 探针 + 2026-08-23 块级专项 60 例，见 全量验证.md）：

- **开侧**（行内）：`$` 前必须是空格、行首或半角 `(`，别无其他。
- **闭侧**（行内）：`$` 后不能是 ASCII 词字符（字母/数字/下划线，即 `\w`）。
- **内侧 padding**（行内）：开 `$` 后空白无条件阻断；闭 `$` 前空白仅在"前后都是空白且其后有文字"时阻断。
- **容器**：跳过 `em`/`b`/`a`/`code` 元素子树（em 死 i 活、b 死 strong 活，与样式语义无关；`i`/`strong`/`span`/`del`/`li`/表格/标题/引用块均正常进入）。
- **块级**（坑五）：只认"整段内容恰为 `$$…$$`"的纯段落（Phase 1 段落结构决定），**不做边界判定**（padding/贴界/缩进全免）；混在段落文字里的单行 `$$…$$` 按行内规则判，边界干净时包成 inline（内容保留 `$$`，静默退化）。

合法 → 包成 `<math-renderer class="js-inline-math" data-run-id="…">$x+y$</math-renderer>`（display 块为 `js-display-math` + `display: block`，内容含 `$$` 定界符原文）；非法 → `$` 原样留在文本里。`` $`x+y`$ `` 代码包裹语法也在此层转为标准 inline math（内容变 `$x+y$`）；该形式**不受边界与容器规则约束**（实测：全角标点紧贴两侧、em 容器内均照常提取），内容也不经 Phase 2 转义层（单反斜杠原样送达）。

块级提取对段内 HTML 做**重建**（2026-08-23 实测）：`<em>`/`</em>` 替换回 `_`（成对 `*` 乘号 `a*b*c` 静默变 `a_b_c`；成对 `_` 侥幸还原原样）；行尾孤 `\` 加倍回 `\\`（所以行尾 `\\` 换行经"塌缩—重建"往返后存活，行中 `\\` 塌成 `\`）；内容行前导缩进剥除。行内提取无重建——em 直接切断文本节点，公式死（同源分裂：块内成对 `_` 活，行内单公式内成对 `_` 死）。

证据：`POST /markdown` API 返回的 HTML 已含/不含 math-renderer，边界判定全部在服务端完成，与 2026-08-17 网页端人工核对一致。

## Phase 4｜浏览器端 MathJax 排版

- 客户端按需加载 MathJax，定位 `.js-inline-math` / `.js-display-math`（math-renderer 元素），把元素内的 TeX 文本交给 MathJax 排版，元素内容随后被替换为排版输出（网页 DOM 里 math-renderer 内嵌的 MathML 即此产物）。
- **MathJax 不做任何"公式在哪"的判定**：扫描、边界、容器规则全部在 Phase 3.5 服务端结束，客户端只负责"怎么排"。
- 网页实测佐证：闭 `$` 前带空白的服务端已提取公式在网页端照常排版，官方文档所载"闭 `$` 左侧须非空白"在提取与排版两层均未严格执行（服务端只拦 ` $ ` 三明治情形）。
- 失败模式由此二分：坑三/坑四 = 服务端没包 math-renderer（元素缺失，`$` 原样）；坑一/坑二 = math-renderer 已生成，但 TeX 源在 Phase 2 被转义层改坏（裸 `_` 报错、`%` 变注释静默吃尾）。

## 议题 A｜工具陷阱的重新归因（坑一~四已解决 2026-08-22；坑五 2026-08-23）

五坑归因全部落位（结论见本文 Phase 1/2/3.5 与 `全量验证.md`；正文按管线行为组织于 `公式排版速查.md`）：

| 坑 | 真实归因 | 执行层 |
|---|---|---|
| 坑一 `\_` | 转义层把 `\_` 改写成裸 `_`（TeX 源坏）；另一路径：`_` 配对造出 em，吞/截公式 | Phase 2（+3.5 容器） |
| 坑二 `\%` | 转义层把 `\%` 改写成裸 `%`，TeX 注释符静默吃尾 | Phase 2 |
| 坑三 斜体容器 | 提取跳过 em/b/a/code 子树；旧结论被全角标点（开侧规则）混淆 | Phase 3.5 |
| 坑四 边界 | 开侧/闭侧/padding 边界规则（525 例实测） | Phase 3.5 |
| 坑五 块级 `$$` | `$$` 块 = 纯段落，段落结构决定生死（独占/空行/懒延续/表格吞行）；块内重建改写（em→`_`、`\\` 往返、转义塌缩）与静默退化 inline（60 例实测） | Phase 1 + 2 + 3.5 |

统一原理：每层只回答一个问题，坑 = 上游层替下游做了不知情的决定。

## 议题 B｜公式排版规范重构（坑一~四 2026-08-22；坑五 2026-08-23；2026-08-24 规范退役）

坑式规范（`公式排版规范.md`，按坑一~五组织）曾于 2026-08-22 按管线层重写、2026-08-23 增坑五。2026-08-24 评估：坑编号是发现顺序的化石，与管线分层（Phase 1/2/3.5）错位，技术债务大于价值——**规范退役删除**，`公式排版速查.md` 升格为正文（从管线行为出发，不使用坑编号）。坑一~五作为历史名在此保留索引：转义塌缩与配对（坑一）、百分号（坑二）、容器跳过（坑三）、行内边界（坑四）、块级 `$$`（坑五）。

## 议题 C｜单一调整脚本设计（2026-08-22 定向；2026-08-23 补坑五）

**失败三分法**（Phase 4 失败模式的脚本视角）：

- **未提取**：API 渲染后无 math-renderer，`$` 原样 → 边界规则（坑四）或标签规则（坑三容器、坑一配对路径、未闭合标签）或坑五段落结构（未独占/块内空行/表格吞行）
- **退化**（坑五，2026-08-23 新增）：display 候选被包成 `js-inline-math`（内容保留 `$$`）——不报错但视觉异常，必须比对 renderer class 才能发现
- **已提取但源坏**：有 math-renderer 但内容与源码预期不符 → 转义层冲突（坑一/坑二/`\{`）或块内重建改写（成对 `*`→`_`）
- （罕见第四类：内容正确但视觉异常，即真 LaTeX 语法错误，人工处理）

**核心方法：API 作判据（oracle）**。脚本不自行建模闭源规则，直接问服务端：POST 全文，源码公式候选（排除代码 span/块）与返回 HTML 的 math-renderer 集合做差 → 缺失 = 未提取类，内容 diff = 转义类。一次调用完成两级分诊；修复后重新 POST 复检，形成"检查 → 修 → 复检"闭环。

**修复顺序 = 诊断顺序**（逐层消元，从最安全到最侵入）：

1. **空格层**（自动、安全）：开 `$` 前补空格（合法集合：空格/行首/`(`/节点首），闭 `$` 后与 ASCII 词字符之间补空格 → 复检，剩余未提取者进下一层
2. **标签层**（半自动）：`*` 包裹改 `<i>`、补未闭合标签、公式移出链接文字；能自动的自动改，不能的标记人工 → 复检
3. **转义层**（建议式）：`\_` → `\\_`、`\%` → `\\%`（或 `%` 移出公式），改动公式内容，默认给建议不盲改 → 复检内容 diff 归零

实现状态：只读诊断已落地（`math_check.py`：扫全仓 md → API 分诊 → 按 L1 边界 / L2 标签 / L3 转义 / 退化 输出问题清单，2026-08-23 加入坑五：renderer class 比对（退化）、$$ 上下文矩阵（独占段落/表格吞行/懒延续）、块内空行与 `$$$` 检查、em 改写与 `\$` 病理签名）；修复动作后续迭代。实证探针 `display_probe.py` 保留，可复跑。
