# 公式排版速查

> 定位：公式排版的正文规范。实测记录见 `全量验证.md`，管线推导见 `Markdown渲染管线研讨.md`。
>
> **判据**：一个公式"活着"的唯一标志，是服务端把它包进 `<math-renderer>` 元素——行内与块级共用这一元素、以 class 分流：行内 `js-inline-math`，内容 `$x+y$`；块级 `js-display-math` 加 `display: block`，内容 `$$x+y$$`（定界符保留）。包装发生在服务端，网页端 MathJax 只负责排版——它不做任何"公式在哪"的判定，生死全在服务端。全部判据经 GitHub `POST /markdown` API 实测取得（与 github.com 网页端同一条管线）。

## 一、行内 `$…$`：服务端到底返不返回我的公式

> **验证方法**：按上述判据逐例渲染。**一维扫描**：一次只动一侧——测开侧时闭侧固定空格，测闭侧时开侧固定空格；公式统一 `x+y`，排除转义与斜体的干扰。

### 1. 边界字符（525 例一维扫描，两侧不对称）

| 位置 | 合法（公式被提取） | 阻断（`$` 原样输出） |
|---|---|---|
| 开 `$` **前** | 空格、行首、半角 `(` | 其余一切：可见 ASCII 93 个（字母/数字/下划线/全部标点，含 `[` `{` `<`）、全角 `（`、CJK 汉字、CJK 标点、日韩文字与符号 |
| 闭 `$` **后** | 除 ASCII 词字符外的一切：ASCII 标点（含 `$` `*` `` ` ``）、CJK 汉字/标点、日韩文字符号、空格、行尾 | ASCII 字母 52 + 数字 10 + 下划线 1（共 63，恰为 `\w` 词字符集） |
| 开 `$` **后**（内侧 padding） | —— | 空白，无条件阻断（`$ x+y$`） |
| 闭 `$` **前**（内侧 padding） | 空白可容忍：闭 `$` 后是非空白字符（` $后`、` $。`）或行尾时存活 | 闭 `$` 前是空白**且**后也是空白且其后仍有文字（` $ ` 三明治）时阻断 |

实操：两侧各留一个半角空格——`值为 $x+y$ 个`；公式内侧不贴空白。一条规则覆盖全部场景，其余合法边界（闭 `$` 后贴中文、开 `$` 前贴半角 `(`）上表备查、不必记。

### 2. 容器与标签：公式死在哪个元素里

提取步骤按元素名跳过四个子树：`em`、`b`、`a`、`code`——与样式语义无关（em 死 i 活、b 死 strong 活，闭源实现按实测记录）。四种元素各有来路：`*`/`_` 斜体的产物是 `em`，markdown 链接是 `a`，代码 span 是 `code`，再加上直接手写的标签。哪些元素安全（下表同一文本 `前 $x+y$ 后`、干净边界，只变容器）：

| 提取被跳过（公式死） | 提取正常（公式活） |
|---|---|
| `em`（`*`/`_` 斜体的产物）、`b`、`a`（含 markdown 链接 `[..](..)`）、`code`（代码 span） | `i`、`strong`（`**`）、`del`（`~~`）、`span`、`u`、`s`、`sub`、`sup`、`mark`、`q`、`kbd`、`var`、`cite`、`abbr`、`small`、`li`、`blockquote`、表格单元格（公式内裸 `|` 例外，见第 4 节）、标题、`p` |

手写标签的坑在**未闭合**：结尾由服务端自动补（API 返回的 HTML 里直接可见），补法是"双补"——先在段尾补一个闭合，紧接着**新开一个同名元素**把后续所有块包住，直到文档末尾再补闭合（`…</em></p><em><p>二段 $x+y$ 公式</p>…</em>`）。标签之后的段落、标题、列表就此整块落进跳过集合——公式没写错也死，死因与斜体完全一致：都在 em 子树里。

| 写法 | 公式结果 |
|---|---|
| 单个 `*` / `**`（未配对） | 活（无配对对象，保持字面） |
| 未闭合 `<em>` / `<b>` | 标签之后全部死；公式在标签之前不受影响 |
| 未闭合 `<a>` | 仅标签所在段内死，不跨段 |
| 未闭合 `<i>`（非跳过集合） | 活 |
| 孤立的 `</em>`（无对应开启） | 活（解析器丢弃） |

对策只有一条：**别让公式文本落进 em/b/a/code 子树**——斜体改用 `<i>`、链接文字不放公式、手写标签必须闭合。排查：公式死了但旁边没有定界符，先往回找未闭合的手写标签，再看是否紧邻全角标点（第 1 节开侧规则）。

### 3. 原始 HTML 块（了解特性即可）

**HTML 块**：行首出现块级标签（`<div>`、`<p>`、`<table>` 等）即开启，**直到空行结束**——期间整块原样输出，不做行内解析。它和围栏代码块同类：都是"写什么就显示什么"的块，`$` 在里面只是普通字符。公式死因与第 2 节不同：不是落进跳过集合（被跳过），而是没有文本节点（从未成为候选）。

| 写法 | 公式结果 |
|---|---|
| `<div>前 $x+y$ 后</div>`（单行独占） | 死——整行 = HTML 块 |
| `<div>`＋内容＋`</div>`（多行无空行） | 死——HTML 块到空行才结束 |
| `<div>`＋空行＋段落＋空行＋`</div>` | 活——空行结束块，中间恢复正常段落 |
| `<span>前 $x+y$ 后</span>`（单行独占） | 活——span 非块级标签，不触发 HTML 块 |
| `文字 <div>…$x+y$…</div>`（行首有文字） | 死——整行是段落而非 HTML 块，但行内 `<div>` 标签包夹同样不提取（机制闭源） |

### 4. 内容级杀手：公式文本本身触发过滤（2026-08-24 全量修复实测）

边界干净、无容器、无配对，公式仍然死——行内 `$…$` 的内容命中管线过滤（机制闭源，按实测记录）：

| 杀手 | 实测细节 | 行内 `$…$` | 块级 `$$…$$` | `` $`…`$ `` |
|---|---|---|---|---|
| `select` 子串 | 大小写不敏感、词内也算（`xselected`、`Selector` 同死）；`insert`/`update`/`delete`/`drop`/`union`/`div`/`span`/`script` 等词均安全 | 死 | 活 | 活 |
| `\begin{env}` | `cases`/`aligned` 均验，行内必死 | 死 | 活 | 活 |
| `[x](y)` 链接模式 | 含 `[x]()` 空括号；`$f()$` 无方括号则安全；块级同死（2026-08-30 补测） | 死 | 死 | 活 |

对策：改写标识符避开杀手子串（`selected-version` 改名、`\begin{cases}` 移块级），或 `` $`…`$ `` 兜底（三者全免疫）。

**表格单元格内的管道符**：第 2 节"表格单元格＝活"的前提是单元格不破——公式内裸 `|` 在单元格解析时切分（先于代码 span，`` $`…`$ `` 也救不了），公式跨单元格死。对策：公式内 `|` 一律写 `\|`（送达还原为 `|`，TeX 渲染正常）。

## 二、提取成功 ≠ 送达正确：转义层——`\` 的双重身份

根源一句话：**TeX 数学公式里大量字符必须加 `\`（`\%`、`\{`、`\_`……），而 CommonMark 也用 `\` 做转义，且 Markdown 转义发生在公式判定之前**——于是"为 TeX 写的反斜杠"先被 Markdown 吃掉一层。公式照常提取（边界合法、`math-renderer` 正常生成），但送达 MathJax 的 TeX 源已经坏了：失败模式从"公式不见了"变成"公式在、内容错"，更隐蔽。

| 源码写法 | 送达 TeX | 后果 |
|---|---|---|
| `$M_1 = 10\%$` | `$M_1 = 10%$` | `%` 是 TeX 注释符：静默吃掉其后一切，不报错、百分号消失 |
| `$\text{per\_channel}$` | `$\text{per_channel}$` | `\text{}` 内裸 `_`：TeX 语法错（MathJax 红字） |
| `$\{x\}$` | `${x}$` | 字面花括号变分组符：渲染出来括号消失（2026-08-23 实测） |
| `$a \& b$` | `$a &amp; b$` | 双重转义：`&` 恒送达为 `&amp;`（2026-08-30 实测修正，见对策 4） |
| `\$` | 裸 `$` | TeX Misplaced $；还会连带改写开侧边界字符 |
| `$a \\ b$` | `$a \ b$` | `\\` 塌成 `\`（行内公式内本就少见） |

规律：凡 TeX 要求加 `\` 的 ASCII 标点（`%` `_` `{` `}` `&` `$` `#`），单写必被吃一层。

对策（从简到繁）：

1. **字符移出公式**：`$M_1 = 10$%`——单位与数值分离，最干净。
2. **双写反斜杠**：`$M_1 = 10\\%$`、`$\\{x\\}$`——Markdown 吃一层后恰剩 TeX 需要的 `\%`、`\{`（实测有效）。
3. `\text{}` 里的 `_` 首选改写法（`per-channel`）；`\\_` 只有同段无其他可配对 `_` 时才安全（原因见下）。
4. 硬场景兜底：`` $`x+y`$ `` 代码包裹——内容不经转义层、`_` 不参与配对，第一节与第二节的坑基本免疫；唯一例外是 `&`：行内一切写法（裸 `&`、`\&`、`\\&`、代码包裹）均送达 `&amp;`，对齐符不可得，需对齐环境用块级 `$$…$$`（裸 `&` 送达正常，2026-08-30 实测）。GitHub 专属语法，可迁移性差。

### `_` 的第二条死路：emphasis 配对（与转义无关）

`\_`（单反斜杠）与 `\\_`（双反斜杠）恰好是一对互补的坑：Markdown 把 `\_` 当作转义整体消耗成字面 `_`——它失去定界符资格、不会被配对，但送达 TeX 的就是裸 `_`（语法坏）；`\\_` 里 `\\` 被消耗成字面 `\`，跟在后面的 `_` **没有被转义**，仍是一个活的配对候选——若同段还有另一个能配对的 `_`，两者组成 `<em>` 把公式吞进 em 子树，提取层跳过 em，公式死。

危险取决于同段**其他** `_` 的位置：普通下标 `x_i`、`M_1` 的 `_` 两侧都是字母/数字（intraword），CommonMark 规定这种 `_` 永不参与配对——天生惰性，不构成威胁。真正有配对能力的是**词首/词尾位**的 `_`：`$_{j}$`（公式开头）、`_foo`、`foo_`。所以"`\\_` 单独出现时安全"的确切含义是：同段没有词首/词尾位的 `_`；intraword 规则正是这个条件如此宽松的原因——段落里最常见的下标 `_` 都在豁免之列（2026-08-24 实测：`\\_` 与 `x_i` 同段活；与 `$_{j}$` 同段死，HTML 含 `<em>`）。

## 三、块级 `$$…$$`：同一套管线的另一套答案

前两节的两个问题——"服务端返不返回"（第一节）、"送达内容对不对"（第二节）——对块级公式同样要问，但答案几乎全部改变。核心模型（60 例 + 毒害实测）：**`$$` 块不是独立语法，它就是一个"整段内容恰为 `$$…$$`"的普通段落**，段内照常跑行内解析，段落结构决定生死。专项实测记录见 `全量验证.md`，管线行为见 `Markdown渲染管线研讨.md`。

### 1. 提取判定：不看边界，看段落结构

边界/padding **全免**：`$$ x+y $$`、内容贴定界符行（`$$x+y`）、内容行缩进、界行尾随空格、整块 ≤3 空格缩进——全部正常 display。第一节的开侧/闭侧/padding 规则对独占段落的 `$$` 块一概不适用。

看的是上下文（实测矩阵）：

| 场景 | 结果 |
|---|---|
| 前后真空行（标准写法） | display |
| 前行是标题行 / 围栏闭合行 / 分隔线（无空行） | display——前块自然结束，`$$` 行起新段 |
| 前行是正文（无空行） | 死——`$$` 行并入段落（`$$` 不能打断段落） |
| 前行是列表项 / 引用行（无空行） | 死（多行）——懒延续并入；单行形态见下"退化" |
| 后行是正文（无空行） | 死——文字行并入块段落，段不再纯 |
| 前行是表格行 | 死——`$$` 行被吞成表格单元格 |
| 块内含空行 | 死——段落拆散；空行后 ≥4 缩进行还会变缩进代码块 |
| 同行有前缀或尾巴文字（`文字 $$x+y$$`） | 退化（见下） |

定界符病理：`$$$x+y$$$` 会提取但内容带多余 `$`（TeX 错）；空块 `$$$$` 死。

形态注记（2026-08-30 实测）：上表的"死"按多行块（`$$` 独占行）取得。单行 `$$x+y$$` 在同样上下文（前/后行正文无空行、表格行之后，含第 4 节的引用懒延续场景）不会死，一律按第 2 节"退化"处理——包成 `js-inline-math`、内容保留 `$$`。

### 2. 静默退化：提取了，但不是你要的形态（行内没有的失败）

混排单行、边界干净时（`值 $$x+y$$ 值`、列表项内单行、表格单元格内），服务端**照常提取**——但包成 `js-inline-math`、内容保留 `$$` 定界符，MathJax 收到 `$$x+y$$`：视觉异常、不报错、没有任何"提取失败"的迹象。**"公式还在"不等于"块级写法生效"**，判定必须看 renderer 的 class（`math_check.py` 的"退化"类即查此）。

### 3. 送达改写：转义与第二节一致，emphasis 却相反

- 转义层行为与第二节完全一致：`\X` 吃一层、`\\X` 修复有效（`\\%`、`\\{`、`\\_` 块内实测送达正确）；行中 `\\` 塌成 `\`。
- **行尾 `\\` 意外存活**：经"塌缩—重建"往返被加倍回来——多行公式用 `\begin{aligned}` + 行尾 `\\` 可靠。
- emphasis 与第二节相反：行内公式内成对定界符必死；块内会被**重建**——成对 `_` 还原原样（行内致命的配对吞并在块内不致命），但成对 `*` 被改写成 `_`：乘号 `a*b*c` 静默变 `a_b_c`（TeX 双下标错）。对策：乘号一律写 `\cdot`。

### 4. 容器：引用可用，列表有毒

- **引用内**：逐行 `>` 前缀即可，正常 display。两条懒延续死法：引用文字紧邻 `> $$`（缺 `>` 空行）死；`$$` 段后直接跟裸文字行（无 `>` 前缀）被吸入、死。
- **列表项内：不放 `$$` 块**。"空行 + 缩进到内容列"的写法单独成文时有效，但实测存在**文档级毒害**：更早处（含同项内）存在任何已提取公式（行内、退化、正文/引用块）即静默失效；列表整体之后的公式无害；正文与引用内的块免疫。真实文档几乎必有行内公式 → 列表内块级实际不可靠：列表内用行内 `$…$`，块级移到正文。
- em/b/a/code 容器内死（第一节容器表对 `$$` 同样适用）。
