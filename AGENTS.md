# 写作规范：定义与连接

## 总纲

本仓库描述的对象是一个个复杂的技术整体——计算机体系、数据结构与算法、各门编程语言。它们的共性是：**由无数小块通过复杂连接构成**。因此每一篇文档做两件事：

1. **定义**：对每个概念给出第一性原理定义——它是什么、由什么构成、能做什么、边界在哪里。
2. **连接**：沿事物之间真实存在的连接展开描述——物理链路、数据流向、因果依赖、层级归属、时序先后。

每当结构的形状需要辩护——存在岔路或反直觉——先交代**约束**：它回应什么钉死的条件。

判断一句话该不该存在，问两层：

- **语言层**：它描述的是事物本身，还是作者的修辞？
- **结构层**：它在约束—结构—连接链上有自己的位置吗？重复的和偏离的都不该存在。

## 一、约束规范（为什么是这个形状）

1. 约束回答"为什么是这个形状"，仅当形状需要辩护时出场：存在可行替代或反直觉。自明结构不需要约束段。
2. 约束成链不成堆：子结构继承父节已建立的约束，一句话指认；禁止为每个对象单独编造约束。
3. 约束分级：物理定律级、工程经济级、计算下界级、接口契约级、历史遗留级。软件层的约束多以复杂度账 / 资源账形式出现。
4. 区分"约束的产物"与"历史的遗物"：后者明说"这是历史包袱，不是设计"——禁止事后合理化。

## 二、定义规范（第一性原理）

1. 每个新概念首次出现时，先给一句话本质定义，再展开。
2. 定义回答四个问题：是什么（物理实体还是抽象角色）、由什么构成、能做什么、边界（不做什么 / 何时失效）。
3. 区分本质属性与实现细节：本质属性进定义；实现细节可以出现，但必须标明适用范围（哪一代、哪个平台），口径分级按事实规范的三级事实执行。
4. 前置知识纪律：不允许使用尚未定义的机制。不得不提前提及的，就地给一句话简要定义，而不是标注后文位置。简要定义的及格线：说清它是什么、管什么用，且不许撒谎——后文的正式展开只能加深它，不能推翻它。检验方法：捂住后文，读者仅凭这句话能否正确理解当前论述。只有一句话装不下的大概念，才退而标注后文位置。
5. 术语格式统一：中文名（English，ABBR）；同一概念全文同名。

## 三、连接规范（事物的连接，不是内容的连接）

1. 论述顺序 = 事物的连接顺序。沿信号流、层级、因果链走；不允许由一个联想跳到另一个联想。
2. 机制性断言——描述行为、过程、性能的——必须能回答：它和谁相连？谁驱动谁？数据/信号从哪来、到哪去？定义性断言不在此列，它回答构成与边界，归定义规范管辖。
3. 描述一个部件时必须交代接口：上游是谁，下游是谁，中间隔着什么。
4. 跨节引用必须指向真实存在的标题和真实存在的结构关系。
5. 两段话之间如果只能靠"气氛"衔接，说明缺了一个真实的连接——补连接，不补过渡词。

## 四、语言规范

1. 比喻只允许"结构同构"：喻体与本体的连接方式一致，比喻之后必须落回机制，且不许撒谎——不能导出错误的心智模型。
   - 好例：缓存层级 = 漏斗（都是逐级过滤，通过率相乘）。
   - 坏例一（不同构）："虚无缥缈的系统总线"——不对应任何结构。
   - 坏例二（撒谎）："硬件写死的宿命被软件打破"——因果是反的，存储程序是硬件的设计决策。
2. 禁止空转句：没有信息量的感叹、评价、过渡一律删。检验：删掉它论述是否受损？不受损就删。
3. 每个分句都必须能单独验证；长句里夹带的不可验证分句，拆开或删除。

## 五、事实与数字规范

1. 所有数字必须带锚点：年代、平台、型号（如"2020 年 i7-10750H"）。
2. 可推导的数字给出推导路径；同一物理量在同一文档内只允许一个数值口径——确需不同精度时，换算路径必须写出。
3. 区分三级事实：物理常数级（64bit 通道位宽）、典型值（DDR5 7200 MT/s）、举例值（"假设 100 GB/s"）。举例值必须明示"假设/比如"。
4. 无法验证的断言：验证后保留，或直接删除，不允许"听起来合理"地留着。历史遗留内容（早期模型生成、未验证）默认存疑，逐条过。

## 六、修订流程（AI 执行）

每节按以下顺序过：

1. **追问约束**：这节有没有需要辩护的形状？有，约束给出了吗？没有，它是不是自明的？
2. **清点定义**：本节出现了哪些概念？每个是否已定义（本节或前文）？
3. **检验连接**：逐句问"它描述了哪种真实连接？"标出只起修辞作用的句子。
4. **验证事实**：数字、型号、机制断言逐条核对。
5. **输出诊断**（每条标注：保留 / 改写 / 删除 + 理由），与维护者确认后再落盘。
6. 落盘后检查：交叉引用存活、公式规范（math_fix.py）、比喻全部落回机制。

---

排版层（数学公式写法、math_fix.py 验证）见 `.agents/skills/gfm-math/` 与 `公式排版/公式排版规范.md`，与本规范正交。

---

## 附录：全文级扫描（默认不执行）

本级只在两种情况启用：AI 判断问题可能跨节存在时，**先询问维护者**再执行；或维护者主动提出。日常逐节修订不经过本级。

1. 节名与内容是否一致——节名承诺的，内容交付了吗？
2. 节序是否符合前置纪律——概念先定义、后使用？
3. 同一机制是否在多节重复——重复归并到它在约束链上的主场节？
4. 标题层级是否反映真实从属关系？
