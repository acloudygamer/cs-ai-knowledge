# 写作规范：定义与连接

## 总纲

本仓库描述的对象是一个个复杂的技术整体——计算机体系、数据结构与算法、各门编程语言。它们的共性是：**由无数小块通过复杂连接构成**。因此每一篇文档只做两件事：

1. **定义**：对每个概念给出第一性原理定义——它是什么、由什么构成、能做什么、边界在哪里。
2. **连接**：沿事物之间真实存在的连接展开描述——物理链路、数据流向、因果依赖、层级归属、时序先后。

判断一句话该不该存在，只问一个问题：**它描述的是事物本身，还是作者的修辞？**

## 一、定义规范（第一性原理）

1. 每个新概念首次出现时，先给一句话本质定义，再展开。
2. 定义回答四个问题：是什么（物理实体还是抽象角色）、由什么构成、能做什么、边界（不做什么 / 何时失效）。
3. 区分本质属性与实现细节：本质属性进定义；实现细节必须注明"当前实现如此"。
4. 前置知识纪律：不允许使用尚未定义的机制。必须提及的，给一句话临时定义或标注后文位置。
5. 术语格式统一：中文名（English，ABBR）；同一概念全文同名。

## 二、连接规范（事物的连接，不是内容的连接）

1. 论述顺序 = 事物的连接顺序。沿信号流、层级、因果链走；不允许由一个联想跳到另一个联想。
2. 每个断言必须能回答：它和谁相连？谁驱动谁？数据/信号从哪来、到哪去？
3. 描述一个部件时必须交代接口：上游是谁，下游是谁，中间隔着什么。
4. 跨节引用必须指向真实存在的标题和真实存在的结构关系。
5. 两段话之间如果只能靠"气氛"衔接，说明缺了一个真实的连接——补连接，不补过渡词。

## 三、语言规范

1. 比喻只允许"结构同构"：喻体与本体的连接方式一致，且比喻之后必须落回机制。
   - 好例：缓存层级 = 漏斗（都是逐级过滤，通过率相乘）。
   - 坏例："虚无缥缈的系统总线"（不对应任何结构）。
2. 禁止空转句：没有信息量的感叹、评价、过渡一律删。检验：删掉它论述是否受损？不受损就删。
3. 拟人化可以用，但不许撒谎：比喻不能导出错误的心智模型（反例："硬件写死的宿命被软件打破"——因果是反的，存储程序是硬件的设计决策）。
4. 一句话只断言一件事；长句拆到每个分句都可单独验证为止。

## 四、事实与数字规范

1. 所有数字必须带锚点：年代、平台、型号（如"2020 年 i7-10750H"）。
2. 可推导的数字给出推导路径；同一节内同一物理量只允许一个数值口径。
3. 区分三级事实：物理常数级（64bit 通道位宽）、典型值（DDR5 7200 MT/s）、举例值（"假设 100 GB/s"）。举例值必须明示"假设/比如"。
4. 无法验证的断言：验证后保留，或直接删除，不允许"听起来合理"地留着。历史遗留内容（早期模型生成、未验证）默认存疑，逐条过。

## 五、修订流程（AI 执行）

每节按以下顺序过：

1. **清点定义**：本节出现了哪些概念？每个是否已定义（本节或前文）？
2. **检验连接**：逐句问"它描述了哪种真实连接？"标出只起修辞作用的句子。
3. **验证事实**：数字、型号、机制断言逐条核对。
4. **输出诊断**（每条标注：保留 / 改写 / 删除 + 理由），与维护者确认后再落盘。
5. 落盘后检查：交叉引用存活、公式规范（math_fix.py）、比喻全部落回机制。

## 附录：已确认的判例（随修订持续补充）

| 判例 | 问题 | 处置 |
|------|------|------|
| "硬件写死的宿命被软件打破" | 因果反转：存储程序是硬件设计决策 | 已删 |
| "决定数据供给极限的不再是虚无缥缈的系统总线" | 修辞无结构对应 + 忽略缓存层级 | 已改（开篇段重写为通路结构：核心→缓存层级→IMC→通道→DIMM） |
| "只需半个核心就能抽干所有带宽" | 举例值无口径、伪装成断言 | 已改（改为量级表述，指向"需求侧与临界核数"一节口径） |
| "活塞如同节拍"（时钟比喻） | 比喻错位：活塞是动力源不是节拍器 | 已删 |
| "一拍吸入指令和数据" vs "一拍只能服务一方" | 同句内部矛盾 | 已改 |
| "常见误区：插满内存槽 = 带宽翻倍？"（设问标题 + 误区三节独立成块） | 设问修辞代替直接解析；信息量撑不起独立小节 | 已改（删去降频/体感两节，结论内化为供给侧的逐变量检验） |
| "ρ = N/N_critical 代入排队公式得 ∞，系统瘫痪" | 意愿需求（核心永不停顿）与实际需求（会自我萎缩）混为一谈 | 已改（临界核数一节澄清：N_critical 是标尺不是工作点） |
| "DDR5 子通道对小粒度访问延迟更优" | 机制断言未核算：名义服务时间其实翻倍（8→16 拍），优势不在速率 | 已改（收益=真实服务时间方差趋零 + 空泡被兄弟子通道填充、有效供给接近标称；代价单列；后随 M/D/c 节解散移入 1.2 节 D 假设处，"通道利用率上升"改写为"有效供给更接近标称"以区别于 ρ） |
| "拐点条件"节名、"系统撞墙的拐点" | 命名误导：N_critical 是两条假想外推线（永不停顿的线性需求 vs 供给天花板）的交点，真实系统曲线光滑无折点；唯一真实的崩溃点是死亡螺旋，且在 N_critical 之前 | 已改（需求侧与拐点两节合并为"需求侧与临界核数"，相关表述改为"越过临界核数"/"崩溃来得更早"） |
| "需求侧与临界核数"节的跨模型接线（意愿需求命名、前向引用 1.5、"标尺不是工作点"整段讨论） | 假想模型（永不停顿+固定未命中率）与真实模型（排队+自洽）是两个数学对象，跨模型接线让假想量看起来像真实模型的运行参数——正是"错误拼接"判例的结构版 | 已改（本节自足化为纯假想估算：删"意愿需求"命名（下游改用"假想需求/假想口径"）、前向引用、"OS 记账满格"等；下游仅保留同模型的参照用法：1.2 的 ρ=N/N_critical 恒等式、1.4 的"比标尺更早"对比） |
| "结合 M/D/c 排队模型的直观解剖"独立成节 | 名不副实：三个场景只做供给核算、无排队计算；M/D/λ/μ 提前定义却本节闲置（λ 全文只用一次），c 与供给侧重复定义；场景与供给侧引用块、破局段信息重复；DDR5 块混装供给侧事实与模型假设事实 | 已改（节解散：插槽拓扑并入供给侧引用块，Kendall 记号移至 1.2 首次使用处定义，λ/μ 符号退役，场景 3 因与破局段重复而删） |
| "带宽与延迟的桥梁"节名下挂"片上片外带宽落差"+"漏斗机制"两小节 | 名不副实：两小节是需求侧精化（M_rate 从哪来），不是延迟↔带宽桥梁；同一论点拆成两节；AMAT 递推式预告出现两次（一的带宽论证其实不需要延迟公式） | 已改（合并上移入"需求侧与临界核数"一节；递推式首次出现移至 1.1；桥梁节改名"延迟与带宽的统一"，只留统一主题；编号 3.x→1.x，引用约 10 处同步改写） |
| "漏斗思维的核心"引用块；"需求侧精化"独立小节与第二个加框 N_critical | 评判段脱离机制（"滤网""压缩几个数量级"未落回乘积公式）；精化小节与本体本就是同一假想模型，拆成两节徒增接线；精化版加框公式与本体公式只差一次代入 | 已改（并入"需求侧与临界核数"一节：M_rate=M_1M_2M_3 恒等式内联，P_外部请求与漏斗算式的展示公式改行内，第二个加框公式删；"漏斗"比喻保留本体一句并落回"通过率逐级相乘"） |
| "微架构物理漂流"作为 1.3 嵌在统一模型主线中 | 体量占全节近半，其中"前任指针四幕剧"讲投机正确性，与内存墙/带宽无连接；RAT/PRF 定义重复两次 | 已改（整节移出主线，独立为"微架构补篇"置于本节之后，介绍改为回溯式衔接；主线重编号为 1.1–1.4） |
| "死亡螺旋"节："前端冻结后 LSQ 疯狂发送海量请求"、"彻底锁死与数学发散，崩溃更早更惨烈" | 与自身设定的机制矛盾（MSHR 仅 10–16 条，在途请求有硬上限，"海量"不可能），且与同章自洽模型的负反馈结论直接冲突 | 已改（重构为"撞墙的微观过程：为什么更早，又为什么不瘫痪"——供给打折三因素（脏写回双倍带宽/有界投机浪费/DDR4 效率折扣）+ ROB/MSHR 闸门作为负反馈的微观载体；"死亡螺旋"命名退役） |
| 自洽模型 B_demand = N×64/T×M₁M₂M₃ 未标注串行假设 | 隐含"核心一次只有一个 Miss 在途"，与同文补篇讲的 MLP 并发矛盾，理想化未声明 | 已改（补口径说明：一阶模型忽略 MLP，MLP 只放大需求、不改变负反馈方向；另补不动点存在唯一的数学收口） |
| 复查三处：MSHR 封顶（~10 GB/s 量级）与假想口径 100 GB/s 无人点破；"撞墙略早"与"脏写回双倍带宽"定量冲突；"延迟惩罚（~100ns）"把期望项读成单次代价 | 跨节量级矛盾其实是最强论据（假想口径物理不可达）却没接上；"略"字与自身机制矛盾；期望与单次混读 | 已改（1.3 补闭环句点破量级差；撞墙点改"更早，极端写密集最多提前到 N_critical 的一半"；1.1 改"按概率发生、每次发生付出 ~100ns"；另删 1.2 第三次重复的 DIMM 尾巴、补纯访存流假设、排队因子改写真式） |

---

排版层（数学公式写法、math_fix.py 验证）见 `.agents/skills/gfm-math/` 与 `公式排版/公式排版规范.md`，与本规范正交。
