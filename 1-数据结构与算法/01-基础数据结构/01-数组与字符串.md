# 数组与字符串

## 核心要点

- 连续内存存储，O(1)随机访问
- 插入/删除操作平均O(n)，末尾操作O(1)
- 字符串本质是字符数组

## 常用操作模式

### 前缀和

区间查询优化。

```python
# 区域和检索
class PrefixSum:
    def __init__(self, nums):
        self.prefix = [0]
        for num in nums:
            self.prefix.append(self.prefix[-1] + num)

    def sum_range(self, left, right):
        return self.prefix[right + 1] - self.prefix[left]
```

### 差分数组

区间更新优化，将O(n)的区间更新变为O(1)。

```python
class DifferenceArray:
    """
    差分数组：支持区间加法，最后还原原数组
    区间更新: O(1)，还原: O(n)
    """

    def __init__(self, arr):
        self.n = len(arr)
        self.diff = [0] * (self.n + 1)
        # 构建差分数组
        self.diff[0] = arr[0]
        for i in range(1, self.n):
            self.diff[i] = arr[i] - arr[i - 1]

    def range_add(self, left, right, val):
        """
        对区间 [left, right] 的所有元素加 val
        时间复杂度: O(1)
        """
        self.diff[left] += val
        if right + 1 < self.n:
            self.diff[right + 1] -= val

    def restore(self):
        """
        从差分数组还原原数组
        时间复杂度: O(n)
        """
        result = [0] * self.n
        result[0] = self.diff[0]
        for i in range(1, self.n):
            result[i] = result[i - 1] + self.diff[i]
        return result


# 航班预订问题（LeetCode 1109）
def corp_flight_bookings(bookings, n):
    """
    bookings: [[first, last, seats], ...]
    n: 航班数
    使用差分数组 O(n + len(bookings))
    """
    diff = [0] * (n + 1)

    for first, last, seats in bookings:
        diff[first - 1] += seats  # 起点加
        if last < n:
            diff[last] -= seats   # 终点后一位减
        # last == n 时不用减，因为数组长度是 n

    result = [0] * n
    result[0] = diff[0]
    for i in range(1, n):
        result[i] = result[i - 1] + diff[i]

    return result
```

### 二维前缀和

```python
class PrefixSum2D:
    """
    二维前缀和：快速计算子矩阵元素和
    """

    def __init__(self, matrix):
        if not matrix or not matrix[0]:
            self.m, self.n = 0, 0
            self.prefix = []
            return

        self.m, self.n = len(matrix), len(matrix[0])
        self.prefix = [[0] * (self.n + 1) for _ in range(self.m + 1)]

        for i in range(self.m):
            row_sum = 0
            for j in range(self.n):
                row_sum += matrix[i][j]
                self.prefix[i + 1][j + 1] = self.prefix[i][j + 1] + row_sum

    def sum_region(self, row1, col1, row2, col2):
        """
        计算左上角 (row1, col1) 到右下角 (row2, col2) 的子矩阵和
        时间复杂度: O(1)
        """
        return (
            self.prefix[row2 + 1][col2 + 1]
            - self.prefix[row1][col2 + 1]
            - self.prefix[row2 + 1][col1]
            + self.prefix[row1][col1]
        )
```

## 字符串操作技巧

- 字符串不可变，频繁修改用list或StringBuilder
- 回文判断：双指针或反转比较
- 字母异位词：排序或计数比较

```python
# 有效的字母异位词
def is_anagram(s, t):
    if len(s) != len(t):
        return False
    count = [0] * 256
    for c in s:
        count[ord(c)] += 1
    for c in t:
        count[ord(c)] -= 1
        if count[ord(c)] < 0:
            return False
    return True
```
